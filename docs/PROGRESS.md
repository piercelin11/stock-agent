# 目前進度

已完成：資料庫 schema（12 個 model，含 `MonthRevenue`、`FinancialStatement`、`InstitutionalTrading`、`TechnicalIndicator`）建立並套用 migration、`prisma/seed.ts` 執行完成、逐支股票用 FinMind 回補歷史報價（`backfill-daily-quotes.ts`）、技術指標計算（`calculate-technical-indicators.ts`）、全市場篩選（`run-screener.ts`）、全市場報價缺漏回補（`fill-daily-quotes.ts`，TWSE 用 `MI_INDEX` API 可補任意歷史日期，TPEx 用 TPEx OpenAPI 只能補當天）、每日排程主控腳本（`daily-pipeline.ts`，串起缺漏檢查→補齊 TWSE+TPEx→算指標→跑篩選）皆已完成並手動測試成功（2026-08-17）。`fill-daily-quotes.ts` 已排除權證/可轉債寫入，資料庫裡既有的權證/可轉債資料也已清理完畢（2026-08-17）。

**Phase B 擴充（估值/市值/產業熱度）已完成（2026-08-18）**：依 `docs/PLAN.md` 完成 schema migration（`StockValuation`、`IndustryHeatSnapshot`、`Stock.sharesOutstanding`，migration `20260818130854_add_valuation_heat_shares`）、`scripts/calculate-industry-heat.ts`（已回補 20 個交易日）、`scripts/fill-gap-valuation.ts`（TWSE + TPEx 皆可查歷史日期，已抓 2026-08-17 與 08-18）、`scripts/update-shares-outstanding.ts`（已手動跑過一次，TWSE 1095 檔 + TPEx 890 檔）。估值與熱度已整合進 `daily-pipeline.ts`（放在技術指標之後、篩選之前，失敗不中斷 pipeline），冪等性與失敗路徑皆已驗證。股本更新為月頻手動執行，不進 daily pipeline。

**Phase C（候選股深度資料抓取）已完成（2026-08-18）**：`scripts/fetch-candidate-details.ts` 讀取 `data/screener-results/{日期}.json` 的候選股清單，逐支依序抓取籌碼面（`InstitutionalTrading`）、月營收（`MonthRevenue`）、季報（`FinancialStatement`）、新聞（`NewsArticle`/`NewsStock`）四個面向，寫入資料庫。已用 2026-08-18 篩選結果（23 檔候選股）實測成功，零失敗。`NewsArticle.link` 已加上 `@unique` 約束（migration `20260818112801_add_news_article_link_unique`）。

**全市場評分腳本（screen_score）已完成（2026-08-18）**：依 `docs/PLAN.md` 完成 Step 0（`calculate-technical-indicators.ts` 新增 `volatility20d`/`maxDrawdown20d`/`atr20`/`rsi14`/`macdStatus` 五欄位，migration `20260818155230_add_screen_score_indicators`）與 Step 1（新增 `scripts/calculate-screen-score.ts`，九因子加權評分，匯出 `calculateScreenScore(date)`，結果輸出至 `data/screen-score-results/{date}.json`，不寫資料庫）。`rankScore`（橫向百分位排名函式）已用假資料單元測試三種邊界情況（全部有值/部分 null/全部 null）；開發過程中發現並修正一個方向性 bug（`lowerIsBetter=false` 時最大值誤排到最低分，已修正並用台積電市值/成交金額實測驗證為 100 分）。已用 2026-08-18 全市場資料實測（1947 檔一般股票），肉眼核對前 20 名、抽查 PE/PB 因子方向、確認估值缺值股票（如 DR 存託憑證）正確降級為 `naScore` 不崩潰。**尚未接進 `daily-pipeline.ts`**（依計畫先獨立驗證，穩定後再考慮整合）。

**突破強度篩選腳本（breakout-strength）已完成（2026-08-19）**：新增 `scripts/calculate-breakout-strength.ts`，依 `docs/PLAN.md` 實作「觸發 → 資格門檻 → 強度評分」三層架構，回答「今天市場的焦點在哪」，與 `calculate-screen-score.ts` 的九因子橫向排名互補（後者是全市場排序，前者是帶量突破訊號的獨立篩選）。匯出 `calculateBreakoutStrength(date)`，結果輸出至 `data/breakout-strength-results/{date}.json`，不寫資料庫。用 2026-08-18 資料實測（1947 檔一般股票 → 觸發 33 檔 → 通過市值/量能門檻 16 檔），完成 PLAN.md 列的 5 項驗證：候選名單肉眼核對（抽查 2605、5608 確認真實突破布林上軌且量比達標）、firstBar 抽查（確認昨日收盤在帶內對應 100 分）、stats 三數字合理遞減、同日重跑兩次輸出完全一致（純確定性）、`sharesOutstanding` 為 null 的股票會被市值門檻正確擋下且計數 log（今天無實際觸發案例撞上此分支，邏輯已審查確認不會 crash）。歷史視窗（base 位階、proximityToHigh）目前資料庫約 60 個交易日，皆已寫成「視窗上限 N 天、不足時降級並標記 `degraded`」，未來歷史補齊後不需改程式碼即可自動使用滿 240 日視窗。

**launchd 排程驗證 + 三大法人籌碼腳本 + daily-pipeline.ts 改寫（2026-08-23）**：先用一支一次性測試腳本（已刪除）驗證「launchd 定時觸發 → tsx 執行 Prisma 查詢 → 寫入 log」整條流程可行，關鍵結論：`generated/prisma/` 是 Prisma 產生的純 `.ts` 原始檔（非編譯過的 `.js`），plist 裡不能直接用 `node` 執行 `scripts/*.ts`，必須讓 `ProgramArguments` 指向 `node_modules/tsx/dist/cli.mjs`（絕對路徑）、把目標腳本當參數傳入。確認可行後新增 `scripts/fill-institutional-trading.ts`：抓取 TWSE `T86`（三大法人買賣超日報，支援任意歷史日期）+ TPEx `tpex_3insti_daily_trading`（不支援日期參數，永遠回傳「目前最新一天」，跟 TPEx 報價 API 同樣限制）寫入 `InstitutionalTrading`，只 upsert `Stock` 表已存在的一般股票代號（不像 `fill-daily-quotes.ts` 會自動新增股票——籌碼資料不該是新股票的來源）。同時把 `daily-pipeline.ts` 整支改寫成五步：補齊今日 TWSE+TPEx 報價 → 抓今日 TWSE+TPEx 三大法人籌碼 → 抓今日估值（`fillOneDayValuation`）→ 算技術指標 → 算產業熱度，移除歷史缺漏回補與 `runScreener` 兩塊（這些功能仍在，只是改回個別手動執行，不再進 daily pipeline）。

**「非當日資料」防呆邏輯（同日追加）**：起因是 2026-08-23（週日）跑 pipeline 時發現 TPEx 報價/籌碼把上一個交易日（08-21）的舊資料當成「今天」寫入 `DailyQuote`/`InstitutionalTrading`，upsert 到同一天雖無害但容易誤判「今天有交易」。修正後行為：`fillTodayTpex(expectedIsoDate?)` 與 `fill-institutional-trading.ts` 的 TPEx 那段都會比對 API 回傳日期與預期日期，**不符就跳過寫入**（不再寫入舊資料）並印出 `⚠` 開頭的 warning、回傳 `isStaleDate: true`。`daily-pipeline.ts` 在報價步驟後判斷：**TWSE 無資料（非交易日）且 TPEx 也拿不到當日資料時，直接印出訊息並提前結束整支 pipeline**（不跑籌碼/估值/技術指標/熱度，因為兩邊都沒有新資料可用時後面的計算沒有意義）；只要有任一邊有正確當日資料就繼續跑完所有步驟（TWSE、TPEx 交易日曆理論上一致，不會有一邊開盤一邊沒開盤的情況，這裡防的是 TPEx API 本身「回傳日期落後」的資料新鮮度問題，不是市場狀態不同步）。總結區塊新增「今日警告：N 則」統計（來自報價/籌碼兩處 `isStaleDate` 加總），方便一眼確認當天有沒有異常，不用逐行翻 log。log 是 append 累積寫入 `logs/daily_pipeline_stdout.log`／`_stderr.log`，不會覆蓋。

已完整跑過三次驗證：（1）平日交易日資料完整時的正常路徑 exit code 0（2148 檔股票技術指標、44-45 個產業熱度、TWSE+TPEx 籌碼皆正確寫入，抽查 2609/2892 兩檔股票籌碼數字與 API 原始回應逐位元核對正確）；（2）今日（週日）非交易日的提前結束路徑，1.7 秒內正確判斷並結束、警告數正確計為 1；（3）「TWSE 有資料但 TPEx 過期」的混合情境，以 `fillOneDayTwse("2026-08-21")` + `fillTodayTpex("2026-08-22")` 模擬驗證繼續執行的判斷邏輯正確。**尚未串接 `InstitutionalTrading` 到任何 calculate 腳本**——`calculate-screen-score.ts`/`calculate-breakout-strength.ts` 目前都不讀籌碼資料，這次寫入的資料短期內只是存底。**plist（`daily_pipeline.plist`）已建立在專案根目錄，設定每天 17:00 觸發，尚未由使用者手動複製到 `~/Library/LaunchAgents` 並 `launchctl bootstrap`**。

**盤中一次性快照篩選（intraday-snapshot）已完成（2026-08-24）**：新增 `scripts/check-intraday-breakout.ts`，把 `calculate-breakout-strength.ts` 的觸發/門檻/評分邏輯提前套用在盤中即時價格上，資料源是 `mis.twse.com.tw` 的社群逆向工程即時報價端點（非官方文件，欄位可能無預警變動）。一次性手動執行，不排程、不接進 `daily-pipeline.ts`。實作重點：批次查詢（每批 120 檔、間隔 1.5 秒節流）、上市/上櫃前綴直接查 `Stock.market` 判斷（不猜測重試）、`elapsedRatio`（開盤到查詢當下的時間比例）用查詢當下時間現算並 clip 到 `[0.05, 1.0]`，用以把累計量換算成預估全天量；下限與上限分別對應「開盤前」（印警告）與「收盤後」（不印警告，此時預估量即為完整全天量）兩種情境，不合併成一種警告。同時新增 MIS 回傳日期與資料庫最新 `DailyQuote` 日期的錯位偵測（僅做「相不相同」二分判斷，因專案無交易日曆表無法正確排除週末）。為避免兩支腳本各自維護一份評分邏輯，把 `GATES`/`WEIGHTS`/`TRIGGER_VOLUME_RATIO` 等常數與六項評分純函式（`computeVolumeStrength`/`computeBreakoutMargin`/`computeFirstBar`/`computeBase`/`computeProximityToHigh`/`computeMarketWideReturns`/`rankScore`/`clip`）、`fetchHistoryWindow` 抽到新檔案 `scripts/breakout-shared.ts`，`calculate-breakout-strength.ts` 同步改為從此檔案 import（原本的內部定義已移除，程式碼行數從 693 行降到 416 行）。結果不寫資料庫，輸出 JSON 快照至 `data/intraday-breakout-snapshots/{timestamp}.json`（檔名含完整時間戳記，同一天可執行多次不覆蓋）。已實測執行成功（1727 檔查詢、45 檔觸發、22 檔通過門檻），輸出分數與統計數字合理遞減。執行：`npx tsx scripts/check-intraday-breakout.ts`（無參數，本質是「現在」的快照，不支援 `--date`）。

**突破評分機制調整：新增 K 棒型態評分、權重重分配、市值門檻調降（2026-08-24）**：依 `docs/PLAN.md`（已改寫，原「盤中一次性快照篩選」計畫已完成並被此新計畫取代）調整 `scripts/breakout-shared.ts` 共用邏輯，供 `calculate-breakout-strength.ts` 與 `check-intraday-breakout.ts` 共用。改動四項：
  1. **新增 `computeCandleShape`**：依當日 K 棒的 open/high/low/close 算「上影線比例」+「收盤位置」型態分數（無上影線且收最高 = 100 分，floor 40 分），並對收黑（close < open）額外封頂 50 分——修正舊版評分只比較收盤價、無法辨識「爆量突破但收黑或留長上影線」賣壓訊號的結構性缺陷。open/high/low 缺值時降級給 50 分並標記 `degraded`；`high === low`（一字線，如鎖漲停無量）視為最強型態給 100 分。
  2. **`WEIGHTS` 重分配**（方案一：保守等比例縮放）：原六項權重乘 0.85 讓出 0.15 給新增的 `candleShape`，七項總和仍為 1.0（`candleShape: 0.15`、`volumeStrength: 0.17`、`breakoutMargin: 0.1275`、`firstBar: 0.17`、`base: 0.17`、`proximityToHigh: 0.1275`、`relativeStrength: 0.085`）。
  3. **`computeBreakoutMargin` 改為鐘型曲線**：乖離 ≤3% 維持原本 40~100 分線性遞增；超過 3% 後改為每多 1% 扣 5 分（下限 60 分），避免舊版「乖離越大分數越高」邏輯獎勵跳空/追高風險。
  4. **`GATES.minMarketCap` 從 50 億調降至 30 億**（`minVolumeShares` 1000 張維持不變）。
  `firstBar` 邏輯刻意維持不動（使用者確認保留「已站上軌 3 天以上仍給 20 分」緩衝，不做剔除）；盤中量能線性外推的既有偏誤問題本次不處理（優先度最低）。`calculate-breakout-strength.ts` 用收盤 `DailyQuote.open/high/low` 計算；`check-intraday-breakout.ts` 用 MIS 即時回傳的 `o`/`h`/`l`（盤中至今最高/最低）計算，兩者皆為 `computeCandleShape` 的合法輸入但語意不同（後者收盤前仍可能變動）。

三大法人歷史回補腳本 `scripts/backfill-institutional-trading.ts`（2026-08-24 建立）：逐支一般股票用 FinMind `TaiwanStockInstitutionalInvestorsBuySell` 回補歷史三大法人買賣超至 `InstitutionalTrading`，寫法比照 `backfill-daily-quotes.ts`（逐支 API 呼叫、`REQUEST_DELAY_MS` 節流、`BACKFILL_LIMIT` 環境變數測試用）。用意是補上 `fill-institutional-trading.ts` 只能抓當天資料的歷史缺口。**首輪（2026-08-26）**：2025-05-02 起 324 個交易日、589,786 筆、1989 檔。**6 年回補（2026-08-27，見下方「兩支回補腳本改為固定起始日」段落）後，2026-08-28 重新查 DB 確認**：`InstitutionalTrading` 涵蓋 **2020-01-02 → 2026-08-28、約 1476 個交易日、2,662,485 筆**（逐年：2020=349,547／2021=376,427／2022=388,570／2023=392,978／2024=429,336／2025=432,544／2026 至 08-28=293,083；每日檔數 2020 約 1808、2025 約 1983，早期較少屬正常）。與 `DailyQuote`（3,066,466 筆）、`TechnicalIndicator`（3,060,066 筆）同起點，accumulation 回測區間可拉滿 6 年。

**盤後選股 v2「投信吃貨訊號」（accumulation-score）已完成（2026-08-27）**：ROADMAP 第 1 階段。新增 `scripts/calculate-accumulation-score.ts` + `scripts/accumulation-shared.ts`，找「還沒出現第一根突破、但籌碼/技術面在醞釀」的股票，與 `calculate-breakout-strength.ts`（找已發生的突破）互補。

設計經過一輪 LLM 檢視後定案，關鍵決策：
  1. **計分改為乘法結構**（非四因子等權加總）：`最終分數 = 籌碼分數 × 技術就緒係數`。籌碼是主因、技術面只放大或抑制它、不能取代——避免「零籌碼但盤整量縮」的股票靠 c/d 兩項就衝進前排。
  2. **籌碼分數 = 投信分數 × 0.7 + 其他法人分數 × 0.3**。投信分數：近 20 日「買超頻率」（淨買超天數佔比，取代脆弱的連續買超天數——投信實務常穿插一天小賣超，嚴格連續會歸零）＋「買超淨額佔發行量比例」（帶正負號），兩子項各半權重、各自 `rankScore`。其他法人分數：**排除投信**，只算 `sum(外資+自營商淨買超) ÷ sum(volume)`（帶正負號、視窗加總而非逐日比例平均）——排除投信是為了從結構上消除與投信分數的雙重計數，不用事後正交化。
  3. **技術就緒係數**：`壓縮度 × 0.5 + 窒息量 × 0.5`（0~100）線性映射到 `READINESS_FLOOR`(=0.5)~1.0。下限 0.5 刻意設計：技術面完全沒收斂只把籌碼分數打對折、不歸零、不從排名抹掉（連續係數取代 cliff 式剔除，符合「不用硬門檻」原則）。壓縮度分數直接沿用 `breakout-shared.ts` 的 `computeBase`（帶寬歷史百分位＋低帶寬持續天數），與突破腳本口徑一致；窒息量用近 5 日 `volume/volumeMa20` 平均量比（多日平均濾單日雜訊）。
  4. **候選池資格篩選**（pool eligibility，非訊號門檻）：剔除「今日 close > 布林上軌」（與 `calculate-breakout-strength.ts` 觸發條件互斥，兩份清單天生不重疊）與「近 20 日均量 < `MIN_AVG_VOLUME_SHARES`(=500 張)」（避免長期無量的死股因「量/均量」比值天生不穩而系統性霸榜）。`sharesOutstanding` 為 null 不剔除，只讓投信分數的佔比子項降級。

所有待校準參數（`CHIP_WEIGHTS`/`TRUST_SUB_WEIGHTS`/`TECH_WEIGHTS`/`READINESS_FLOOR`/`MIN_AVG_VOLUME_SHARES` 等）集中在 `accumulation-shared.ts`。結果不寫資料庫，輸出至 `data/accumulation-score-results/{date}.json`，`code`+`date` 欄位格式對齊突破腳本輸出——方便日後寫比對腳本驗證「冷水區 top-N 之後 N 天內有幾支真的出現在突破結果裡」的預測力。

**兩支回補腳本改為固定起始日 + 6 年回補接力（2026-08-27）**：`backfill-daily-quotes.ts` 與 `backfill-institutional-trading.ts` 原本用「執行當天往前推 N 個日曆天」（480 / 120），分兩天跑時起始日會不一致。改為讀 `BACKFILL_START_DATE` 環境變數（預設 `2020-01-01`），兩支共用同一個值，`endDate` 仍為執行當天。`PROGRESS_INTERVAL` 由 50 降到 25。
  - 事前實測 FinMind 單支查詢 6 年是否被截斷（2330，`2020-08-01`~`2026-08-27`）：`TaiwanStockPrice` 回 1477 筆＝1477 個交易日、`TaiwanStockInstitutionalInvestorsBuySell` 回 7380 筆＝1476 交易日 × 5 法人分類（比數精確吻合 5，對照組 1 年 260 天 × 5 = 1300 亦吻合），確認「單一 `data_id` + 日期區間」查詢無會影響本專案的筆數上限，**不需要加逐年分段查詢**。
  - 1-stock 冒煙測試：`2020-01-01` 起兩支各寫 1616 / 1614 筆，零失敗。
  - 新增 `scripts/backfill-6y.sh`：四步接力（報價 → 三大法人 → `calculate-technical-indicators.ts` → `calculate-industry-heat.ts --backfill 1600`）。技術指標腳本天生對每支股票的全部 `DailyQuote` 逐日重算、產業熱度用 `--backfill 1600` 涵蓋 6 年約 1477 個交易日，兩者都會把整段 6 年算出來。`set -e` 任一步失敗即停。log 寫到 `logs/backfill_6y_{timestamp}.log`（`tee` 同時輸出）。用 `nohup ... &` 背景執行，休眠／闔蓋只暫停 process、開蓋接著跑（`upsert` 冪等，最壞情況重跑也只是重複打已完成股票的 API）。預估總時長約 7~8 小時。
  - **實際執行（2026-08-27 14:36 → 2026-08-28 02:28，約 12 小時，中途一次休眠斷線 + 一次手動 `SIGSTOP` 暫停半小時）**：步驟 1 報價寫入 3,057,682 筆／失敗 1 支（4104，休眠斷線時正在打該支 API，被 `try/catch` 接住記入失敗清單、腳本續跑）；步驟 2 三大法人寫入 2,661,693 筆／失敗 0；步驟 3 技術指標寫入 3,058,014 筆（22 分鐘）；步驟 4 產業熱度從 2020-02-04 起逐日算到最新（2 分鐘）。
  - **4104 事後修補**：`backfill-6y.sh` 跑完後 4104 的 `DailyQuote` 只有 326 筆（三大法人 1616 筆是完整的），單獨用 FinMind 重抓補回 1616 筆。技術指標步驟 3 是在 4104 只有 326 筆報價時算的，所以順帶把 `calculate-technical-indicators.ts` 加了「可選股票代號參數」（`calculateTechnicalIndicators(codes?: string[])`；CLI 可傳 `npx tsx scripts/calculate-technical-indicators.ts 4104` 只重算那幾支），用它把 4104 的指標補回 1616 筆。無參數時行為不變（跑全市場一般股票）。產業熱度單支缺料對等權平均影響極小，不重算。
  - **休眠／暫停實務**：`SIGSTOP`（`pkill -STOP -f "backfill-institutional"`）凍結 process、`SIGCONT` 解凍，適合幾分鐘～半小時的短暫中斷（要關網路做別的事很適合，凍結期間不會發任何請求）；長時間（過夜）不建議用此法，FinMind keep-alive 連線與 Prisma 連線池閒置太久會失效，恢復後至少丟一支、最壞 `upsert` 連線錯誤讓 process exit 1。實測半小時暫停後解凍無失敗行、正常續跑。`nohup` 起的 job 可直接關終端（保險起見先 `disown -a`）。

實測（2026-08-26）：1955 檔一般股票 → 剔除站上上軌 97、低流動性 1131 → 候選池 727 檔全數評分、零降級。手動驗算 rank 1（3706 神達）的 `finalScore = chipScore × readinessCoef`、`chipScore = trustScore×0.7 + otherInstScore×0.3`、`readinessCoef = 0.5 + 0.5×(squeeze×0.5+quiet×0.5)/100` 三條公式皆逐位元吻合。非交易日路徑（`--date=2026-08-23`）正確印「無 DailyQuote，跳過」並 exit 0。`npx tsc --noEmit` 對新檔案無錯（既有的 `prisma.config.ts` 錯誤與本次無關）。**已知問題待校準**：首版前 30 名偏大型股（台積電、華碩、緯創、奇鋐等），主因是「其他法人集中度」子項對大型權值股的外資穩定流入給高百分位。需依實際排名調整 `CHIP_WEIGHTS` / `READINESS_FLOOR` 等常數，此為 ROADMAP 第 1 階段最後一個待辦（其餘子項已打勾）。**獨立手動執行，不進 `daily-pipeline.ts`**。

**daily-pipeline 對外 fetch 加上 retry + timeout（2026-08-28）**：起因是當天 launchd 跑 `daily-pipeline.ts` 在第一步 `[補齊今日報價]` 抓 TWSE `MI_INDEX`（2026-08-27）時 TLS 連線被中途切斷（`terminated` / `cause: read ECONNRESET`），單一暫時性網路錯誤讓整條 pipeline exit 1。手動重跑即成功（報價 2362、籌碼 1858、估值 1968、技術指標 2148 檔、產業熱度 48 個、0 警告），確認非程式 bug。
  - 新增 `scripts/http.ts`（純函式庫），匯出 `fetchJson<T>(url, options?)`：每次嘗試帶 `AbortSignal.timeout`（預設 30s），對「網路層拋錯（`ECONNRESET`/`terminated`/timeout）」與「5xx / 429」自動退避重試（預設 3 次嘗試，間隔 2s→4s），對「其他 4xx」不重試（用私有 `NonRetriableHttpError` 立即往外拋，不落入 catch 重試迴圈）。全部失敗後拋一個帶 `cause` 與 URL 的 `Error`，讓 `daily-pipeline.ts` 的 `PipelineStepError` cause 仍有意義。
  - 改寫 6 個呼叫點：`fill-daily-quotes.ts`（`fetchTwseQuotes` MI_INDEX、`fetchTpexQuotes`）、`fill-institutional-trading.ts`（`fetchTwseInstitutional` T86、`fetchTpexInstitutional`）、`fill-gap-valuation.ts`（`fetchTwseValuations` BWIBBU_d、`fetchTpexValuations` peQryDate）。各檔原本「非 200 拋中文訊息」由 `fetchJson` 統一處理；**回應內容判斷（非交易日 `stat` / `totalCount` / 空陣列 / 日期核對）仍留在各呼叫端**，`fetchJson` 只負責「拿到 2xx 並 `res.json()`」。
  - 範圍刻意只涵蓋 `daily-pipeline.ts` 會呼叫到的三支。`backfill-*.ts`（逐支 loop、單支失敗不中斷、有人在旁邊看）、`check-intraday-breakout.ts`、`fetch-candidate-details.ts`（皆手動、非排程）不接，要用可自行 import。
  - 驗證：`npx tsc --noEmit` 對新檔案與 6 個改動點無錯（既有 `prisma.config.ts` 錯誤與本次無關）。三支腳本 `--date=2026-08-27` 實跑正常寫入、無多餘 retry log。retry 行為用 `timeoutMs: 1` 強制逾時測試，確認會退避重試 2 次（2s→4s，實測用 `baseDelayMs: 100` 壓縮）後才拋最終錯誤，每次重試印一行 `⚠ fetch 第 N 次重試 {url}`。**注意**：TWSE 對不存在的路徑會回 HTTP 200 + HTML 錯誤頁，此時 `res.ok` 為 true，會在 `res.json()` parse 失敗並進入重試——這是可接受的預設（暫時性維護頁重試合理），非 regression。

**回測前置：0050 基準回補 + scripts/ 目錄重整（2026-08-28）**：ROADMAP 第 3 階段（歷史回測）的前置整理。
  - **新增 `backfill-benchmark-quotes.ts`**：回測要用大盤基準算「選股 vs 大盤」超額報酬，但 `Stock` 表的 `TAIEX` 一筆 `DailyQuote` 都沒有、0050 只有最近 pipeline 寫的 9 筆（`backfill-daily-quotes.ts` 只跑 `securityType=stock`，ETF 從沒被回補）。獨立寫一支專補基準標的（`BENCHMARK_CODES = ["0050"]`），FinMind `TaiwanStockPrice` 抓 2020-01-01 起，只寫 `DailyQuote`、不碰 `TechnicalIndicator` / `InstitutionalTrading`（基準只需要收盤價）。已執行：0050 補了 1612 筆（2020-01-02 ~ 2026-08-28），零失敗。已知精度限制：0050 未還原股價，除息日 `close` 含假跌幅；若基準報酬對除息敏感，日後改抓 `TaiwanStockPriceAdj`。
  - **`scripts/` 依用途分子資料夾**：原本 21 個檔案全平鋪在 `scripts/`。重整為 `pipeline/`（6 支，每日自動）、`screening/`（3 支，選股手動）、`backfill/`（4 支，偶爾手動回補）、`lib/`（3 支純函式庫：`http.ts` / `breakout-shared.ts` / `accumulation-shared.ts`）、`archive/`（4 支已停用，不維護）。用 `git mv` 保留歷史；import 路徑：`../generated` → `../../generated`、pipeline 內 `./http.js` → `../lib/http.js`、screening 內 `./breakout-shared.js` → `../lib/breakout-shared.js`；`data/` 輸出路徑 `join(__dirname, "..", "data")` → `join(__dirname, "..", "..", "data")`。`package.json` 的 `"main": "top20-gainers.js"`（單腳本時代遺留、無意義）順手移除。
  - **移到 `archive/` 的 4 支**：`calculate-screen-score.ts`（全市場九因子排序，已被 breakout + accumulation 取代）、`run-screener.ts` + `screener-conditions.json`（選股 v1 條件式篩選，已被 `calculate-breakout-strength.ts` 三層邏輯取代）、`fetch-candidate-details.ts`（讀 v1 輸出做深度抓取，輸入來源已停用）、`top20-gainers.js`（最早的一次性玩具）。**移除**：`backfill-6y.sh`（6 年回補已一次性完成，四步接力不會再跑）。
  - 驗證：`npx tsc --noEmit` 對移動後的檔案零新增錯誤（既有 `prisma.config.ts` 錯誤與本次無關）；`daily-pipeline.ts` + 5 個 step 模組 standalone typecheck 通過（import graph 解析正確）；`calculate-breakout-strength.ts --date=2026-08-27` 實跑，輸出正確落在專案 `data/breakout-strength-results/`（非 `scripts/screening/data/`）。CLAUDE.md 與 README.md 的腳本清單與使用範例已同步改成新路徑。

**前端 scaffolding（Next.js + Prisma）已完成（2026-08-28）**：ROADMAP 第 2 節。依 `docs/PLAN.md` 搭前端骨架，只搭殼、無業務頁面。五個子項全數完成。

- **步驟 0 煙霧測試（切風險）先做**：純 `app/page.tsx`（不碰 Prisma/CSS）+ 最小 `next.config.ts` + `layout.tsx`，`npm run dev` 出現 "ok"、`npm run build` 過。**結論：Next.js 16.3.3（Turbopack）+ React 19 + TS 7（`^7.0.2`）+ Prisma 7 + ESM（`type: "module"`）這組合直接可跑，不需要降 `typescript` 版本、不需要 `--webpack`。** PLAN §4.5 列的「TS 7 × Next 不相容」風險未發生。Next 首次 `dev` 會自動改寫 `tsconfig.json`（加 `moduleResolution: "bundler"`、`resolveJsonModule`、`esModuleInterop`、`plugins: [{name:"next"}]`、`allowJs`/`noEmit`/`incremental`、`include`），`build` 時再強制 `jsx: "react-jsx"`。
- **`tsconfig.json` 收斂**：採 PLAN §4.3 方案 A——不建 `app/tsconfig.json`，一份根 config 同時服務 `scripts/`（tsx 執行，不看 `moduleResolution`）與 `app/`（Turbopack，需 `bundler`）。手動把 `module` 從 `nodenext` 改成 `esnext`（配合 Next 塞的 `moduleResolution: bundler`，兩者不相容會報錯），`lib` 補 `dom`/`dom.iterable`。`npx tsc --noEmit` 對 `scripts/` 零新增錯誤（比對基準：僅 `prisma.config.ts:12` 一個既有錯誤）。
- **`prisma.config.ts:12` 既有錯誤順手修掉**：`url: process.env["DATABASE_URL"]`（`string | undefined` 不符 `exactOptionalPropertyTypes`）本來 `npx tsc --noEmit` 忽略即可，但 `next build` 對整專案跑 typecheck 會把它當致命錯誤擋住 build。改成 `?? ""`（語意等價——`DATABASE_URL` 沒設本來連線就會失敗；此為 Prisma 官方新版模板寫法）。使用者確認採此方案。
- **`generated/prisma` 的 `importFileExtension = ""`**（PLAN §3/§4.5 預留的坑）：generated client 內部用 `import * as $Enums from "./enums.js"` 這種 nodenext 風格 specifier，Turbopack 打包時不會把 `.js` 映射到實際的 `.ts` 檔 → `Module not found: Can't resolve './enums.js'`。`serverExternalPackages` 只吃套件名、不吃 `generated/prisma` 這種本地路徑，無法用它繞過。解法：`schema.prisma` 的 `generator client` 加 `importFileExtension = ""` 讓內部 import 不帶副檔名，tsx（nodenext，容忍 extensionless）與 Turbopack（bundler）都能解析，`npx prisma generate` 重生。連帶：本專案 import client 一律寫 `from "../generated/prisma/client"`（不帶 `.js`），舊腳本殘留的 `.js` 仍能跑但不一致。tsx 冒煙測試（`import { PrismaClient }`）與全市場 `calculate-breakout-strength.ts --date=2026-08-27` 實跑確認 scripts 未受影響。
- **目錄**：repo 根目錄 `app/`（PLAN §2，不開 `web/`、不做 monorepo）。新增 `app/`（`layout.tsx` 側邊欄殼 + `page.tsx` dashboard + `globals.css` + `globals.css.d.ts`）、`lib/`（`prisma.ts` 單例 + `actions/health.ts` + `actions/poc.ts`）、`components/`（`ui/Card.tsx` + `ChartSmoke.tsx` + `PocRunner.tsx`）、`scripts/_poc/long-task.ts`。
- **Prisma 單例**（`lib/prisma.ts`）：`globalThis` 快取 + `PrismaPg` adapter，檔頭 `import "server-only"`（裝了 `server-only` 套件，<1KB，是唯一能在 build 時擋「client component 誤 import」的機制）。hot reload 連線數測試：連續 touch `app/page.tsx` 觸發 5 次 reload，`pg_stat_activity` 連線數維持個位數不持續增長（每次 `tsx` 檢查腳本本身也會開 1~2 條瞬時連線，非 pool 洩漏）。
- **Server Actions**（PLAN §5，不建 REST/GraphQL）：`lib/actions/health.ts` 的 `getDbHealth()` `Promise.all` 三個 count/findFirst，回傳可序列化 plain object（`Date` 在邊界轉 `YYYY-MM-DD` 字串）。首頁 Server Component 直接 `await`，渲染出 **DailyQuote 3,066,466 / Stock 2,799 / 最新報價 2026-08-28**，與 `npx tsx` 直查 DB 完全一致（核心驗收）。
- **`app/page.tsx` 加 `export const dynamic = "force-dynamic";`**：否則 `next build` 靜態預渲染此頁會在 build time 連本機 Postgres。加了之後 `next build` 成功，`/` 標為 `ƒ (Dynamic)`、build 期間零 DB 連線、零型別錯誤。
- **`import "./globals.css"` × `noUncheckedSideEffectImports`**（PLAN §4.5、§8）：採方案 ①，新增 `app/globals.css.d.ts` 內容 `declare module "*.css";`。不關該旗標。
- **圖表庫定案 Recharts 3**（PLAN §7）：`components/ChartSmoke.tsx`（`"use client"`）畫寫死資料的小折線圖。HTML 內確認 recharts client chunk（`node_modules_recharts_es6_*`）有被載入、`ChartSmoke` client boundary 有 SSR 出來；圖本身因 `ResponsiveContainer` 需要瀏覽器測量 DOM，在 hydration 後才畫出（初始 HTML 沒有 SVG 屬正常）。
- **樣式/版面定案 Tailwind CSS v4**（PLAN §8）：手動裝 `tailwindcss @tailwindcss/postcss postcss` + `postcss.config.mjs` + `app/globals.css` 的 `@import "tailwindcss";`。元件先不引入元件庫，手刻 `components/ui/Card.tsx`（`Card` + `Stat`）。導覽：`app/layout.tsx` 極簡側邊欄，三個佔位連結（Dashboard / Backtest / Screening，後兩個路由還不存在）。固定淺色主題，不做深淺切換。
- **背景任務機制定案**（PLAN §9，只做 PoC）：「Server Action `spawn` detached 子進程 → 子進程每步覆寫 `data/_poc/progress.json` → Server Action 輪詢讀檔 → client `setInterval` poll」。`lib/actions/poc.ts` 的 `startPocTask()` 用 `spawn(process.execPath, [<絕對路徑>/node_modules/tsx/dist/cli.mjs, scripts/_poc/long-task.ts], { detached: true, stdio: "ignore" })` + `child.unref()`（**不用 `npx tsx`**，沿用 `daily_pipeline.plist` 的教訓）。`scripts/_poc/long-task.ts` 跑 10 秒假迴圈每秒寫進度。實測（直接模擬 spawn）：progress.json 從 `current:0` 走到 `current:10 status:"done"`，exit 0。`components/PocRunner.tsx` 按鈕 + 進度條放首頁一角。**`_poc` 整組（`scripts/_poc/` + `lib/actions/poc.ts` + `components/PocRunner.tsx`）在下一份回測 PLAN 開始時刪除，換成真的 Layer 0 runner。** `data/_poc/` 已加進 `.gitignore`。
- **`.gitignore`** 追加 `/.next/`、`/next-env.d.ts`、`/out/`、`/data/_poc/`。**`.env.example`** 新增（列 `DATABASE_URL` + `FINMIND_API_KEY`）。
- **`package.json`** `scripts` 加 `dev` / `build` / `start`，移除無意義的 `test`。新增 deps：`next@16.3.3`、`react@19` / `react-dom@19`、`recharts@3`、`server-only`；devDeps：`@types/react*`、`tailwindcss@4` / `@tailwindcss/postcss` / `postcss`。
- **驗收清單（PLAN §11）逐項過**：步驟 0 煙霧測試 ✓、首頁真實 DB 數字與直查一致 ✓、hot reload 連線數不增長 ✓、`npm run build` 成功無型別錯誤 ✓、`globals.css` 不再報 side-effect 錯 ✓、`tsc --noEmit` 對 `scripts/` 無新增錯誤 ✓、Recharts smoke 圖 SSR+hydration 無 error ✓、背景任務 PoC 進度 0→10 ✓、`calculate-breakout-strength.ts` 既有行為未變 ✓、四份文件同步 ✓。
- **已知 TODO（留給後續）**：`scripts/screening/` 兩支的頂層 `new PrismaClient()`（會多開一個不受 `lib/prisma.ts` 單例管理的連線池）本 PLAN 未動——留給 ROADMAP 3.1「腳本參數化」連同 `check-intraday-breakout.ts` 無匯出一起重構成 `calculateX(date, { prisma, config })` 注入式。因此 health action 只碰 Prisma、不 import 選股函式（不是會爆，是沒必要在 scaffolding 階段引入多開連線池的雜訊）。`_poc` 檔案待刪（見上）。

**套件管理器從 npm 換成 pnpm（2026-08-28）**：使用者要求。步驟：`.npmrc` 加 `node-linker=hoisted`（扁平 `node_modules`，保 `generated/prisma` client / tsx 腳本 / 未來 plist 的絕對路徑照舊解析，代價是放棄 pnpm 嚴格 phantom-dependency 檢查）→ `package.json` 加 `packageManager: "pnpm@8.15.4"`（corepack 鎖版）+ `engines.node: ">=22"` → `pnpm import`（`package-lock.json` → `pnpm-lock.yaml`）→ `rm -rf node_modules package-lock.json` + `pnpm install`（221 套件、4.3 秒、Prisma/esbuild postinstall 正常執行）。驗證全過：`node_modules/tsx/dist/cli.mjs` 仍在扁平路徑（plist 模式不受影響）、`pnpm prisma generate` OK、`pnpm tsx scripts/screening/calculate-breakout-strength.ts --date=2026-08-27` 實跑輸出正確、`pnpm exec tsc --noEmit` EXIT 0、`pnpm build` + `pnpm dev` 首頁 DB 數字照舊（3,066,466 / 2,799 / 2026-08-28）。CLAUDE.md（開發慣例加「套件管理器是 pnpm」段 + 全文 `npx`→`pnpm`）、README.md（環境需求加 pnpm、使用方式加 `pnpm install`、指令全改）已同步。本 PROGRESS 舊段落的 `npx tsx ...` 指令保留原樣（當時屬實，是歷史紀錄）。pnpm 8.15.4 提示可升 11.x，暫不升。

**腳本參數化（回測前置，ROADMAP 3.1）已完成（2026-08-29）**：依 `docs/PLAN.md` 讓兩支選股純函式吃可覆蓋參數 + 可被 Next.js 安全 import，不碰回測引擎本身。

- **新增 `scripts/lib/types.ts`**：一個 `DeepPartial<T>` 遞迴可選型別，`resolve*Config(override?)` 與回測引擎共用。
- **`scripts/lib/breakout-shared.ts` / `accumulation-shared.ts` 各匯出 config 三件組**：`XxxConfig` 型別（分 `gate` 門檻類 + `score` 加權/曲線/視窗類兩子物件）、`DEFAULT_XXX_CONFIG`（從既有 `export const` 常數組出來，數值不變——舊常數全部保留當「單一數值來源」）、`resolveXxxConfig(override?)`（手寫 2 層展開合併，config 結構固定且淺，不用泛型遞迴 merge）。純函式簽章一律多吃「該函式真正需要的最小 config 片段」（如 `weights.chipWeights`），不讀 module-level 常數，也不吃整包 config。
- **§3 門檻歸類決策**：`minMarketCap` / `minVolumeShares` / `triggerVolumeRatio` / `minAvgVolumeShares` → `gate`（決定誰進候選池，Layer 1）。degraded 門檻（`baseMinHistoryDays` / `minInstitutionalDaysRatio` / `minSqueezeVolumeDays`）與視窗天數（`*WindowDays` / `*MaxDays`）→ `score`（不從候選池剔除股票，只讓某分項降級成中性分／改變聚合值，Layer 2）。型別註解標了「回測調大視窗需確認 Layer 0 序列夠長」的 3.2/3.3 隱患。
- **§4 magic number 抽出決策（先全抽，未撞 diff 退路）**：**抽進 `config.score.curves`**——`computeVolumeStrength`（`{lowRatio,lowScore,highRatio,highScore}`）、`computeBreakoutMargin`（`{kneePct,penaltyPerPct,floor}`）、`computeBase`（`{depthWeight,durationWeight,durationCapDays}`）。**維持寫死並加 `// TODO(backtest)` 註記**——`computeCandleShape`（權重僅 0.15，投報率低）、`computeProximityScale`（0.7 下界 / 0.5-0.5 合成）、`computeFirstBar`（離散規則，使用者明確要求保留緩衝）、`computeBase` 的 p25 門檻（改了語意就變）。另把 `firstBarLookbackDays=30`（原寫死在腳本內）與 `naScore=50`（原多處字面量）收進 `config.score`。
- **accumulation 的 `computeBase` 曲線參數**沿用 breakout 預設（壓縮度口徑一致）：`calculate-accumulation-score.ts` 從 `DEFAULT_BREAKOUT_CONFIG.score.curves.base` 取一份 `BASE_CURVE` 傳入。
- **消掉三支 screening 腳本頂層的 module-level `new PrismaClient()`**：改成檔案內 `makePrisma()` + `calculateX(date, { prisma?, config? })`，`const prisma = options.prisma ?? makePrisma()` + `ownsPrisma` 旗標——函式自建的 client 在 `finally` 自行 `$disconnect()`，注入的（Next.js 單例）絕不 disconnect。`main()` 查「最新交易日」用一次性 bootstrap client 查完即關，再把 `Date` 傳入 `calculateX`。
- **`check-intraday-breakout.ts` 從私有 `main()` 抽出 `export async function checkIntradayBreakout({ prisma?, config?, now? })`**（原本完全沒匯出），`main()` 變薄殼。`now` 一併參數化（`options.now ?? new Date()`）供回測注入時間。MIS 抓取常數（`BATCH_SIZE` / `BATCH_DELAY_MS` / `MARKET_OPEN_HOUR` 等）非選股邏輯，維持 module const 不進 config。
- **`.js` import 副檔名**：本次動到的 5 個檔（2 shared + 3 screening）把相對 import 的 `.js` 全去掉（`from "../lib/breakout-shared"`），否則 `scripts/screening/*` 進 Next Turbopack bundler 會 `Module not found: Can't resolve '../lib/breakout-shared.js'`。CLAUDE.md 早已載明「import 時不要帶 `.js`」，這次順手把殘留的改齊（其餘未動到的檔仍有 `.js`，能跑但不一致，之後動到再改）。
- **`writeOutput` 的 `params` 區塊**：accumulation 從「import 進來的預設常數」改成輸出「實際生效的 resolved config」（`{ gate, score }`）；`windowDays` 頂層欄位保留。breakout 的 `gates` / `weights` 區塊改輸出 `config.gate` / `config.score.weights`（`gates` 現在多一個 `triggerVolumeRatio` 欄位）。
- **驗證（PLAN §6/§9 全過）**：改動前對 breakout `2026-08-18`/`2026-08-21`/`2026-08-27`、accumulation `2026-08-26` 用現有程式碼重跑建黃金檔（**發現 `2026-08-18`/`21`/`26` 的既存輸出是舊常數產物、與現行程式碼不符，golden 以現行程式碼重跑為準**）。改動後同指令重跑，四份黃金檔的 `results` 陣列**逐位元相同**（順序 / 每檔每分項 / `totalScore` / `rank` / `degraded`）；一次性 `_verify-param-noop.ts` 確認「傳 `DEFAULT_*_CONFIG`」與「不傳 config」輸出 `results` 相同（驗完已刪）。`pnpm exec tsc --noEmit` EXIT 0。Next import 冒煙：`lib/actions/health.ts` 暫時 import `calculate-breakout-strength` → `pnpm build` 通過（驗後移除）。連線池：`pnpm dev` 下 health action 傳單例呼叫 `calculateBreakoutStrength` 10 次，`pg_stat_activity` 連線數穩定在 4（5→6 為並發下 pg adapter 瞬時擴張，隨即回落，非洩漏）。CLI 行為不變（`--date=` 照舊輸出到 `data/*-results/`）。`data/_golden/` 驗完刪除、`.gitignore` 的暫時項也移除。
- **順手刪 `_poc`**（PROGRESS 2026-08-28 記「回測 PLAN 開始時刪」，本批即回測系列第一份，使用者已確認）：`scripts/_poc/`、`lib/actions/poc.ts`、`components/PocRunner.tsx`、`app/page.tsx` 對 `PocRunner` 的 import 與那張 `<Card>`。`.gitignore` 的 `/data/_poc/` 留著（無害）。
- **已知 TODO（留給後續 PLAN）**：`computeCandleShape` / `computeProximityScale` 曲線參數之後真要校準時再抽（已留 `// TODO(backtest)`）；視窗天數在回測調大時 Layer 0 落地的原始序列長度要 ≥ 新視窗（3.2/3.3）；選股函式實際串進 Server Action / 回測引擎（ROADMAP 4.1 / 回測 PLAN）；`check-intraday-breakout.ts` 的歷史回測受 TPEx 盤中端點限制（見 CLAUDE.md）。

**Layer 0 基準跑 + 資料完整性檢查（回測第 2 批，ROADMAP 3.2/3.3）已完成（2026-08-29）**：依 `docs/PLAN.md`。只做「資料完整性檢查腳本」+「Layer 0 批次歷史模擬引擎」+「背景任務最小骨架」，不碰 Layer 0.5 / Layer 1/2/3 / 回測 UI。

- **fetch helper 抽出（PLAN §3.5）**：把兩支選股腳本「撈 DB + 組視窗序列」的部分抽進對應 shared 檔，Layer 0 與正式跑共用同一份。
  - `breakout-shared.ts` 新增 `fetchTodayQuotes` / `fetchIndicatorsForDate`（從腳本移入）+ `fetchBreakoutRawInputs(prisma, date, windows, codes?)`（回傳每股 `{ quote, indicator, prevTradingDate, firstBarSeries, history, rsCloseSeries }`；`codes` 省略 = 全市場）。`calculate-breakout-strength.ts` 的 `runCalculation` 對應段落（原 147–305 行）刪除、改呼叫 helper。
  - `accumulation-shared.ts` 新增 `fetchAccumulationRawInputs(prisma, date, codes, volumeMa20ByCode, windows)`（從 `buildFactorInputs` 抽，去掉 `buildCandidatePool` 門檻）。`calculate-accumulation-score.ts` 的 `buildFactorInputs` 刪除。
  - **黃金檔驗證**：抽前對 breakout `2026-08-18/21/27`、accumulation `2026-08-26` 用現有碼跑存 `data/_golden/`（4 檔大小與 committed 版一致——這批 committed JSON 沒過期）。抽後同指令重跑，`results` **逐位元相同**。驗完刪。
- **`scripts/backtest/check-data-completeness.ts`（PLAN §4）**：`checkDataCompleteness(range, { prisma?, minTradingDays? })` + CLI。掃三表覆蓋率 + `missingDates`（整段缺日期）+ `thinStocks`（單股實際/應有 < 0.9，`expectedDays` 用該股第一筆 DailyQuote 之後的交易日數 → 區間中途上市的新股不誤報）。`hardFailures` 非空 exit 1。實測 `2024-01-02 → 2026-05-30`：三表覆蓋率 100%、無整段缺、`thinStocks` 441 筆（多為 `institutionalTrading` 對小型/上櫃股缺 3 大法人資料，屬正常；真正信號是 8418/4712/2358 那種連 `dailyQuote`/`technicalIndicator` 都稀疏的），無 hard failure。
- **`scripts/backtest/run-layer0.ts`（PLAN §3）**：`runLayer0(options)` + CLI（`--strategy` / `--start` / `--end` / `--train-end` / `--valid-start` / `--run-id` / `--resume` / `--min-trading-days`）+ `isMain` guard。逐交易日撈 helper → 組 raw-factors 行 → 寫 `data/backtest-runs/{run-id}/raw-factors/{date}.jsonl`（原子寫）+ `config.json`（git hash + dirty + `sharedLibHash` = `sha256(breakout-shared + accumulation-shared + types.ts)` + `windowConfig`）+ `progress.json`（原子寫，每寫完一天覆寫）。開跑前呼叫 `checkDataCompleteness`，hard failure → `status=error` 中止。`--resume` 掃 `raw-factors/` 已有的 `{date}.jsonl` 跳過。
  - **視窗緩衝（§3.3）**：`LAYER0_WINDOW_BUFFER` 常數（不進 config 型別）——`history` 240→260、`rsCloseSeries` 61→76、`firstBarSeries` 30→40、`institutional` 20→30、`recentVolumes` 5→15、`bandwidthHistory` 240→260。記進 `config.json.windowConfig`。**Layer 2 讀 raw-factors 時必須先把緩衝陣列截到 config 視窗長度再算**——`_verify-layer0.ts` 印證 `computeBase` 對 260 vs 240 筆的 percentile / p25 不同（第一次驗證 FAIL 就是忘了截）。
  - **體積退路：改用無損編碼取代 PLAN §2.3 的語意退路**。實測原 `{key:val}` 物件陣列讓 breakout 6 年 raw-factors 達 **~61 GB**（PLAN 估 3.4 GB 的 18 倍！每行 22 KB，其中 `history` 佔 21 KB），accumulation ~21 GB，遠超 §9 的 5 GB 上限。使用者選「方案 A 無損編碼」：`history`/`firstBarSeries`/`institutional` 改**位置對齊 tuple**（`[close, bandwidth]` 等，不用 `{key:val}`）+ **浮點 round 到 6 位小數**（`r6()`，遠低於價格 tick 精度）+ **`history`/`bandwidthHistory` 不存每筆 date**（序列是「從 `prevTradingDate` 起、新到舊、連續交易日」，純函式按位置取值）。**不損任何可重算能力**。實測降到 breakout **~15 GB** / accumulation **~9 GB**（6 年外推，9 交易日試跑實量 `du`）。PLAN §2.3 已同步改寫。真正的語意退路（`history` → `depthPercentile` + `durationDays` 兩中間量）仍留作最後手段，目前不啟用。
  - **accumulation 全市場 OOM 修正**：`fetchAccumulationRawInputs` 原本三個 `findMany` 無 `take`、無日期下界（撈全歷史再截）——正式跑 `codes` 是候選池（幾百檔）沒事，Layer 0 傳全市場（~1976 檔 × 6 年）**heap 爆在第 3 天**（實測 `FATAL ERROR: Ineffective mark-compacts near heap limit`）。試過「`take` + `orderBy [stockCode, date]`」但那在多股查詢上做不到「每股最近 N 筆」（變成前幾檔拿滿、後面被截，黃金檔驗證 FAIL）。定案：**對 `codes` 分批**（`CODE_BATCH_SIZE = 250`），每批各自撈，查詢本身維持原樣。黃金檔重驗逐位元一致。`lib/actions/backtest.ts` 的 spawn 加 `--max-old-space-size=8192` 給 6 年全跑留 heap。
  - **`recentVolumes` 存原始 volume 不存比值**：`fetchAccumulationRawInputs` 加回 `recentVolumesNewestFirst`（原始 volume），Layer 0 存這個而非 `quietVolumeRatioNewestFirst`（已除以 volumeMa20）——否則 Layer 2 改不了 volumeMa20 口徑（PLAN §1.3）。正式跑仍用比值序列，行為不變。
- **小區間試跑 + 交叉驗證（PLAN §6.2）**：breakout `2026-08-15 → 2026-08-27`（9 交易日）→ 每日 jsonl 行數 == 當天 DailyQuote 一般股票數（1971~1976）；隨機抽 2330 `2026-08-27` 手動比對 `close` / `bollingerUpper` / `volumeMa20` / `history[0]` / `prevClose` 與直查 DB 一致；`history` 長度 260 / `firstBarSeries` 40 / `rsCloseSeries` 76 == windowConfig。`_verify-layer0.ts`（一次性，驗完刪）在記憶體套 gate + 七項評分 + 全市場 rankScore 重算 `2026-08-27`，46 檔 `totalScore` 與 `data/breakout-strength-results/2026-08-27.json` **最大誤差 1.2e-5**（純 6 位小數 round 造成，容忍度設 1e-3）→ 確認 Layer 0 存的資料夠算出正確結果。
- **背景任務骨架（PLAN §5）**：`lib/actions/backtest.ts`（`startLayer0Run` spawn `process.execPath` + `--max-old-space-size=8192` + `node_modules/tsx/dist/cli.mjs` 絕對路徑 detached 子進程，`--run-id` 在 spawn 前決定；`getLayer0Progress` / `listBacktestRuns` 讀檔）。`app/backtest/page.tsx`（`force-dynamic`，策略下拉 + 日期輸入 + 觸發鈕 + run 清單）+ `components/BacktestRunner.tsx`（`"use client"`，`setInterval` 2s 輪詢進度條）。`layout.tsx` 的「Backtest」連結已指向 `/backtest`（原本就是）。`pnpm build` 通過（`/backtest` 路由 compile、TS 乾淨）。
- **文件同步**：CLAUDE.md（新增 `scripts/backtest/` 段 + `scripts/lib/` 補 helper + `scripts/` 清單加 `backtest/`）、PLAN.md §2.3（無損編碼改寫）、ROADMAP.md（3.3 三項打勾）、本 PROGRESS。README 待補。
- **`.gitignore`** 加 `/data/backtest-runs/` + `/data/backtest-cache/`。**`data/backtest-runs/` 需手動清**（本批不做 GC）。
- **已知約束 / 留給後續**：Layer 2 調視窗超過 `windowConfig` 記錄值 → 需重跑 Layer 0（本批只在 config 留判斷依據，UI 提示留 3.7）；Layer 0.5 forward-returns cache → 3.4；Layer 1/2/3 記憶體重算 → 3.5；訓練/驗證期鎖定 UI → 3.6；`data/backtest-runs/` GC 策略未做。accumulation Layer 0 每日約 2 分鐘（6 年約 5 小時）、breakout 約 20 秒/日（6 年約 8 小時）——都是背景跑、一次性、之後靠 raw-factors 快取，符合分層設計預期。

**Forward-returns cache + replay + 統計純函式（回測第 3 批，ROADMAP 3.4/3.5）已完成（2026-08-29）**：依 `docs/PLAN.md`。Layer 0.5 forward-returns cache + Layer 1/2 記憶體重算骨架 + Layer 3 統計純函式 + 最小驗證入口。**不做**完整回測 UI（滑桿 / 圖表 / 個股檢視 / 版本比較，留 3.7）、train/valid 鎖定行為（留 3.6）、停損停利模擬、`versions/*.json` 具名保留。

- **`scripts/backtest/build-forward-returns.ts`（PLAN §2）**：`buildForwardReturns(options)` + CLI（`--start` / `--end` / `--horizons` / `--codes` / `--force`）+ `isMain` guard。逐基準日一次 range query 撈「d 之後 40 日曆日內、全市場 close」→ 記憶體按 code 分組取前 N（不足才對該檔補一次單獨 query）→ append `data/backtest-cache/forward-returns.jsonl` + 寫 `forward-returns.meta.json`。「第 N 天」用**該股自己的報價序列數**（停牌股不誤算），不足 N 筆記 `null` 不跳過整行；benchmark（0050）用**全市場交易日曆**、缺日往後找並記 `benchmarkGapDays`。命中判定不寫進 cache。去重：算過的 `(date, code)` 跳過（`rowsSkipped`），`--force` 才重算。每 50 個基準日 flush（臨時檔 + rename）。
  - **小區間驗證（PLAN §6.1）**：`--start=2026-07-01 --end=2026-07-10`（7 基準日）→ 13832 行 == Σ 每日全市場一般股票數（~1976/日）。手動比對 `(2330, 2026-07-01)`：`entryClose=2505`、`ret5=-1.5968…`（+5 交易日 `2026-07-08` close 2465）、`ret10`/`ret20` 與直查 DB **完全一致**；`benchmarkRet5/10/20` 用 0050 全市場日曆算的同期報酬也完全一致。近期停牌股（`entryClose=0`）→ `retN` 記 `null`。去重：同指令再跑 `rowsWritten=0` / `rowsSkipped=13832`，檔案不變。體積：每行約 150 bytes → 6 年約 288 萬行 ≈ 450 MB（在 1 GB 內）。
- **`scripts/backtest/load-forward-returns.ts`**：`loadForwardReturns(cachePath?)` → `ForwardReturnLookup`（`Map<`${date}|${code}`, {ret, benchmarkRet}>` 包成 `get(date, code)`）。動態 parse `retN` / `benchmarkRetN` 欄位（保留加 `ret60` 的空間）。
- **`scripts/lib/backtest-replay.ts`（PLAN §3）**：Layer 1 + Layer 2 純函式，**不 import Prisma、不讀檔**。`parseBreakoutRow` / `parseAccumulationRow`（tuple → 具名）、`sliceWindows`（緩衝陣列截到 resolved config 視窗長度）、`replayBreakout(rows, config)` / `replayAccumulation(rows, config)` + `*Range` 批次版。replay 在「套完 `gate` 篩池」之後才重跑 `rankScore`（breakout RS 母體為全市場例外）；其餘直接呼叫 shared 檔的七項 / 四項評分函式。**唯二格式適配** = parse + sliceWindows。
  - **逐位元對齊 screening 腳本（PLAN §6.2，`_verify-replay.ts` 一次性驗完刪）**：先用現行程式碼重跑 `calculate-breakout-strength.ts --date=2026-08-27` / `calculate-accumulation-score.ts --date=2026-08-26` 覆蓋 golden（committed 版可能 lag），再跑小段 Layer 0（`--min-trading-days=1`）產 raw-factors，replay 讀它比對。結果：**`rank` / `degraded` 與 golden 100% 相同**（46 檔 breakout / 727 檔 accumulation）；`totalScore` 最大誤差 **1.2e-5**（breakout，純 `r6` round 造成）；`finalScore` 2/727 檔（2206、1444）差 **~0.03**——查證（`_probe.ts` 逐檔比 raw vs r6-rounded 的 `computeBase`）：某筆 `bollingerBandwidth` 剛好卡在 `computeBase` 的 p25 門檻附近，`r6` 後跨越門檻讓「低帶寬持續天數」計數差一格（× 0.4 權重 ≈ 0.03 分）。**這是無損編碼的既知代價、不是 replay bug，不影響 rank**。
- **`scripts/lib/backtest-stats.ts`（PLAN §4）**：`computeBacktestStats(picks, forwardReturns, options)` → `BacktestStats`（`overall` / `byTopN` / `byQuarter` / `bySplit` + 每組 `HorizonStats`）。**不碰 DB、不讀檔、不 import React / Prisma**。`HorizonStats`：`n`（該 horizon retN 非 null 的 pick 數）、`hitRate`（`ret > benchmarkRet`，benchmarkRet 為 null 不計入分母）、`avgReturn` / `medianReturn` / `avgExcessReturn` / `winRate` / `profitFactor`（Σ正/|Σ負|，無負→Infinity、無正→0）、`maxDrawdown`（**等權、不重疊部位粗估**：picks 按 date 升冪、retN 相加的累積曲線 peak-to-trough，非資金加權）。命中判定放這裡（cache 不存）。`byTopN` 切點預設 `[10, 30, Infinity]`。`options.split` 給 → `bySplit.train`（`date <= trainEnd`）/ `valid`（`date >= validStart`），不做鎖定行為。
- **`scripts/lib/backtest-stats.test.ts`**：專案**首個單測檔**（`node:test` + tsx，零新依賴）。`pnpm tsx --test scripts/lib/backtest-stats.test.ts` → 7 組手算案例全綠（hitRate/avg/median/winRate/profitFactor 對齊、`retN` null 不影響其他 horizon 且不進分母、maxDrawdown 對已知曲線 (+10,-5,+3,-8) → 10、byTopN 切點只納 rank ≤ N、split 分段筆數、byQuarter 分桶、profitFactor 邊界）。
- **最小驗證入口（PLAN §5）**：`lib/actions/backtest.ts` 加 `ensureForwardReturns(range)`（spawn `build-forward-returns.ts` detached，比照 `startLayer0Run`）+ `runBacktestSummary({ runId, configOverride?, split? })`（**同步、進程內**：讀 `config.json` 拿 strategy → `loadRawFactorsByDate` → `replayXxxRange(DEFAULT/override config)` → 攤平成 `CandidatePick[]` → `computeBacktestStats`）。UI：`app/backtest/page.tsx` 的 run 清單改成每個 `done` 的 run 附 `<BacktestSummary runId>`（`components/BacktestSummary.tsx`，「跑統計摘要」按鈕 → `overall` + `byTopN` 表格）+ `components/ForwardReturnsBuilder.tsx`（「補 forward-returns cache」按鈕 + 日期輸入）。
  - **端到端鏈路驗證（PLAN §6.4）**：`runBacktestSummary("_e2e-breakout")`（`2026-07-01→07-04` 小 run，forward-returns 已建）→ `meta.totalPicks=169`、`overall[5].n=169`、`hitRate=0.467 ∈ [0,1]`、`byTopN[10][5].n=30`、`bySplit.train/valid[5].n=86/83`、`byQuarter=["2026Q3"]`。晚 8 月的 verify run（`_verify-breakout` 等）forward-returns 全 null（區間後不足 5 交易日），屬正常——驗證入口需用較舊區間。
  - **import 邊界**：`lib/actions/backtest.ts` 不 import `build-forward-returns.ts`（含 module-level `dotenv` / Prisma import）以免拉進 Next bundler；horizons 改讀 `forward-returns.meta.json`、fallback `[5,10,20]`。`load-forward-returns.ts` 只 import 型別。`pnpm build` 通過、`pnpm exec tsc --noEmit` 乾淨。
- **`_verify-replay.ts` 已刪**（一次性）。`data/backtest-runs/_verify-*` / `_e2e-*` 與試跑的 `forward-returns.jsonl` 已清（cache 於下次正式跑重建）。
- **文件同步**：CLAUDE.md（`scripts/backtest/` 段補四層資料流概述 + `build-forward-returns.ts` / `load-forward-returns.ts`；`scripts/lib/` 段補 `backtest-replay.ts` / `backtest-stats.ts` / `backtest-stats.test.ts`）、README（`scripts/backtest/` 清單 + 使用方式 + 前端段）、ROADMAP（3.4 三項 + 3.5 五項打勾）、本 PROGRESS。
- **留給後續**：**回測系統剩 ROADMAP 3.6（訓練/驗證期顯式解鎖 + 紅色警示 + 驗證期結果自動落地）+ 3.7（完整 UI：參數滑桿即時回饋 <2s、儀表板圖表、個股檢視、版本比較）**。`maxDrawdown` 精細化（資金加權 / 重疊部位）、benchmark 除息誤差（0050 未還原，改抓 `TaiwanStockPriceAdj` / 加 006208）、forward-returns cache 的 GC（與 `data/backtest-runs/` 同為手動清）皆待日後。

**回測系統擱置（2026-08-30）**：3.6/3.7 曾在 `feat/backtest-ui-3.6-3.7` 分支上做完（訓練/驗證切分 + 顯式解鎖 + `validation-unlock.json` 落地、`ParamPanel` 滑桿 + 300ms debounce、`StatCards` + Recharts `ReturnHistogram` / `HitRateByQuarter` / `TopNBars` / `TrainValidCompare`、`StockDetailTable` + modal、`VersionCompare` + `versions/{name}.json`、`backtest-config-hash.ts`、`HorizonStats.returnDistribution`、`backtest-stats.test.ts` 9 組）。

- **擱置原因**：進 `/backtest/{runId}` 詳細頁時 `runBacktestSummary` 呼叫 `loadRawFactorsByDate`，把**整個 run 的 `raw-factors/*.jsonl` 一次 `JSON.parse` 進記憶體**。實測「2025 全年 breakout」（243 天、每天全市場約 2000 檔、每行帶 `history`(260) / `rsCloseSeries`(76) / `firstBarSeries`(40) 等序列）就讓 Next dev server heap OOM（8 GB 打滿，死在 `JSON.parse` 的 `BuildJsonArray`）。這批雖然加了 module-level `rawFactorsCache`，但 cache 只在「已載入成功」後才幫得上忙，救不了首次載入。6 年更不用談。
- **要修的方向**：`lib/actions/backtest.ts` 改成「逐日 `raw-factors/{date}.jsonl` → `replayXxx` 那天 → 收集候選 pick（小）→ 釋放該日 raw rows」的串流；module cache 改快取「replay 完的 picks」而非 raw rows，key 帶 `configHash`。breakout 的 `relativeStrength` 母體剛好就是「當天全市場」，分日處理不影響正確性。
- **從 `main` 移除的檔案**：`scripts/backtest/*`（run-layer0 / check-data-completeness / build-forward-returns / load-forward-returns）、`scripts/lib/backtest-{replay,stats,stats.test,config-hash}.ts`、`lib/actions/backtest.ts`、`app/backtest/*`、`components/{BacktestRunner,BacktestSummary,ForwardReturnsBuilder}.tsx` + `components/backtest/*`、`app/layout.tsx` 側邊欄 Backtest 連結。**完整實作保留在 `feat/backtest-ui-3.6-3.7` 分支**，`git checkout` 該分支即撿回。`data/backtest-runs/` `data/backtest-cache/`（`.gitignore`）的執行產物直接刪，重跑腳本即重生。
- **`scripts/lib/breakout-shared.ts` / `accumulation-shared.ts` 留在 `main`**：`fetchXxxRawInputs` helper（回測第 3 批為 Layer 0 抽出、選股腳本本身不呼叫）跟著留，不影響 `calculate-{breakout-strength,accumulation-score}.ts`。
- **接下來**：改做 ROADMAP 第 4 節（選股→挑股→觀察清單）。選股頁沒有這個效能問題——是「跑一天」不是「243 天全 parse」，Server Action 同步 import `calculateXxx(date)` 秒回。

**選股 → 挑股 → 觀察清單流程（日常操作殼，ROADMAP 第 4 節）已完成（2026-08-30）**：依 `docs/PLAN.md`。三件事：選股頁 `/screening`、`WatchlistItem` schema 升級、觀察清單頁 `/watchlist`。

- **`WatchlistItem` schema 升級 + migration**：加 `isPurchased Boolean @default(false)` / `buyPrice`/`targetPrice`/`stopLossPrice`（`Decimal(10,2)?`）/ `buyDate DateTime? @db.Date` / `source String?`（記錄從哪個策略加入）。`source` 是 ROADMAP 沒明列、本批加的——供觀察清單頁分組 + 日後盤中提醒選訊號邏輯。migration `20260830085717_add_watchlist_purchase_fields` SQL 只有 6 個 `ADD COLUMN`（全 nullable / `@default`），既有資料無痛升級。
- **選股純函式回傳擴充**：`calculateBreakoutStrength` / `calculateAccumulationScore` 原本**只回傳統計摘要**，候選名單 `results` 只 `writeFileSync` 到 `data/*-results/{date}.json`。本批把 `BreakoutResult` / `AccumulationResult` 從檔內 `interface` 改 `export interface`，回傳型別加 `results: XxxResult[]`，三個 `return` 點（非交易日 / 空候選池 / 正常）都補 `results`（空分支給 `[]`）。**落地與 CLI 輸出完全不動**——重跑 `--date=2026-08-27`（breakout）/ `--date=2026-08-26`（accumulation），`data/*-results/` 逐位元不變（`diff` 無輸出）。
  - **為什麼擴充回傳而非讓前端讀 `data/*-results/{date}.json`**：避開「committed JSON lag 現行程式碼」的記憶問題（`MEMORY.md` 記過），且前端不需碰檔案系統。
- **`lib/actions/screening.ts`**：`runScreening({ strategy, date? })` → `ScreeningResult`（`breakoutRows?` / `accumulationRows?`）。決定日期：`input.date` 給了就用，否則查 `DailyQuote` 裡 `securityType="stock"` 的 `max(date)`（= 最新交易日）。**注入前端 Prisma 單例**（`calculateXxx(target, { prisma })`）——純函式偵測 `options.prisma !== undefined` 時 `ownsPrisma=false`、不 `$disconnect`（單例是 `globalThis` 快取的，disconnect 會害其他請求）。**同步跑不 spawn**：單日約 1900 檔、秒級（背景任務模式是給幾百日回測的）。純函式回傳已全是 `number`/`string`（無 `Decimal`/`Date`/`BigInt`），直接挑欄位組 Row。`date?` 參數預留給日後開歷史查詢，UI 本批不接。
  - **E2E 驗證**：最新交易日解析為 `2026-08-28`；breakout 回 41 rows（top1 `6179` total 83.2）、accumulation 回 697 rows（top1 `3706` final 83.1）；注入的 prisma 單例在兩次呼叫後仍可用（`stock.count()` 正常）——確認純函式沒 disconnect。
- **`lib/actions/watchlist.ts`**：`listWatchlist` / `addToWatchlist` / `removeFromWatchlist` / `updateWatchlistItem`。
  - `listWatchlist`：`watchlistItem.findMany` + 對每檔三表（`dailyQuote` / `technicalIndicator` / `institutionalTrading`）各 `findFirst`（`orderBy date desc`）。N 檔 → 3N 查詢（清單 < 50 檔可接受）。三表 `date` 不對齊 → 各取各自最新一筆，UI 各標各自日期。**邊界轉換**：`Decimal` → `.toNumber()`（`buyPrice` 等）、`BigInt`（`volume` / `*NetBuy`）→ `Number(...)`、`Date` → `.toISOString().slice(0,10)`。
  - `addToWatchlist`：先 `stock.findMany({ where: { code: { in } } })` 過濾不存在的 code（避免 FK violation），再 `createMany({ skipDuplicates: true })`，回 `{ added, skipped }`。
  - `updateWatchlistItem`：動態組 `data`（`undefined` 的 key 不放、`null` 放進去表示清空）；`buyDate` 收 `YYYY-MM-DD` 字串 → `new Date(...)`。
  - 全部 mutation 後 `revalidatePath("/watchlist")`（Next.js 16 `07-mutating-data.md` 標準做法）。單人本機、不做 auth。
  - **E2E 驗證**：add（含無效 code 過濾 + dup skip）、update（`isPurchased`/`buyPrice=123.45` Decimal/`buyDate` Date/`notes`）、三表 snapshot 查詢（`1101` 拿到 close 24.3 / ma20 / rsi14 / foreignNetBuy）、remove 全部逐項通過。
- **前端頁面**：`app/screening/page.tsx`（Server Component 殼，`force-dynamic`）+ `components/screening/ScreeningPanel.tsx`（`"use client"`：兩策略 tab、`useTransition` 跑按鈕、可排序表格、點列 `expandedCode` 行內展開評分明細、checkbox `Set<string>` 勾選 → `addToWatchlist`、非交易日 / 空結果提示）。`app/watchlist/page.tsx`（`force-dynamic`，`listWatchlist` → `WatchlistTable`）+ `components/watchlist/WatchlistTable.tsx`（`"use client"`：每檔一卡片，當日三表快照 + `isPurchased` toggle + 買入價/買入日/目標價/停損價 `onBlur` 存 + `notes` textarea + 移除；漲跌色台股慣例漲紅跌綠；`*NetBuy` /1000 顯示「張」）。`components/ui/Button.tsx`（最小抽象）。`app/layout.tsx` `navLinks` 加 `{ href: "/watchlist", label: "Watchlist" }`。
- **typecheck / build**：`pnpm exec tsc --noEmit` 乾淨（`SnapshotBlock` 的 `date?` prop 因 `exactOptionalPropertyTypes` 要顯式寫 `string | undefined`）。`pnpm build` 通過——**§5.5 擔心的 `scripts/screening/*` 的 `node:fs` / `dotenv/config` import 炸 Turbopack bundler 沒發生**（Server Action 檔在 server bundle 跑，`node:fs` 沒問題；`dotenv/config` 在 Next 環境 no-op）。`/` `/screening` `/watchlist` 三頁 curl 皆 200。
- **舊行為不變**：三支 screening 腳本 + `daily-pipeline.ts` CLI 輸出與落地照舊。
- **文件同步**：CLAUDE.md（`WatchlistItem` 條目補新欄位、前端段補 `/screening` `/watchlist` 頁 + `lib/actions/` 補 `screening.ts` / `watchlist.ts`、`screening/` 兩腳本補「回傳含 `results`」）、README（前端段 + `scripts/screening/` 兩腳本 + 使用方式）、ROADMAP（4.1 三項 + 4.2 四項打勾），本 PROGRESS。
- **留給後續**：尾盤 LLM 分析（依買入狀態判斷該買/該賣，ROADMAP 第 4 節 Note 明訂暫不排）、盤中提醒 / 通知管道 / 排程（ROADMAP 第 5 節，依賴觀察清單有內容 = 本批完成後才有前提）、選股跑任意歷史日期（`runScreening` 留 `date?` 參數，等回測系統撿回）、候選股深度抓取（月營收 / 季報 / 新聞，之後獨立 PLAN）、參數調整 UI（滑桿覆蓋 config，回測 3.7）。

**盤中即時掃描 tab（背景任務模式，`/screening` 第三分頁）已完成（2026-08-30）**：依 `docs/PLAN.md`。ROADMAP 沒明列的加項（第 4 節聚焦盤後、第 5 節的盤中是「排程 + 通知」）。背景任務模式在 `main` 上的**首個正式落地**（PoC 已於 3.1 刪）。

- **`check-intraday-breakout.ts` 進度回報 + 回傳擴充**：`CandidateResult` 改 `export`；新增 `IntradaySnapshotOutput`（`results` + `stats` + `warnings` + `queriedAt` + `elapsedRatio`），回傳型別從 `Promise<void>` 改成它，兩個 `return` 點（空候選 / 正常）都回。`fetchAllMisQuotes` 加可選 `onBatch(done, total, failedSoFar)` callback；`runSnapshot` 傳 callback → 每批原子覆寫（`.tmp` + `renameSync`）`data/intraday-breakout-snapshots/progress.json`（`status`/`phase`/`fetchedBatches`/`totalBatches`/`warnings`）；抓完進 `phase: "scoring"` 覆寫一次。現有兩個關鍵 `console.warn`（開盤前 `elapsedRatioRaw < 0.05`、MIS 日期與 DB 最新相同）**同時 push 進 `warnings` 陣列**寫進 `progress.json` + 結果檔。落地行為不變（結果檔多一個無害的 `warnings` 欄位）；`console.table` 輸出照舊。CLI 直跑（2026-08-30 週六 = 非交易日）：`progress.json` `fetchedBatches` 0→18 遞增到 `done`，結果檔正常產出，warnings 含「MIS 日期與 DB 最新相同」。
- **`scripts/screening/_run-intraday-scan.ts`（新，薄 runner）**：被 `startIntradayScan()` spawn 的內部進入點（底線前綴 = 非手動）。建 `prisma` → `checkIntradayBreakout({ prisma })` → 最外層覆寫 `progress.json` 的 `done` / `error` 終態（逐批進度由 `checkIntradayBreakout` 內部負責，職責分工）。`process.exitCode = 1` on error，`finally` `$disconnect`。不 `isMain` guard（就是為了被 spawn）。CLI 直跑驗證通過。
- **`lib/actions/intraday.ts`（新）**：`startIntradayScan()` / `getIntradayProgress()` / `getIntradayResult()`。
  - `startIntradayScan`：先讀 `progress.json`，若 `status === "running"` 且 `updatedAt` 在 90 秒內 → 回 `{ started: false, reason: "已有掃描進行中" }`（防重複 spawn；超過 90 秒視為死掉的舊掃描允許重跑）。spawn **前**先原子寫一份 `running`（子進程啟動有幾秒延遲，讓 client 第一次輪詢就拿得到狀態）。`spawn(process.execPath, [tsxCli, script], { detached, stdio: "ignore", cwd })` + `unref()`，`tsxCli` = 扁平 `node_modules/tsx/dist/cli.mjs`。
  - `getIntradayResult`：`readdirSync` filter `/^\d{4}-\d{2}-\d{2}T.*\.json$/`（排除 `progress.json`）→ 字典序最大 → parse → 挑欄位組 `IntradayRow`（`CandidateResult` 已全是 `number`/`string`，直接挑）。
  - **import 邊界**：只 `import type { CandidateResult }`——不 import `checkIntradayBreakout` 本體（它 module-level `import "dotenv/config"` + `node:fs`）。`pnpm build` 確認沒把腳本拉進 Turbopack bundle。
  - **驗證**：連按兩次 `startIntradayScan` → 第二次 `{ started: false, reason: "已有掃描進行中" }`；輪詢期間 `getIntradayProgress` 回 `running` + `fetchedBatches` 2→18 + phase `fetching-quotes`→`scoring`；`getIntradayResult` 回 37 rows、`JSON.stringify` 不丟錯、warnings 傳到；runner 收尾寫 `done`。
- **`components/screening/ScreeningPanel.tsx`**：tab 值擴成 `Tab = ScreeningStrategy | "intraday"`（元件內 local 型別，不動 `runScreening` 的 `ScreeningStrategy`）。`STRATEGY_LABELS` → `TAB_LABELS` 加 `intraday: "盤中即時掃描"`。intraday 專屬 state（`intradayProgress` / `intradayResult` / `intradayError` / `scanBusy` / `pollRef`），不放進 `results: Partial<Record<...>>`。`startPolling`/`stopPolling` 用 `useRef` 存 interval；輪詢 `setInterval(2s)` → `getIntradayProgress` → `done` 時 `getIntradayResult` + `clearInterval`、`error` 時顯示。`useEffect([tab])`：切到 intraday 時若 `progress.json` 還 `running` 就重新輪詢（切走只 `clearInterval`、**不中斷 detached 子進程**）。純 Tailwind 進度條（`<div>` width %）。常駐警語列（「與盤後結果不可直接比較」「僅盤中 09:00–13:30」）。`result.warnings` 非空 → 紅字列出。跑完重用 `intradayColumns`（標題「即時價」「量比(估)」，`Column` 型別 / 排序 `useMemo` / `<Detail>` 7 分項渲染 / 勾選 `Set<string>` 全共用）；`<Detail>` 的 `tab === "breakout" || tab === "intraday"` 走同一分支。`addToWatchlist({ codes, source: "intraday" })`。空結果（`status === "done"` 且 `rows.length === 0`）→「本次快照無符合條件的候選股」。
- **`app/screening/page.tsx`**：說明文字補「『盤中即時掃描』為背景執行，可切走再回來看進度」。
- **typecheck / build**：`pnpm exec tsc --noEmit` 乾淨；`pnpm build` 通過，`/screening` 仍 `ƒ (Dynamic)`。
- **舊行為不變**：盤後兩 tab（同步 `useTransition`）行為完全不變；三支 screening 腳本 CLI/落地照舊（`check` 結果檔多 `warnings`）；`daily-pipeline.ts` / `/` / `/watchlist` 不受影響。
- **文件同步**：CLAUDE.md（`check-intraday-breakout.ts` 條目補回傳型別 + progress.json、新增 `_run-intraday-scan.ts` 條目、前端段 `/screening` 補第三 tab 背景任務描述、`lib/actions/` 補 `intraday.ts`、「背景任務」段 PoC → 正式使用者）、README（`scripts/screening/` 兩條 + `lib/actions/` + 前端段 + 使用方式註解）、ROADMAP（第 5 節前加註記），本 PROGRESS。
- **留給後續**：排程自動盤中掃描 + Telegram 通知 + 「大漲大跌」判斷（ROADMAP 第 5 節，依賴 DB 外部連線 + 通知模組）、`data/intraday-breakout-snapshots/` 結果檔 GC（手動清）、盤中訊號歷史回測（回測系統撿回後，受 TPEx 盤中/歷史端點限制）、多實例部署時 `progress.json` 在本機檔案系統（單機 `localhost` 前不考量）。

**Dashboard 改版 + 全站黑暗模式已完成（2026-08-31）**：依 `docs/PLAN.md`。三件事：整站切固定深色、重寫 `/` DB 連通性區塊、移除 Recharts smoke 換「觀察類股今日表現」。

- **全站黑暗模式（無 toggle）**：`app/globals.css` `:root { color-scheme: dark }` + `body` 改 `bg-slate-950 text-slate-100`。逐檔換寫死類名：`app/layout.tsx`（側欄 `bg-slate-900` / `border-slate-800` / nav `text-slate-300 hover:bg-slate-800`）、`components/ui/Card.tsx`（`bg-slate-900 border-slate-800`、拿掉 `shadow-sm`、`Stat` value → `text-slate-100`）、`components/ui/Button.tsx`（`primary` = `bg-blue-600 text-white`、`secondary` = `bg-slate-800 border-slate-700`、`danger` = `border-rose-800 bg-slate-900 text-rose-400`）、`ScreeningPanel.tsx` + `WatchlistTable.tsx`（表格 `divide`/表頭 `bg-slate-800/50`、選中列 `bg-blue-950/40`、提示框改深色、漲跌色 `rose-500/emerald-500` → `rose-400/emerald-400`、`<input>`/`<textarea>` 補 `bg-slate-800 border-slate-700 text-slate-100`）、三個頁面 `text-slate-900` 標題 → `text-slate-100`。驗收 grep（`bg-white\|slate-50\b\|slate-100\b\|slate-200\b\|text-slate-900`）殘留全是「深色底下合理」的 `text-slate-100/200`（亮字）。
- **`getDbHealth` 換回傳（`lib/actions/health.ts`）**：舊 `{ quoteCount, stockCount, latestQuoteDate }` → `DbHealth`（`today` / `latestQuoteDate` / `quoteFresh` / `stockCount` / `coverage.{quote,institutional,technical}.{count,pct}`）。**燈號判定** = `latestQuoteDate === today`，`today` 用 `Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Taipei" })`（不用伺服器 local time）。**覆蓋率分母** = `stock.count({ where: { securityType: "stock" } })`（跟 `runScreening` 一致，非全 `Stock` 表）。**覆蓋率基準日** = DB 最新交易日 `refDate = new Date(latestQuoteDate + "T00:00:00.000Z")`（非交易日 `today` 無資料會全 0，用 DB 最新那天才有意義）。三表各 `count({ where: { date: refDate, ... } })`（`institutional`/`technical` 只 upsert 一般股票不必再過濾）。`pct = round(count/denom*1000)/10`。實測（2026-08-31，DB 最新 08-28）：紅燈 + 2148 檔 + 覆蓋率 91%/86.7%/91%。
- **Dashboard 新區塊「觀察類股今日表現」**：`lib/actions/dashboard.ts` 的 `getWatchlistPerformance()` → `WatchlistPerfRow[]`。**採 PLAN §3.1 的方案 (B)**：不重跑 `calculateBreakoutStrength`（其 gate 會濾掉未突破的觀察股 → 交集太少，不符「列出每一檔」），改對 watchlist 每檔用 `breakout-shared.ts` 的 pure helper 現算，不經 gate。每檔查：最新 `DailyQuote`（沒有就濾掉）→ `refDate` 往前 21 筆 volume（算 20 日均量）+ `refDate` 往前 `baseMaxWindowDays+1` 筆 `TechnicalIndicator`（`bollingerBandwidth` / `bollingerUpper`）+ 最新 `InstitutionalTrading`。面向：`candleScore = computeCandleShape({open,high,low,close}).score`（degraded → null）、`volumeScore = computeVolumeStrength(todayVol/avg20Vol, curves.volumeStrength)`、`baseScore = computeBase(latestBandwidth, bandwidthHistory[1..], baseMinHistoryDays, curves.base).score`（**注意 `computeBase` 吃布林帶寬序列不是 close 序列**——PLAN §3.2 表格寫「近 240 筆 close」是筆誤，實作對齊 `calculate-breakout-strength.ts` 的既有用法；degraded / 不足 40 筆 → null + `degraded.push("base")`）、`aboveBollingerUpper = close >= 當日 bollingerUpper`、`instTotalNet`/`trustNet` = 三欄（單位股）`/1000` round 成「張」。curve 參數用 `resolveBreakoutConfig().score.curves.*`。`components/dashboard/WatchlistPerfTable.tsx`（Server Component，`await getWatchlistPerformance()`，純展示無互動）：欄位 `代號/名稱 | 收盤 漲跌% | K棒 | 動能 | 力道 | 籌碼(合計/投信) | 位階`；分數欄 `>=70` `emerald-400` / `40~70` `slate-200` / `<40` `slate-500`，null → 「—」+ degraded 時加「資料不足」tag；動能欄漲跌%台股色 + `aboveBollingerUpper` 時加藍色「突破」pill；空清單「觀察清單為空，先到 Screening 加入標的」。
- **移除 Recharts smoke**：`app/page.tsx` 刪 `<Card title="Recharts smoke...">` + `import { ChartSmoke }`，刪 `components/ChartSmoke.tsx`。`package.json` 的 `recharts` 依賴**保留**（日後 screening 展開明細等可能用）。
- **`app/page.tsx` 重組**：`getDbHealth()` → 「資料狀態」Card（`grid sm:grid-cols-3`：燈號圓點 + 文字 / `一般股票檔數` Stat / 三表覆蓋率純文字 + 顏色，`<50` `rose-400` / `<90` `amber-400` / 其餘 `slate-100`）+ 「觀察類股今日表現」Card。`export const dynamic = "force-dynamic"` 保留。
- **typecheck / build**：`pnpm exec tsc --noEmit` 乾淨；`pnpm build` 通過（`/` 仍 `ƒ (Dynamic)`）；`pnpm dev` 三頁 curl 200，`/` HTML 含「資料狀態」「觀察類股今日表現」、無「Recharts」殘留。
- **import 邊界**：`lib/actions/dashboard.ts` 從 `scripts/lib/breakout-shared.ts` import pure helper（`screening.ts` 已透過 `calculate-breakout-strength.ts` 間接 import 過同一檔，Turbopack bundle 無問題）。
- **舊行為不變**：`/screening`、`/watchlist` 只換配色，結構 / 互動 / 資料完全不動；腳本零改動。
- **文件同步**：CLAUDE.md（前端段補「全站固定深色（無 toggle）」「`/` Dashboard = 資料狀態卡 + 觀察類股今日表現表」「`lib/actions/dashboard.ts`」）、README、ROADMAP（第 4 節加註記），本 PROGRESS。
- **留給後續**：light/dark 切換 UI（主題 toggle + 記憶偏好，本批明確不做）、「觀察類股今日表現」的排序 / 展開明細 / 歷史留存（本批純展示）、`computeFirstBar` 面向（需另拉 `bollingerUpper` 歷史序列，本批用「當日是否站上上軌」代替）、完整 accumulation chipScore（本批籌碼只顯示當日淨額）。

**「觀察類股今日表現」改卡片式 + 60 日走勢圖（2026-08-31，同批延伸）**：使用者要求把表格改成卡片，左側放 60 日線圖，且「每張卡的起伏幅度可比、不被個股絕對振幅誤導（避免技術圖那種因振幅不同而誤判盤整寬度的問題）」。

- **走勢圖 Y 軸 = 布林帶寬正規化**：每點 = `(當日 close − 當日 bollingerMid) / bollingerMid`（＝收盤偏離 20 日均線的百分比），固定夾在 ±`SPARK_CLIP`(=0.25)，**所有卡共用同一刻度**。布林通道寬度本身就是近 20 日波動度 → 波動大的股票 `bollingerMid` 一起放大，偏離比例仍歸一。效果：盤整期（close 貼中軌）線壓在中線窄帶、噴出時線衝到 ±邊界，卡跟卡之間「盤整多扁 / 噴多兇」直接可比。跟位階分數（`computeBase`，同樣基於布林帶寬）同源 → 線圖形狀 = 位階分數的視覺化解釋。**評估過另兩案並否決**：min-max 正規化（每卡各自拉滿 0~1）就是使用者要避免的技術圖老問題；固定百分比區間（相對 60 日前收盤）沒有「盤整寬度」資訊。
- **`lib/actions/dashboard.ts`**：`quoteWindow` 撈量從 `take: 21` 改 `take: max(21, SPARK_WINDOW=60)` 並多選 `date` / `close`；`indicatorWindow` 多選 `bollingerMid`。新增 `spark: SparkPoint[]`（舊→新，每點 `{ dev: number | null }`，把 `quoteWindow` 前 60 筆的 `close` 對到同日 `bollingerMid` 算偏離比例並夾邊；無 `bollingerMid` 該點 null）。
- **`lib/dashboard-spark.ts`（新，非-server）**：`SPARK_CLIP` / `SPARK_WINDOW` const + `SparkPoint` 型別。**原因**：`lib/actions/dashboard.ts` 檔頭 `"use server"`，Next 只准匯出 async function，`export const SPARK_CLIP` 會讓 `next build` 報 `the requested export doesn't exist`（`tsc` 不會擋，只有 build 擋）。常數同時被 action（算資料）與元件（畫 SVG）用 → 抽獨立檔。
- **`components/dashboard/WatchlistPerfTable.tsx` 重寫**：表格 → `grid gap-3 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4`（使用者要「網格 2-4 張」）。每張 `<Card>`：左側 `<Sparkline>`（160×72 inline SVG，`viewBox` + `preserveAspectRatio="none"`；虛線畫 dev=0 中軌基準；`<path>` 線 + `<linearGradient>` 線下漸層淡出；線色 `rising = changePercent >= 0` → `#fb7185`(rose-400) / 否 `#34d399`(emerald-400)，照台股慣例；有效點 < 2 → 「資料不足」佔位）。右側：`代號`(2xl) + `漲跌%` 台股色 + `突破` pill（`aboveBollingerUpper`）／第二行 `名稱 收盤 refDate`。底排 `border-t` 分隔：K棒·力道·位階三個 `<ScoreItem>`（`>=70` emerald-400 / `40~70` slate-200 / `<40` slate-500，null → 「—」+ degraded 加「不足」tag）+ 籌碼「合計 / 投信」（張，正紅負綠）。
- **驗證**：`tsc --noEmit` 乾淨、`pnpm build` 通過（先踩到 `"use server"` 匯出 const 的 build error，抽 `dashboard-spark.ts` 後解掉）、`pnpm dev` `/` 200；HTML 檢查 = 2 檔觀察股各一張卡、各有 `<svg>` + `<linearGradient>` + 線 path + area path、`突破` pill / K棒力道位階分數 / 籌碼皆正確。實測 4908 的 path：前 ~40 天 y 在中線下方窄幅晃動（close 貼近 bollingerMid），最後 ~15 天從 y≈65 衝到 y≈0（頂到 +25% clip）——正是「壓縮打底 → 帶量噴出」的形狀。
- **留給後續**：走勢圖加互動（hover 顯示當日 dev / 日期）、`SPARK_CLIP` 是否要可調（目前寫死 25%）、卡片點擊展開完整評分明細。

**Dashboard 卡片微調（2026-08-31，同批）**：使用者要 (1) grid 改一欄 2–3 張 (2) 文字整體放大、要有擴充性 / 一致性 (3) 走勢圖頂端留空間別貼框。

- **grid**：`sm:grid-cols-2 xl:grid-cols-3`（拿掉 `2xl:grid-cols-4`），卡片變寬、走勢圖 SVG 從 160×72 放大到 200×88（`h-22 w-50`）。
- **文字放大用「共用 label 元件」而非逐處改 class**：新增 `components/ui/Card.tsx` 的 `<FieldLabel>`（`text-sm text-slate-400` + 可選 `className` 覆蓋色），當**全站次級說明文字的單一出處**。`Stat` label 改用它（順帶 `Stat` value `text-2xl`→`text-3xl`、`Card` title `text-sm`→`text-base`）。`app/page.tsx` 資料狀態卡的「今日行情燈號」/「當日三表覆蓋率」label、`WatchlistPerfTable` 的 K棒/力道/位階/籌碼 label 全改用 `<FieldLabel>`。`WatchlistPerfTable` 內所有硬寫的 `text-[10px]` / `text-[9px]` / `text-[11px]` 迷你字清光，提到 Tailwind 標準級距（分數 `text-lg`、代號 `text-3xl`、名稱/日期 `text-sm`/`text-xs`）。以後要調這類字級只動 `<FieldLabel>` 一處。**沒改 Tailwind `@theme` token**（那會連 screening / watchlist 一起放大，非本意）。
- **走勢圖留邊**：`Sparkline` 加 `PAD = 8`，`dev = ±SPARK_CLIP` 從對應 `y = 0 / H` 改成 `y = PAD / H-PAD`，中軌基準線仍在 `H/2`。夾到邊界的點不再貼框（實測線頂端 y 從 0 變 ~8）。
- **驗證**：`tsc` 乾淨、`pnpm build` 通過、`pnpm dev` `/` 200；抓 `app_globals.css` 確認 `h-22`(`calc(--spacing*22)`=88px) / `w-50`(200px) / `text-3xl` / `text-lg` / `xl:grid-cols-3` 都有生成（Tailwind v4 對未知 utility 會靜默略過，故實查）；HTML 確認走勢圖 path 頂端 y≈8.1、`xl:grid-cols-4` 不再出現。

**大盤濾網（市場狀態燈號）Step 1 已完成（2026-08-31）**：ROADMAP 4.5.1，依 `docs/PLAN.md` 分兩階段的第一階段——只做「市場寬度 breadth」單維度（零新資料，`DailyQuote` + `TechnicalIndicator` 現成），先讓三段燈號上線。（Step 2 同日接著做完，見下一段。）

- **`scripts/lib/market-regime.ts`（新，純函式庫允許查 DB，比照 `breakout-shared.ts`）**：`RegimeLabel`（`bullish`/`neutral`/`bearish`）、`DimensionScore`（`score: -1|0|1` + `degraded` + `detail: Record<string, number|null>`）、`MarketRegimeResult`（`date`/`label`/`totalScore`/`dimensions.{indexPosition,ma60Slope,breadth}`/`stage`）、`RegimeConfig` + `DEFAULT_REGIME_CONFIG`（門檻集中，首版未校準：`breadthBullPct: 55` / `breadthBearPct: 45` / `indexPosBufferDays: 3` / `ma60SlopeLookbackDays: 5` / `ma60Slope*Threshold: 0.005` / `bullish/bearishTotalScore: ±2`）。`calculateMarketRegime(date, { prisma, config? })`。**stage 自動判定**：`hasTaiexIndicatorForDate()` → 有 TAIEX 該日 `ma60` = `step2-full`、沒有 = `step1-breadth-only`（優雅降級，TAIEX 沒補時 pipeline 不炸）。**breadth 維度**：該日全市場 `securityType="stock"` 有 `DailyQuote` 的股票中 `close > 當日 TechnicalIndicator.ma60` 佔比；分母 = `ma60` 非 null 檔數；`pct > 55` → +1 / `< 45` → -1 / 之間 0；分母 < 500 → `score 0 + degraded`（資料不齊不表態）。**Step 1 三段判定**：直接看 `breadth.score`（只有一維、範圍 -1~+1），不套 `bullishTotalScore`。查 DB helper 同檔：`fetchBreadthInputs` / `fetchTaiexCloseVsMa60` / `fetchTaiexMa60Series` / `hasTaiexIndicatorForDate`（後三個給 Step 2 用，已寫好）。
- **`scripts/pipeline/calculate-market-regime.ts`（新）**：薄殼 `calculateOneDayRegime(date, prisma)` → `calculateMarketRegime` → 原子寫 `data/market-regime/{date}.json`（先寫 `.tmp` 再 `renameSync`，比照 intraday progress.json）+ `generatedAt`。CLI（`isMain` guard）：無參數跑 DB 最新交易日（`securityType="stock"` 的 `max(date)`）；`--date=YYYY-MM-DD` 補算。**不落地 DB**（PLAN 明確：整個市場每天一標籤、資訊量小、盤中多次取允許一天多筆覆寫、不做回測 → 建表不划算）。
- **`daily-pipeline.ts` 第 5 步（原第 5 步產業熱度順延為第 6 步）**：算完技術指標後 `calculateOneDayRegime(latestQuote.date, prisma)`。**非關鍵路徑**：`try/catch` 只印 `console.warn` + `warningCount++`，不 `throw`、不讓 pipeline 非 0 結束。收尾 log 加「大盤濾網: {label}」。
- **`lib/actions/market-regime.ts`（新 Server Action）**：`getMarketRegime() → MarketRegimeView`。純讀 `data/market-regime/` 目錄取檔名最大（日期最新）的 `{date}.json`（排除 `.tmp`），parse 回可序列化物件（`MarketRegimeResult` 本來就無 `Date`/`Decimal`/`BigInt`）。組 `advice`（三段部位建議文字：bullish「正常執行」/ neutral「降到 1%」/ bearish「壓到 0.5–1%」/ 不可用「尚無資料」），degraded 維度 > 0 時附「（部分維度資料不足，判斷僅供參考）」。**只 `import type`**（`DimensionScore` / `RegimeLabel`），不 import `market-regime.ts` 本體（避免把非型別東西拉進 bundler，比照 `intraday.ts`）。檔頭 `"use server"`，**不碰 DB**（不 import `lib/prisma.ts`）。
- **`components/dashboard/RegimeBanner.tsx`（新，Server Component，純顯示）**：`variant="banner"`（首頁，完整橫幅 + `<details>` 三維度明細，`bearish` 時整條 Card `border-rose-500/40`）／`variant="strip"`（`/screening` 頁頂，精簡一行：圓點 + `大盤：偏空` + advice）。**配色 = 通用號誌色**（綠 `bg-emerald-500`=偏多 / 琥珀 `bg-amber-500`=中性 / 紅 `bg-rose-500`=偏空 / 灰 `bg-slate-600`=無資料），跟個股漲跌色（台股漲紅跌綠）不同語意、不同區塊，元件註解寫明此選擇。明細每維 `名稱：+1/0/-1`（degraded 標灰 + 「資料不足」），下面列 `detail` 原始數值；`dim === null`（Step 1 的 indexPosition/ma60Slope）顯「待 TAIEX 資料」。
- **`app/page.tsx`**：`<h1>Dashboard</h1>` 之下、「資料狀態」Card 之上插 `<RegimeBanner regime={regime} />`；`getDbHealth()` / `getMarketRegime()` 併成 `Promise.all`。**`app/screening/page.tsx`**：改 `async`，頁面標題下、`ScreeningPanel` 之上插 `<RegimeBanner regime={regime} variant="strip" />`；不擋按鈕、不改 `runScreening`。
- **驗證（2026-08-31，DB 最新 08-31）**：`pnpm tsx scripts/pipeline/calculate-market-regime.ts` → `data/market-regime/2026-08-31.json` 生成，`label: bearish`、`stage: step1-breadth-only`、`breadth.detail` = 站上 MA60 730/1940（**37.6%**）→ score -1（當前盤勢確實偏弱，判讀合理）。門檻生效測試：`config: { breadthBearPct: 10 }` → `neutral`；`{ breadthBullPct: 30 }` → `bullish`。`pnpm exec tsc --noEmit` 乾淨、`pnpm build` 通過（`/` `/screening` 仍 `ƒ Dynamic`）。`curl localhost:3000/` 與 `/screening` HTML 含「大盤：偏空」「基準 2026-08-31，score -1」「stage step1-breadth-only」+ advice 文字。
- **`.gitignore`**：加 `/data/market-regime/`（執行產物）。
- **留給後續**：三段門檻校準（肉眼對照盤感）、盤中版 regime（`data/market-regime/intraday/{timestamp}.json`，等 4.5.3 即時報價基礎設施）。

**大盤濾網 Step 2 已完成（2026-08-31，同日接著）**：TAIEX 日線落地 + 補「指數位置 indexPosition」「MA60 斜率 ma60Slope」兩維度，變三票合成。

- **FinMind 實測**：`TaiwanStockPrice?data_id=TAIEX` 直接有回日線 OHLC（`close` 万点級 + `spread` = 漲跌、`Trading_Volume` = 全市場成交量），**不需要 fallback 到 `TaiwanStockTotalReturnIndex` 等其他 dataset**。2026-08-01~08-31 回 21 筆，連兩次呼叫結果一致。
- **schema**：`SecurityType` enum 加 `index` 值。`pnpm prisma migrate dev --name add_index_security_type` → migration 只有一行 `ALTER TYPE "SecurityType" ADD VALUE 'index';`，**未遇到** PLAN 提的 transaction-block 錯誤（Postgres 版本相容）。`pnpm prisma generate` 完成。
- **`scripts/backfill/backfill-index-quotes.ts`（新）**：照抄 `backfill-benchmark-quotes.ts` 結構。開頭 upsert `Stock{ code:"TAIEX", name:"發行量加權股價指數", market:"TWSE", securityType:"index", sectorId:null }`（不在 seed 清單裡）。FinMind `TaiwanStockPrice?data_id=TAIEX` → upsert `DailyQuote`（`source:"TWSE"`，`volume` 存全市場成交量，`change = spread`）。**只寫 `DailyQuote`**。首跑：**1618 筆**，2020-01-02 ~ 2026-08-31。`BACKFILL_START_DATE` 可覆蓋。
- **`calculate-technical-indicators.ts` 改兩處**：(1) `where` 從 `{ securityType: "stock", ...codes }` 改 `{ OR: [{ securityType: "stock" }, { code: "TAIEX" }], ...codes }`，log 文案「共 N 支（含 TAIEX）待計算」。(2) **argv 過濾器 bug**：`isMain` 分支的 `codeArgs` filter 是 `/^\d{4}[A-Z]?$/`，`TAIEX` 不符 → 傳 `TAIEX` 當參數會被忽略、跑成全市場。改成 `... || a === "TAIEX"`。修正後 `pnpm tsx scripts/pipeline/calculate-technical-indicators.ts TAIEX` → 1 支、1618 筆 `TechnicalIndicator`（~30s）。實測 TAIEX 最新 `ma60 ≈ 44898`、`bollingerBandwidth ≈ 0.0645`、`rsi14 ≈ 60`，數量級對得上大盤點數。
- **`market-regime.ts`**：三維度 + `fetchTaiex*` helper + `stage` 自動判定**在 Step 1 那批就一次寫好了**（Step 1 只是走 `hasTaiexIndicatorForDate() === false` 的降級分支），Step 2 這批**零程式改動**——TAIEX 指標一補上，`calculateMarketRegime` 自動走 `step2-full`。`RegimeBanner` 的維度 render 也是泛型的（`DimensionScore` 通吃），不用改。
- **`daily-pipeline.ts`**：第 5 步（大盤濾網）依賴第 4 步（技術指標）已更新 TAIEX 的 `ma60`——第 4 步 `where` 改 `OR` 後全市場跑會一起更新 TAIEX，順序已對（第 4 步 → 第 5 步），不用動。
- **驗證（三維度判別力）**：
  - `2026-08-31`（今日）：指數位置 +1（TAIEX 連 3 日站上 MA60）／MA60 斜率 0（`+0.06%` 走平）／市場寬度 -1（37.6%）→ totalScore 0 → **neutral**（指數靠權值股撐、寬度弱的背離，判讀合理）。`stage` = `step2-full`。
  - `--date=2022-10-25`（深熊底）：三維全 -1，寬度 13.9%、斜率 `-1.18%` → totalScore **-3** → **bearish** ✓
  - `--date=2024-03-08`（AI 多頭噴出）：三維全 +1，寬度 55.4%、斜率 `+1%` → totalScore **+3** → **bullish** ✓
- **`pnpm exec tsc --noEmit` 乾淨、`pnpm build` 通過**。`curl localhost:3000/` 首頁 banner 顯示「大盤：中性」+ `stage step2-full` + 三維度明細（含 `latestClose` / `slopePct` / `pct` 等 detail 原始值）。
- **留給後續**：三段門檻校準（`DEFAULT_REGIME_CONFIG` 首版未校準）、盤中版 regime。

**融資融券資料層已完成（2026-08-31）**：ROADMAP 4.5.2，依 `docs/PLAN.md`。只做「資料層 + 進 pipeline + 首頁覆蓋率一個數字」，評分整合（融資融券當修正因子）留 ROADMAP 4.5.3。

- **schema `MarginTrading`（新，放 `TechnicalIndicator` 之後，比照 `InstitutionalTrading` 形狀）**：`stockCode + date` 唯一 + `@@index([date])`。`marginBalance` / `marginBalancePrev` / `shortBalance` / `shortBalancePrev` non-null `BigInt`（沒信用交易的股票是 `0` 不是缺值）；`marginQuota`（融資限額）/ `offsetting`（資券互抵）nullable。`*Prev` 直接取 API 回應的「前日餘額」欄，不自己 join。**不存 change（今日−前日）**（同 `heatScore` 理由）、**不存「融資金額（仟元）」**（個股明細沒有，要金額用 `marginBalance × close` 現算）。`Stock` 加 `marginTradings MarginTrading[]`。`pnpm prisma migrate dev --name add_margin_trading` → migration 只有 `CREATE TABLE` + 2 index + FK，不動現有 row。
- **端點實測（2026-08-31）**：
  - **TWSE `MI_MARGN`**（`www.twse.com.tw/rwd/zh/marginTrading/MI_MARGN?response=json&date=YYYYMMDD&selectType=ALL`）：`date` 傳西元 `YYYYMMDD`，**支援任意歷史日期**。`stat === "OK"` + 有 `tables` 才是交易日；非交易日 `stat` 為「很抱歉，沒有符合條件的資料!」。**個股明細在 `tables[1]`**（`tables[0]` 是全市場彙總）；`fields` 16 欄，用到 index `[0]代號 [1]名稱 [5]融資前日餘額 [6]融資今日餘額 [7]融資次一營業日限額 [11]融券前日餘額 [12]融券今日餘額 [14]資券互抵`。數字含千分位逗號、單位張。`data` 混 ETF（實測有 `00400A` 主動式 ETF）/ 特別股 / 一般股票。
  - **TPEx `margin/balance`**（`www.tpex.org.tw/www/zh-tw/margin/balance?date=YYYY/MM/DD&id=&response=json`）：`date` 收西元斜線格式，**支援任意歷史日期**（與 TPEx 行情端點不同，比照估值端點 `peQryDate`）。`tables[0].date` 民國年斜線格式（`115/08/28`）`+1911` 核對；非交易日 `totalCount` 為 0。`fields` 20 欄，用到 index `[0]代號 [1]名稱 [2]前資餘額(張) [6]資餘額 [9]資限額 [10]前券餘額(張) [14]券餘額 [18]資券相抵(張)`。數字含千分位逗號、單位張。`data` 混 ETF（`00679B` 債券 ETF 等）/ 一般股票。
- **`scripts/pipeline/fill-margin-trading.ts`（新，比照 `fill-institutional-trading.ts` 骨架）**：`fetchTwseMargin` / `fetchTpexMargin` → `writeMarginRows` upsert。`parseLots(raw)` = 去逗號 → `parseFloat` → `Math.round(n) * 1000` → `BigInt`（空 / `-` → `0n`）；`parseLotsNullable` 空 → `null`（給 `marginQuota` / `offsetting`）。過濾用 `toSecurityType`，**保留 `stock` + `preferred`**（特別股也有信用交易資格）；`existingCodes` 從 `Stock` 撈 `securityType in [stock, preferred]`，只 upsert 已存在代號。兩端點皆支援歷史日期，回傳日期不符即 throw（無 `isStaleDate` 邏輯）。CLI：無參數抓今天 / `--date=YYYY-MM-DD`（或 `YYYYMMDD`）/ `--backfill=N`（從今天往回逐個日曆日、`isNonTradingDay` 不計數、每日之間 `sleep(1500)`，實得約 N 個交易日；與 `--date` 互斥）。`isMain` guard 尾巴 `.catch(exit 1).finally($disconnect)`。
- **首次回補 `--backfill=10`**：實得 10 個交易日 **2026-08-17 ~ 2026-08-28**，每日 1860 筆（TWSE 1059 + TPEx 801），共 **18600 筆**。TWSE 每日跳過非追蹤類型 ~235、TPEx ~117；跳過 Stock 表沒有的代號 **0**。
  - **×1000 換算驗證**：2330 每日 `marginBalance % 1000n === 0n` ✓。數量級：2330 融資餘額 2762~2842 萬股（= 2.76~2.84 萬張），融券餘額 2.8~4.5 萬股，合理。
  - **`*Prev` 對帳**：2330 每日 `marginBalancePrev` 精確等於前一交易日的 `marginBalance`（08-18 prev 27727000 = 08-17 bal 27727000，整串連續）✓。
  - **覆蓋率抽算**：2026-08-28 `MarginTrading`(stock) 1860 ÷ `DailyQuote`(stock) 1954 = **95.2%**（落在預期 90~95%，非全部股票有信用交易資格屬正常）。
- **進 `daily-pipeline.ts` 第 3.5 步**（估值之後、技術指標之前；「抓外部資料」四步連在一起）：`try/catch` 包成**非關鍵路徑**——TWSE+TPEx 皆 `isNonTradingDay` 印警告 + `warningCount++`，抓取 throw 也只 `console.warn` + `warningCount++`，不 `throw`、不讓 pipeline 非 0 結束。收尾 log 加「今日融資融券寫入筆數」。後續步驟編號沿用 3.5 標籤，未重編 4/5/6。
- **前端覆蓋率**：`lib/actions/health.ts` 的 `DbHealth.coverage` 加 `margin: { count, pct }`，`getDbHealth` 內 `refDate` 那段 `Promise.all` 加 `prisma.marginTrading.count({ where: { date: refDate } })`。分母沿用 `stockCount`（`securityType="stock"`），`margin` 分子另含約十幾檔特別股 → pct 微幅偏高 <1%，可接受（health.ts 註解標明）。`app/page.tsx` `covRows` 加「融資融券」列，`<FieldLabel>` 文案「當日三表覆蓋率」→「當日四表覆蓋率」，其餘（`coverageClass` 上色、排版）不動。
- **驗證**：`pnpm exec tsc --noEmit` 乾淨、`pnpm build` 通過（`/` 仍 `ƒ Dynamic`）。
- **明確不做（留後續）**：歷史回補到 2020（`scripts/backfill/backfill-margin-trading.ts`，FinMind 逐支）、`MarginTrading` 顯示在 `/watchlist` 明細 / Dashboard 卡片。（融資融券進選股評分 = `margin-chasing` 警示，已於 2026-09-01 完成，見本檔末段。）

**技術指標只算當天 + 首頁「立即更新資料」按鈕（2026-08-31，同批）**：使用者要一顆按鈕從頁面跑整個 daily pipeline，順便發現 pipeline 每天全歷史重算技術指標（~10 分鐘）不合理。

- **`calculateTechnicalIndicators(codes?, options?)` 加 `options.mode: "full" | "latest"`（預設 `"full"`）**：
  - `"latest"`：每支只 `findMany({ orderBy: date desc, take: 250 }).reverse()` 撈最近 250 筆當輸入（`LATEST_LOOKBACK`；足夠算 MA60 / 布林 / RSI14 / ATR20 / MACD EMA 暖身），算完 `rows.slice(-1)` 只 upsert 最新一筆日期。`quotes` 空（新股當天上市還沒 `DailyQuote`）→ `slice(-1)` 也空、該支不寫，與現狀一致。
  - CLI 與不帶 `options` 一律 `"full"`（回補報價後手動重算、debug 要全歷史；不加 `--latest` flag）。
  - 開頭 log 補「模式：只算最新一天 / 全歷史重算」。
  - **數值一致性驗證**：`full` 重算 2330 + TAIEX → 記錄最新一天各欄位 → 故意把該筆 `ma60`/`rsi14`/`bollingerBandwidth` 汙染成 `-999` → 跑 `latest` → 比對：兩者最新一天所有欄位**誤差 0**（`ma60` 2384.00 / 44898.20、`rsi14` 51.67 / 60.01、`bw` 0.04399 / 0.06451 完全一致），汙染值被覆寫，且前一天那筆 `calculatedAt` 沒變（確認只碰最新一天）。
- **`daily-pipeline.ts` 第 4 步改 `calculateTechnicalIndicators(undefined, { mode: "latest" })`**：pipeline 內唯一改動，對外只有「變快」（`indicatorsProcessed` 仍 ~2149、寫入筆數從 ~340 萬變 ~2050、退出碼 / 步驟數 / summary log 格式不變）。第 5 步大盤濾網依賴 TAIEX 的 `ma60`——`latest` 模式 `where` 仍含 TAIEX、一樣算最新一天，`hasTaiexIndicatorForDate()` 對今天為 true → regime 走 `step2-full`，不受影響。**整支 pipeline 實測從 ~10 分鐘降到 21 秒**。
  - **代價**（已跟使用者確認接受）：pipeline 漏跑那天的技術指標不會自動補回，要手動 `calculate-technical-indicators.ts <code>` 全歷史重算。
- **`lib/actions/pipeline.ts`（新，`"use server"`）**：`runDailyPipeline()` / `getDailyPipelineStatus() → DailyPipelineStatus`（`status` / `startedAt` / `finishedAt` / `exitCode` / `logTail` / `logFile` / `logPath`）。
  - **為何不照 intraday 讓子進程自寫終態**：`daily-pipeline.ts` 不能改（要保持乾淨給 launchd）。所以 spawn 它當獨立腳本（利用它無 `isMain` guard、`import` 即跑 `main()` 的特性），`stdio: ["ignore", out, out]` 導到 `data/daily-pipeline-runs/{stamp}.log`，**parent（Next server action 進程）`child.on("exit")` 覆寫 `progress.json` 終態**。進度只有粗粒度（`status` + 存活秒數 + log 檔尾 12 行），沒有 7 步進度條。
  - **hot-reload 兜底**：dev 模式 Next 進程 hot-reload 會丟 exit listener → `progress.json` 卡 `running`。`getDailyPipelineStatus()` 判 `running` 且 `startedAt` 超過 `STALE_MS`(20 分) → 回報 `error`（帶 log tail 幫 debug）、允許重按。生產 `pnpm start` 不 hot-reload，正常情況 listener 都在。
  - **單一任務鎖**：`progress.json` 存在且 `running` 且未逾時 → `runDailyPipeline()` 回 `{ started: false, reason: "pipeline 執行中" }`。
  - **Turbopack build warning 修正**：`tailLines` 原本吃「相對或絕對路徑」再 `join(REPO_ROOT, ...)`，Turbopack 靜態分析判定「dynamic filesystem access」會追蹤整個專案。改成只吃 log 基本檔名、`join(RUN_DIR, logFile)`（靜態 scope 到子資料夾），warning 消失。
- **`components/dashboard/PipelineRunner.tsx`（新，client component）**：骨架照 `ScreeningPanel.tsx` 的 intraday 輪詢段（`pollRef` + `setInterval` + 掛載時若 `running` 接管 + 卸載 `clearInterval`）。「立即更新資料」按鈕 → `runDailyPipeline()` → `setInterval(3s)` 輪詢 → `running` 顯示「已 N 秒（可離開此頁）」+ `<pre>` log tail；`done` → `window.location.reload()`（`app/page.tsx` 是 `force-dynamic`，reload 重抓 `getDbHealth()` 新覆蓋率）；`error` 顯示 exit code + log 路徑。切走 `useEffect` cleanup 停輪詢、子進程不受影響；切回重掛載時若 `running` 接管。
- **`app/page.tsx`**：「資料狀態」Card 的 grid 之後接 `<PipelineRunner />`（`border-t` 分隔）。
- **`.gitignore`**：加 `/data/daily-pipeline-runs/`。
- **端到端驗證**：直接呼叫 `runDailyPipeline()` → `{ started: true }`；立刻再呼叫 → `{ started: false, reason: "pipeline 執行中" }`（鎖生效）；輪詢 `getDailyPipelineStatus()` 看到 `status: running` + `logTail` 逐輪更新（報價 → 籌碼 → 估值 → 技術指標「已處理 N/2149」→ summary）+ 存活秒數遞增；~40 秒後 `status: done` / `exitCode: 0` / 終態 logTail = pipeline summary。log 檔落 `data/daily-pipeline-runs/2026-08-31T15-00-22-614Z.log`。`curl localhost:3000/` HTML 含「立即更新資料」按鈕。
- **`pnpm exec tsc --noEmit` 乾淨、`pnpm build` 通過（無 warning）**。

**選股引擎統一 — 三路線收斂（2026-09-01）**：ROADMAP 4.5.3，依 `docs/PLAN.md`。把 `/screening` 的三分頁（第一根突破盤後 / 冷水區醞釀 / 盤中即時掃描）收斂成單一評分管線 + 單一前端頁 + 階段標籤。

- **`scripts/lib/mis-quotes.ts`（新，純 Node 函式庫）**：從 `check-intraday-breakout.ts` 抽 MIS 即時報價抓取（`fetchMisBatch` / `fetchAllMisQuotes` / `parseMisDate` / `computeElapsedRatio` / `MisQuote` 型別 / `BATCH_SIZE` 等常數）。**行為變更一處**：舊 `fetchMisBatch` 對缺成交價（`z` 為 `-` / 缺）的 row 直接跳過；新版保留該 row、`price: null`，由呼叫端決定代入策略（`run-signal-scan.ts` realtime 用 `h` 代入；`check-intraday-breakout.ts` 沿用「缺價跳過」在自己的觸發迴圈補 `if (quote.price === null) continue`，最終結果不變）。
- **`scripts/lib/signal-factors.ts`（新，統一因子庫）**：PLAN §2.1 選 A —— 只 re-export `breakout-shared.ts` / `accumulation-shared.ts` 的評分函式 + 新增，**不搬評分邏輯本體**（舊兩檔零改動，舊三支不受影響）。新增：
  - `computeInstitutionalFlow()`（三大法人流向，全新因子）：近 5 日投信 + 外資淨買超各自 ÷ `volumeMa20` → clip `[0, clipDivisor=0.5]` 線性映射 0~100 → 投信 0.6 / 外資 0.4 加權。突破當日兩者相加 < 0 → `min(score, sellCapScore=40)`；當日法人任一為 null（盤中）→ 不封頂但 `degraded: true`。`volumeMa20` 缺 / 近窗資料不足一半 → 中性 50 + degraded。
  - `computeBreakoutMarginMonotone()`（§2.5 新，舊 `computeBreakoutMargin` 不動）：0~3% 乖離線性 40→100（不變），**> 3% 維持 100 封頂、不再倒扣**（舊版每 1% 扣 5 分、下限 60）；負乖離 → 40 下限。只讀 `curve.kneePct`。
  - `consecutiveAboveBand()`（§3.3 階段判定 helper）：回 `{ ok, latestAboveBand, consecutiveDays }`。`computeFirstBar` 內部原本就數這個，但 PLAN 不動 `computeFirstBar` 本體（它有使用者要保留的 ≤2→50 / >2→20 緩衝語意），故獨立成 helper。
  - `SignalScanConfig` / `DEFAULT_SIGNAL_CONFIG` / `resolveSignalConfig`：從 `DEFAULT_BREAKOUT_CONFIG` + `DEFAULT_ACCUMULATION_CONFIG` 拼出，**三處刻意偏離舊預設**：(1) `baseCurve` 深度/時長 = **0.4 / 0.6**（舊 breakout 0.6 / 0.4，§2.6 對調）；(2) `breakout.curves.breakoutMargin` 新公式單調遞增、`penaltyPerPct` / `floor` 不生效；(3) `breakout.weights` 7 項各 ×0.90、勻出 `institutionalFlow: 0.10`（總和 1）。
  - 單元測試 `scripts/lib/signal-factors.test.ts`（`node:test` + `tsx`，21 案全綠）：`computeBreakoutMarginMonotone`（0%→40 / 1.5%→70 / 3%→100 / 8%→100 不倒扣 / 負→40 / 掃 0~15% 單調）、`computeInstitutionalFlow`（volumeMa20 缺 / 資料不足 / 滿分 / 淨賣超 clip 0 / 當日翻空封頂 40 / 當日 null 不封頂 degraded）、`consecutiveAboveBand`、`resolveSignalConfig`（權重總和 1 / 部分 override）。
- **`scripts/screening/run-signal-scan.ts`（新，單一評分管線）**：`runSignalScan(date, { prisma?, config?, source?, now? })`。
  - **資料源自動切換（§3.2）**：`options.source` 有值用它；否則查「Asia/Taipei 今天的 `DailyQuote`（`securityType="stock"`）存在嗎」→ 有 `eod`、沒有 `realtime`。`--date` 明確指定非今天 → 一律 `eod` 補算。
  - **eod 路徑**：`fetchBreakoutRawInputs` 撈全市場 → gate（`minMarketCap` 30 億 + 當日 `volume` ≥ 1M 股 + `volumeMa20` ≥ 500k 股）→ `consecutiveAboveBand`（當日 close vs 當日上軌，往回接 T-1 序列）打階段標籤 → pre-breakout 另撈 `fetchAccumulationRawInputs`、breakout 另撈近 5 日 + 當日法人（`fetchInstitutionalFlowInputs`）。
  - **realtime 路徑（§4 MIS z bug）**：`fetchAllMisQuotes` 逐批寫 `progress.json` → 每檔決定 close：有 `z` → `priceSource: "realtime"`；缺 `z` 用 `h` 代入 → `"estimated"`（`candleShape` 強制 degraded 給 50，其餘依賴 close 的因子照常算——`h` 是保守上界）；`z` / `h` 都缺 → 跳過（`skippedNoPriceCount`）。籌碼因子讀到 T-1、當日法人子項 → null → `institutionalFlow` degraded。
  - **階段合併（§3.4）**：pre-breakout = 乘法（`combineTrustScore` → `combineChipScore` → `computeReadinessCoefficient` → `combineFinalScore`，就緒係數下限 0.5，`squeezeScore` 用 §2.6 新權重）；breakout-day / extended = 8 分項加權和（`breakoutMargin` 用 monotone 版、`base` 用新權重）。**排名為同階段內名次**（三階段各 1..N）。
  - 「觸發量比 ≥ 2」下放成 breakout 階段「進榜門檻」（沒過 → 不進 results、`stats.breakoutBelowVolume++`）。
  - 落地 `data/signal-scan-results/{date}.json`（eod）/ `{timestamp}.json`（realtime）+ `params`（resolved config）。CLI：無參數自動判斷、`--date=YYYY-MM-DD`（強制 eod）、`--source=realtime|eod`。**獨立手動執行，不進 daily-pipeline**。
- **`scripts/screening/_run-signal-scan.ts`（新，內部 runner）**：被 `startSignalScan()` spawn，只跑 realtime，最外層覆寫 `progress.json` 終態（逐批進度由 `runSignalScan` 內部負責）。比照 `_run-intraday-scan.ts`。
- **`lib/actions/signal-scan.ts`（新，取代 `screening.ts` + `intraday.ts`，兩檔已刪）**：`getScanMode()`（前端一進頁問，決定按鈕文案）、`runSignalScanEod()`（同步在 Next 進程內跑 DB 最新交易日、傳 prisma 單例）、`startSignalScan()` / `getSignalScanProgress()` / `getSignalScanResult()`（realtime 背景任務，比照舊 intraday）。
- **`components/screening/ScreeningPanel.tsx`（重寫）**：移除三策略 tab。一顆「跑掃描」按鈕（`getScanMode()` 決定 = `runSignalScanEod()` 或 `startSignalScan()` + 輪詢）+ 階段 filter tab（全部 / 醞釀中 / 今日突破 / 已延伸，純前端 `results.filter`）+ 一張可排序表格（`stage` 是其中一欄）+ 點列展開評分明細（by-stage：pre-breakout 醞釀分項 + 原始指標 / breakout 8 分項）+ 勾選加入。`priceSource === "estimated"` 列尾「估」badge（`text-amber-400`）+ 展開多一行提示。「全部」filter 預設依 `totalScore` 降冪 + UI 標註「不同階段分數不可直接比較」。加入觀察清單按每列自己的 stage 映射 `source`（`pre-breakout` → `accumulation`；`breakout-day` / `extended` → `breakout`，`WatchlistItem.source` enum 不動）。
- **舊三支腳本（`calculate-breakout-strength.ts` / `calculate-accumulation-score.ts` / `check-intraday-breakout.ts`）**：這批不刪、留原地可跑。除 `check-intraday-breakout.ts` 改 import `mis-quotes.ts`（行為不變）外未動。退役待下一批 PLAN。`_run-intraday-scan.ts` 因 `intraday.ts` 已刪成孤兒（仍可 CLI 跑），一併留。
- **驗證（2026-08-31 DB 最新交易日，eod 模式）**：
  - `pnpm exec tsc --noEmit` 乾淨、`pnpm build` 通過（`/screening` 仍 `ƒ Dynamic`）、`curl localhost:3000/screening` render 正常（初始「判斷掃描模式中…」）。
  - **breakout 候選集合**：signal-scan breakout-day + extended 共 33 檔，是 `calculate-breakout-strength.ts` 37 檔的**嚴格子集**。少的 4 檔（6491 晶碩 / 6425 易發 / 6499 益安 / 3548 兆利）皆因 `volumeMa20 < 500,000`（499,634 / 334,974 / 421,694 / 327,820）被統一 gate 的 `minAvgVolumeShares`（沿用 accumulation、剔死股）擋下——**設計內差異**（PLAN §2.7 明列此為統一 gate 的一部分）。
  - **pre-breakout 候選集合**：signal-scan 453 檔，是 `calculate-accumulation-score.ts` 707 檔的**嚴格子集**。少的 254 檔皆因不過統一 gate 的 `minMarketCap`(30 億) 或 `minVolumeShares`(當日 1000 張) —— 舊 accumulation 候選池**沒有**市值下限與當日量下限（抽查 5 檔全部卡當日量 < 1M 股）。**設計內差異**（PLAN §3.3「三階段共用同一道 gate」）：統一後 pre-breakout 明顯變嚴。
  - **breakoutMargin 單調性**：3406 玉晶光 舊 67.8 → 新 **100.0**（舊公式乖離 > 3% 被倒扣，新公式封頂不扣），總分 55.5 → 62.6 上升；6933 AMAX-KY 60.6 → 100.0；3374 精材 68.5 → 100.0。乖離 ≤ 3% 的股票 `breakoutMargin` 完全不變。
  - **squeeze 深度/時長權重對調**：共同 pre-breakout 股（1101 台泥 squeezeScore 71.7 → 59.5、1104 環泥 79.0 → 68.5）方向一致（深度分 > 時長分的股票新權重下 squeeze 降低）。`chipScore` 也微幅移位（rankScore 母體從 707 縮到 453，百分位重算）。
  - **realtime 冒煙測試（盤後 23:57 執行、資料為前一交易日）**：管線跑通，`stats.estimatedCount: 207`（`h` 代入生效、這些檔沒被丟掉）/ `skippedNoPriceCount: 31`（`z`+`h` 皆缺）/ `failedCount: 0`。MIS 日期 = DB 最新日 → 警語正確觸發。真正盤中驗證待開盤。
- **`.gitignore`**：加 `/data/signal-scan-results/`。
- **明確不做（留後續）**：舊三支退役（下一批 PLAN，先肉眼比對排名穩定後）、回測 / 參數校準（階段權重、法人因子曲線全首版拍腦袋）、`computeInstitutionalFlow` 歷史百分位正規化、歷史查詢 UI（`runSignalScan` 的 `date` 參數已留、前端不傳）、`daily-pipeline.ts` 加選股步驟（選股維持手動）。（融資融券進評分 = `margin-chasing` 警示，2026-09-01 已完成，見本檔末段。）
- **`computeBase` 已知限制（PLAN §2.6，未修正）**：「壓縮深度」子項只看突破前一天**單一天**的帶寬對 240 日分布取百分位，**無連續性概念**——「壓一下 → 打開 → 突破前一天剛好又壓下去」會被誤判成深度壓縮。改進提案（本批不做）：深度輸入從「前一天單日帶寬」改成「前一天往回 3~5 天帶寬平均」。「壓縮時長」子項已有連續性、不受影響。本批只做權重對調（深度 0.4 / 時長 0.6）。

---

**融資融券修正因子 — `margin-chasing` 警示（2026-09-01）**：ROADMAP 4.5.3 deferred 項，依 `docs/PLAN.md`。把融資融券接進選股，但**不新增分項、不動任何分數**——只在 `breakout-day` / `extended` 階段掛一個 `margin-chasing` 警示標記。

- **動機**：突破當日三大法人（投信＋外資）淨賣超、同時這檔融資餘額近期暴增 = 「法人在派發、散戶在追價」的典型出貨形態。系統誠實呈現這個組合，要不要進一步排除由人肉眼判斷。
- **為何只標記不動分數（使用者定調）**：`computeInstitutionalFlow` 已有 `sellCapScore`(40) 封頂機制，上面再疊「法人賣 + 融資又暴增所以再降」= 特例規則互相牽扯，之後除錯 / 調權重難追蹤是哪層在起作用。標記取代動態調整，跟 `degraded`「標記但不隱藏、不過度處理」哲學一致。因此新增 `SignalResult.warnings: string[]`，**與 `degraded` 分開**（`degraded` = 資料不足所以不確定；`warnings` = 資料充足、系統明確發現的風險訊號 —— 混同一陣列前端沒法用顏色區分）。
- **`computeMarginSurgePercentile()`（新，`scripts/lib/signal-factors.ts`，純函式）**：算「這檔融資餘額近 5 日累積變化率」`(series[0] − series[5]) / series[5]`，對「這檔過去 40 天、每天各自往回 5 日的同種累積變化率」母體取百分位（0~100）。設計理由（同「籌碼因子偏誤修正」的「不跟別的股票比、跟自己比」）：避免大型股 vs 小型股融資規模差異汙染。用「近 5 日累積」不用單日：散戶追高是連續加碼，累積能串起「突破前幾天融資悄悄墊高 + 今天噴發」。
  - **「嚴格小於」避免平盤誤觸發**（PLAN §2.3 挖出的坑）：百分位用 `population.filter(v => v < today)` 而非 `<=`。平盤時 today changeRate = 0、母體也全 0，`0 < 0` 為 false → 百分位 0，不會因 threshold 80 + `> threshold` 而誤觸發。
  - 母體有效樣本 < `minHistoryDays`(20) → 回 null（比照 `computeBase` 保守門檻）。`past <= 0`（融資歸零 / 缺值）該筆跳過不炸。
- **`computeInstitutionalFlow()` 改動**：加 `marginSurgePercentile: number | null` 輸入、加 `marginChasing: boolean` 輸出。`marginChasing = marginSurgePercentile !== null && > surgePercentileThreshold(80) && 突破當日投信+外資淨賣超`。**score / degraded 完全不動**。`InstitutionalFlowConfig` 加 `marginChasing: { lookbackDays: 5, historyWindowDays: 40, minHistoryDays: 20, surgePercentileThreshold: 80 }` 子區塊，`resolveSignalConfig` 手寫展開。
- **`run-signal-scan.ts` 接線**：`fetchMarginSurgeInputs(prisma, toDate, codes, config)` 撈 breakout 階段候選股的 `MarginTrading.marginBalance` 序列（近 `lookbackDays + historyWindowDays + 2` 筆，`BATCH=250` 分批），算出每檔百分位。eod 路徑 `toDate = 掃描日`（含當日融資餘額）；realtime 路徑 `toDate = null` → 全 null（當日融資餘額晚上才公布，比照法人子項盤中降級）。`computeBreakoutFactors` 回傳新增 `warnings: string[]`，`margin-chasing` 時 push；所有 `SignalResult` 生成點補 `warnings`（pre-breakout 恆 `[]`）。
- **前端（`ScreeningPanel.tsx`）**：`row.warnings.includes("margin-chasing")` → 收盤欄尾一個**紅** badge「追」（`bg-rose-500/20 text-rose-400`，跟琥珀「估」、灰字 `degraded` 三者顏色分明）+ 展開明細最上方一個紅框提示。排序 / filter / 加入觀察清單邏輯不動。
- **驗證（2026-08-31 DB 最新交易日，eod）**：
  - `pnpm exec tsc --noEmit` 乾淨、`pnpm build` 通過、單測 31 案全綠（新增 10 案：`computeMarginSurgePercentile` 5 + `marginChasing` 輸出 5）。
  - **被標記的股票**：6209 今國光（當日外資 −29,941、投信 0；融資 5 日 +5.08%、百分位 87.2）、6269 台郡（外資 −282,080；融資 +20.47%、百分位 100）、6668 中揚光（外資 −36,221；融資 +5.81%、百分位 92.3）+ 6491... 實際落地 4 檔（含 5439）。逐檔人工核對：當日投信+外資淨買超確實 < 0、融資餘額近 5 日確實明顯高於 5 日前且增速排在該檔 40 天前段。
  - **分數不變證明**：`config.breakout.institutionalFlow.marginChasing.surgePercentileThreshold` 設 101（永不觸發）跑一次，`results` 每筆 `totalScore` / `scores.institutionalFlow` 與預設跑**完全一致**（0 mismatch），`margin-chasing` 清單從 4 檔變 0 檔。
  - **realtime 冒煙**：breakout 階段每筆 `warnings` 為 `[]`（`marginSurgePercentile` 全 null），管線不炸。
- **資料量**：融資融券歷史已回補到約 45 個交易日（`fill-margin-trading.ts --backfill=45`，2026-09-01）。實測多數個股有 45 筆 `MarginTrading` ≥ `lookbackDays + minHistoryDays`(25)，因子對 breakout 候選股大多能算出百分位。
- **待校準（PLAN §7）**：累積滿 3 個月資料後（約 2026-11），回頭看觸發頻率 + 命中準確度，再決定 `surgePercentileThreshold`(80) / `lookbackDays`(5) / `historyWindowDays`(40)。
- **明確不做**：動 `computeInstitutionalFlow` 分數 / 封頂 / 權重、pre-breakout 接融資融券（無「當日法人賣」概念）、融券 / 資券互抵進因子、跨股票比較、回測校準、`MarginTrading` 進 daily-pipeline 選股步驟。

---

**舊三支退役 + `signal-factors/` 目錄化（2026-09-01）**：ROADMAP 4.5.4，收尾批。統一引擎 `run-signal-scan.ts` 跑穩、`feat/screening-unified` 已 merge → 把舊路線正式收掉。純結構搬遷，零行為變更（`tsc` + `build` + 31 案單測全綠、`grep` 程式碼 0 命中）。分支 `feat/screening-retire-legacy`。

- **舊三支 + 孤兒 runner 直接刪（`git rm`，不進 `archive/`）**：`calculate-breakout-strength.ts` / `calculate-accumulation-score.ts` / `check-intraday-breakout.ts` / `_run-intraday-scan.ts`。判斷：本體只是「串 shared 函式 + 寫 JSON」的 CLI 殼，評分邏輯已整併進 `signal-factors/`，設計理由留在本檔，`git show <sha>:path` 撈得回舊實作。`archive/` 的用途是「還可能撿回參考的獨立實作」，這些殼不符 → 進 `archive/` 只會多一份要在 CLAUDE.md 解釋的死碼。使用者拍板直接刪。
- **`scripts/lib/signal-factors.ts` 單檔 → `scripts/lib/signal-factors/` 目錄**：
  - `index.ts` — barrel（`export * from` 各子檔）。`run-signal-scan.ts`（原 `../lib/signal-factors`）與 `lib/actions/dashboard.ts`（原 `../../scripts/lib/breakout-shared`）都改 import `signal-factors/index`（scripts 走 `nodenext`，目錄 import 需顯式 `/index`）。
  - `util.ts` — `clip` / `rankScore`。原本 `breakout-shared.ts` / `accumulation-shared.ts` 各有一份一字不差的複製，合併成單一出處。
  - `breakout.ts` — 原 `breakout-shared.ts` 全文（路徑修正 `../../generated` → `../../../generated`、`./types` → `../types`；`clip` 改 import `./util`）。
  - `accumulation.ts` — 原 `accumulation-shared.ts` 全文（同路徑修正）。`BASE_MIN_HISTORY_DAYS` 原本 breakout / accumulation 各自 `export const = 40`，barrel `export *` 會撞名（`TS2308`）→ `accumulation.ts` 改 `import { BASE_MIN_HISTORY_DAYS } from "./breakout"`（本來註解就寫「沿用 breakout 的預設值」，改成真的單一數值來源）。
  - `institutional.ts` — `computeInstitutionalFlow` / `computeMarginSurgePercentile` + 其 config 型別。
  - `staging.ts` — `consecutiveAboveBand` / `computeBreakoutMarginMonotone`。
  - `config.ts` — `SignalScanConfig` / `DEFAULT_SIGNAL_CONFIG` / `DEFAULT_INSTITUTIONAL_FLOW_CONFIG` / `resolveSignalConfig` / `baseCurveFor`。
- **`git rm` `breakout-shared.ts` / `accumulation-shared.ts`**：內容已搬進 `signal-factors/`、無人 import（直接刪、不進 `archive/`，留著只會有兩份同名函式）。
- **單測搬遷**：`scripts/lib/signal-factors.test.ts` → `scripts/lib/signal-factors/factors.test.ts`（`git mv`），import 改 `./index`。
- **`data/` 舊輸出資料夾刪**：`data/breakout-strength-results/` / `accumulation-score-results/` / `intraday-breakout-snapshots/`（僅舊腳本會寫、原本就 gitignored、未進版控），`.gitignore` 對應三行一併移除。
- **註解順手更新**：`market-regime.ts` / `mis-quotes.ts` / `_run-signal-scan.ts` 提及已刪檔名的註解改掉；殘留 `breakout-shared` / `accumulation-shared` 字串只剩 `signal-factors/` 內 4 處歷史敘述註解 + CLAUDE.md / docs。
- **未動**（如 ROADMAP 計畫）：`mis-quotes.ts` / `market-regime.ts` / `http.ts` / `types.ts` 本體。ROADMAP 3.1 抽出的兩套 `XxxConfig` 三件組（`breakout.ts` / `accumulation.ts` 內）保留——`run-signal-scan.ts` 正式跑吃的是 `config.ts` 的 `SignalScanConfig`，這兩套現只給「用 config 覆蓋參數比對排名」的輕量校準用。

---

**前端顏色統一管理（角色化 semantic token）（2026-09-01）**：把散在前端各檔的 Tailwind 色階 class（`text-slate-400` / `bg-blue-600` / `text-rose-400`…）收斂成 `app/globals.css` 一處定義的**角色化** semantic token。純 class 字串 / 顏色常數替換，零邏輯 / 版面 / 元件結構改動。分支 `feat/color-tokens`。

- **動機 + 走過的彎路**：漲跌色 / 號誌色 / 狀態色在舊寫法下全靠色相硬記（`rose-400` 到底是「漲」還是「風險」還是「號誌偏空」要看上下文猜），且同一個灰階值散在 11 檔、要整體微調得逐檔改。**第一版（已推翻）**做成「把 `slate-950` 改叫 `--color-bg`、`slate-900` 改叫 `--color-surface`」——23 個 token，其中 10 個灰階（`fg` / `fg-muted` / `fg-subtle` / `fg-faint` / `fg-ghost`）就是 slate scale 一對一改名。使用者指出這不是語意化、只是換皮，要真的照 `../quick-talk` 的角色模型。**第二版（本次採用）**改成角色 token。
- **`../quick-talk` 角色模型的三個核心**（研究其 `globals.css` + `components/ui/{button,card,badge,input}.tsx` 得出）：
  1. **token 是「角色」不是「深淺級數」**：只有 `background`/`foreground`/`card`/`muted`/`primary`/`destructive`… 這些用途名，**沒有** `fg-subtle`/`fg-faint`/`fg-ghost` 這種「弱、更弱、最弱」階梯。要更淡的字用透明度修飾子（`text-foreground/80`、`text-muted-foreground/70`）在 base 角色上打折。
  2. **每個「底色」配一個 `-foreground`**，成對用：卡片 = `bg-card text-card-foreground`、主按鈕 = `bg-primary text-primary-foreground`。元件不需要知道「卡片上的字該用哪階灰」。
  3. **按鈕分級靠角色組合 + 透明度**：`primary` 實心、`secondary` = `bg-muted` + `hover:bg-muted/70`、`destructive` = `bg-destructive/10` + `hover:bg-destructive/20`（淡底無邊框）。不需要為 hover 另立 token。
- **套件**：`pnpm add clsx tailwind-merge class-variance-authority`（`clsx@^2.1.1` / `tailwind-merge@^3.6.0`；cva 讓 `Button` 走 quick-talk 式 variant 定義，PLAN §0 原本說不裝、使用者拍板要裝）。`lib/cn.ts` = 4 行標配 `twMerge(clsx(inputs))`（放 `lib/` 不放 `scripts/lib/`）。
- **`@theme inline` 語法**：`:root` 用不帶前綴的名字（`--background` / `--muted-foreground` …），`@theme inline` 寫 `--color-background: var(--background)`（比照 quick-talk，不用同名自我參照）。curl dev server 抓編譯後 CSS 確認 `.bg-background` / `.text-muted-foreground` / `.text-foreground\/80`（透明度）/ `.hover\:bg-primary\/90`（hover 變體）/ `.border-destructive\/30` 全部正確生成。
- **token 清單（14 個，角色化）**：
  - 背景/前景（成對）：`--background`(#020617)、`--foreground`(#f1f5f9)、`--card`(#0f172a)、`--card-foreground`(=fg)、`--muted`(#1e293b)、`--muted-foreground`(#94a3b8)、`--border`(#1e293b)、`--input`(#334155)、`--ring`(#3b82f6)。
  - 主色（成對）：`--primary`(#2563eb)、`--primary-foreground`(#fff)。
  - 台股漲跌（專屬獨立）：`--up`(#fb7185)、`--down`(#34d399)。
  - 狀態語意（各一 base，字/邊框/底用透明度變體）：`--destructive`(#fb7185)、`--warning`(#fbbf24)、`--success`(#34d399)。
- **五階 slate 文字 → 兩 base + 透明度**：`slate-100`→`text-foreground`；`slate-300/200`→`text-foreground/80`；`slate-400`→`text-muted-foreground`；`slate-500`→`text-muted-foreground/70`；`slate-600`→`text-muted-foreground/50`。
- **大盤號誌燈直接複用狀態色**（`RegimeBanner` `DOT_CLASS`）：偏多=`bg-success`、中性=`bg-warning`、偏空=`bg-destructive`、無資料=`bg-muted-foreground/50`。台股漲跌色（`--up`/`--down`）語意不同、不複用。
- **提示框統一**：舊 `border-X-800 bg-X-950/40 text-X-300`（slate/amber/rose 各一組）→ 一律 `border-X/30 bg-X/10 text-X`。
- **涉及 13 檔**：`app/globals.css`（token）、`lib/cn.ts`（新）、`components/ui/{Card,Button}.tsx`、`app/layout.tsx`、`app/page.tsx`、`app/{screening,watchlist}/page.tsx`、`components/screening/ScreeningPanel.tsx`（最大宗 30 處）、`components/dashboard/{RegimeBanner,WatchlistPerfTable,PipelineRunner}.tsx`、`components/watchlist/WatchlistTable.tsx`。
- **`Button.tsx` 改用 `cva`**：`variant` 維度（`primary`/`secondary`/`danger`），無 `size`（要加再擴）。`primary` = `bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50`；`secondary` = `border border-border bg-muted text-foreground/80 hover:bg-muted/70`；`danger` = `bg-destructive/10 text-destructive hover:bg-destructive/20`。
- **`Sparkline` SVG 硬編 HEX**：Server Component 不能 `getComputedStyle` → 檔頂 `const SPARK_UP = "#fb7185"` / `SPARK_DOWN = "#34d399"` / `SPARK_BASELINE = "#475569"` + 註解「對應 `--up` / `--down` / 中性灰」。grep 收尾唯一允許的 HEX 例外。
- **驗證**：`pnpm exec tsc --noEmit` 乾淨、`pnpm build` 通過（三頁 `ƒ Dynamic`）、`curl` 三頁 200、`grep -rE "(text|bg|border|ring|divide)-(slate|blue|rose|emerald|amber|red|green|yellow|zinc|gray)-[0-9]" app/ components/` 回空。編譯後 CSS 抽驗 30 個 utility（含 `/10` `/30` `/50` `/70` `/80` 透明度、`hover:` 變體）全部生成。
- **8 項「配色會變」的簡化**（已事先逐項向使用者說明並取得同意）：
  1. Button primary disabled：灰底灰字（`bg-slate-700`）→ 半透明藍（`opacity-50`）。
  2. Button danger：rose 邊框 + 深底 + 深 hover → 淡紅底 10% 無邊框 + hover 20%（quick-talk `destructive` 標準）。
  3. Button secondary hover：`800→700` 變深 → `bg-muted`→`/70` 透明度變化（解掉第一版那個「hover 消失」回歸）。
  4. input / textarea 底：實心 `bg-slate-800` → `bg-transparent` + `border-input`（露出卡片色）。
  5. 「突破」pill 文字：`text-blue-300`（亮藍）→ `text-primary`（blue-600 深藍，跟主按鈕同色）。
  6. success 色階：燈號 `emerald-500` / 跌 `emerald-400` 原本差一階 → 統一 `--success` = `#34d399`（跟 `--down` 同值），燈號綠亮一階。
  7. 五階 slate 文字 → 兩 base + 三檔透明度，中間階對比略有位移（多數肉眼難分）。
  8. 提示框統一成 `border-X/30 bg-X/10 text-X`，底色比舊的 `bg-X-950/40` 更淡、更一致。

---

**Screening 頁表格 UI 改版（2026-09-01）**：`docs/PLAN.md`「Screening 頁表格 UI 改版」。`/screening` 結果表格重畫：tab 收斂三階段、欄位比照設計圖、展開列改「線圖 / 法人籌碼 / 突破因子」三欄。**零評分邏輯改動**（公式 / 權重 / 曲線 / gate / 階段判定全不動；改動前後 `2026-08-31.json` 逐檔 `totalScore` / `rank` / `scores` 完全一致）。分支 `feat/screening-ui`。

- **後端只補回傳值**（`SignalResult` 加 3 個 optional 欄位，只在 `breakout-day` / `extended` 的 push 帶，`pre-breakout` undefined）：
  - `volumeRatio?: number` — 觸發量比（`staged.volumeRatio`，breakout 階段必有）。
  - `inst?: { trustRatio, foreignRatio, todayTrustDir, todayForeignDir }` — 法人籌碼中繼值。
  - `factors?: { breakoutMarginScore, breakoutMarginPct, proximityShortScore, proximityLongScore, proximityShortPct, proximityLongPct }` — 突破因子分數 + 原始值。
- **`computeInstitutionalFlow` 回傳擴充**（`institutional.ts`）：加 `trustRatio` / `foreignRatio`（近 5 日淨買超合計 ÷ volumeMa20，volumeMa20 缺 → 0）、`todayTrustDir` / `todayForeignDir`（`sign(todayXxxNetBuy)`；傳入 null → null）。`score` / `degraded` / `marginChasing` 邏輯一字不改（`trustSum` / `foreignSum` 從原本 `toFlowScore` 內的 reduce 提出來共用，不重算）。
- **`computeProximityToHigh` 回傳擴充**（`breakout.ts`）：加 `shortScore` / `longScore`（函式內本來就分開算，只是原本合成後不回）+ `shortPct` / `longPct`（收盤距各窗最高點的 % 距離，`<= 0`；新增 `proximityPct()` helper）。合成公式 `shortScore*0.5 + longScore*0.5` 不動；degraded 早退分支新欄位給預設值（`shortScore=longScore=50`、`shortPct=longPct=0`）。
- **`run-signal-scan.ts` 組裝**：`computeBreakoutFactors` 的 `BreakoutFactorOutput` 多回 `raw` 子物件（純中繼值），新 helper `breakoutExtras(factors)` 把 raw 組成 `inst` / `factors`，在 eod / realtime 兩處 `results.push` 展開。realtime 路徑 `todayTrustDir` / `todayForeignDir` 恆 null（當日法人拿不到），`volumeRatio` 有（即時量比）。
- **Tab（PLAN §1）**：移除「全部」。剩三個順序固定 **今日突破 → 已延伸 → 醞釀中**，預設「今日突破」。`StageFilter` 型別去掉 `"all"` = `SignalStage`。清掉只為「全部」存在的 `sortKey==="rank"` 次要排序、跨階段提示段。每 tab 內 `rank` 是該階段內名次。
- **表格欄位（PLAN §2）**：排名 / 代號 / 名稱 / **價格**（原「收盤/即時」，`estimated` 尾隨琥珀「估」badge）/ 漲跌%（百分比、漲紅跌綠、帶正負號）/ **狀態**（階段中文 pill，`breakout-day`=`bg-destructive/10 text-destructive`、`extended`=`bg-warning/10 text-warning`、`pre-breakout`=`bg-muted text-muted-foreground`）/ **量增**（`x{volumeRatio}`，醞釀中 `—`）/ **籌碼**（一句話結論 chip：`法人偏多`/`法人偏空`/`融資追價`/`資料不足`/`中性`，醞釀中 `—`；盤中沿用昨日籌碼附「昨」badge）/ 總分。移除獨立「降級項目」欄（移到展開列底部）。舊「追」badge 從價格欄移到籌碼 chip（marginChasing → 紅「融資追價」）。`Column` 加 `sortable?: boolean`，`inst` 欄 `false`（chip 是分類不是量，th 不掛 onClick / 不顯箭頭）。
- **展開列三欄（PLAN §3）**：`RowGroup` 展開渲染 `<SignalDetail row={r} />`（原 `Detail` function 搬進新檔 `components/screening/SignalDetail.tsx`）。
  - **stage 分支**：`pre-breakout` → **只有左側線圖**（不渲染法人 / 突破因子 / 舊評分明細網格，使用者明確要求）。`breakout-day` / `extended` → 三欄卡（`grid-cols-1 lg:grid-cols-3`，深色卡 `rounded-lg border border-border bg-card p-4`）。頂部 margin-chasing 紅框 / estimated 琥珀框兩種 stage 都渲染。degraded 資訊移到底部一行灰字。
  - **左欄 `SignalSparkPanel`**（client）：按需撈——`useEffect` 依 `code` 呼叫 `getSignalSpark(code)`，載入中「資料載入中…」框。`rising = row.changePercent >= 0` 由 `SignalDetail` 傳入。切換展開列卸載重撈，不做跨列快取（YAGNI）。
  - **中欄 `InstitutionalFlow`**（依 UI.md 第一節）：頂部 chip（純函式 `resolveInstChip(inst, warnings)` → `{ text, tone }`，依「近 5 日流向強弱 × 今日方向 × marginChasing」組合表，強/中/弱門檻 `ratioSum > 0.3` / `> 0` / `<= 0` **首版拍板待校準**）。投信 / 外資各一列 **diverging bar**：**柱長用 `inst.trustRatio` / `inst.foreignRatio`（近 5 日淨買超 ÷ volumeMa20），不是股數、不是分數**——柱長才跟分數公式同一把尺，達 `±clipDivisor`(=0.5) 即滿格 = 分數已封頂。中線 = 0，右綠買超 / 左紅賣超，寬度 = `min(|ratio|, 0.5) / 0.5 × 50%`。右側文字 `{(ratio*100).toFixed(0)}%` 帶正負號。小箭頭 ▲/▼ 依 `todayXxxDir`（**今日單日**方向，與柱子近 5 日累積是兩回事）；柱子與箭頭不同號（翻臉）→ 柱子 `opacity-50`。融資追價警示列只在 `warnings.includes("margin-chasing")` 時渲染（不觸發完全不佔版位）。盤中今日法人 null → 底部灰字「以上為近日資料」。顏色 = `success`/`warning`/`destructive` token，不複用台股漲跌色。
  - **右欄 `BreakoutFactorBars`**（依 UI.md 第二節）：4 列（突破幅度 / 相對強度 / 距 60 日高點 / 距一年高點），每列 4 欄（標籤 / 長條 / 分數 / 原始值灰字）。**長條寬度用「因子內部分數（0~100）」不是原始數值**（UI.md 2.2 核心規則：四因子原始單位不同，直接比長短有誤導性）；原始值只放右側灰字，不參與長條。分數分級 `barTone(score)`：`>=70` `bg-success` / `40~69` `bg-warning` / `<40` `bg-destructive`。相對強度用 `row.scores.relativeStrength`（本身即百分位分數）。**不放位階（base）因子**（使用者已定；設計圖右欄無 base）。
- **`Sparkline` / `buildSparkSeries` 抽出**（兩個使用者 → 不違反 YAGNI）：
  - `components/ui/Sparkline.tsx`（新）：從 `WatchlistPerfTable.tsx` 搬 `Sparkline` function + `SPARK_UP` / `SPARK_DOWN` / `SPARK_BASELINE` HEX 常數（全 `export`）。`SPARK_CLIP` / `SPARK_WINDOW` / `SparkPoint` 留在 `lib/dashboard-spark.ts` 不動（`Sparkline.tsx` 從那 import）。放 `components/ui/` 與 `Card.tsx` / `Button.tsx` 同層。
  - `lib/spark-series.ts`（新）：`buildSparkSeries(quoteWindow, midByDate)` 純函式（`dashboard.ts` 的 inline map 抽出，行為完全等價）。`dashboard.ts` 改呼叫它、`getSignalSpark` 也用它。
  - `WatchlistPerfTable.tsx` 改 `import { Sparkline } from "../ui/Sparkline"`，刪本地定義與常數（行為不變）。
- **`getSignalSpark(code)` action**（`lib/actions/signal-scan.ts`）：撈該 code 近 `SPARK_WINDOW` 筆 `DailyQuote`（date desc）+ `TechnicalIndicator`（bollingerMid），`buildSparkSeries` → `SparkPoint[]`。查不到 → `[]`。realtime / eod 都用同一支（讀 DB 歷史，與當下報價源無關）。
- **驗證**：`tsc --noEmit` 乾淨、`pnpm tsx --test scripts/lib/signal-factors/factors.test.ts` 38 案全過（新增 `computeInstitutionalFlow` 中繼值 3 案 + `computeProximityToHigh` 中繼值 4 案，含 `shortScore*0.5 + longScore*0.5 === score` 等價性）、`run-signal-scan.ts --date=2026-08-31` 跑得完 + JSON 有新欄位 + 逐檔分數與改動前 0 diff、`pnpm build` 通過（三頁 `ƒ Dynamic`）、`curl /screening` `/` 皆 200。
- **待校準（PLAN §7）**：`resolveInstChip` 的強/中/弱門檻（`ratioSum` `0.3` / `0`）首版拍腦袋，累積實盤觀察後調。

**UI.md 規格重點（併入本段，`docs/UI.md` 已刪）**：
- **法人籌碼區長條量尺規則**：diverging bar 的柱長 = 對應機構的 `ratio`（近 5 日淨買超 ÷ volumeMa20），**不能畫股數或原始金額**——柱長要跟分數公式同一把尺，達 `clipDivisor=0.5` 即圖表滿格、也代表分數已封頂不再加分。每列文字 = `ratio × 100`（%），是「買超相當於均量的百分之幾」，不是股價漲跌% 或股本佔比。小箭頭 = **今日單日**淨買超方向（`todayXxxNetBuy` 正負），跟柱子（近 5 日累積）是兩個不同的量；柱綠箭頭紅 = 近期買、今天賣（翻臉），柱子調淡打折。融資追價警示列只在 `marginChasing === true` 顯示、不觸發不留空位（觸發條件含「今日淨賣超」，故必與翻臉情境同時出現，不會單獨出現在買超情境）。
- **1.4 chip 組合表**：由「近 5 日流向方向 × 今日方向 × marginChasing」三條件決定 chip 文字與顏色。綠 = 高分無警示 / 琥珀 = 封頂但無融資警示 / 紅 = 封頂且有融資警示或直接同步撤出 / 灰 = 今日未定（盤中）或籌碼資料不足（degraded）。
- **突破因子長條用分數不用原始值**：四因子原始數值單位完全不同（突破幅度 = % 乖離、相對強度已是百分位、距高點 = % 距離），直接把原始數值當長條寬度會「看起來能比長短、實際在比不同單位的東西」，有誤導性。正確做法 = 每因子後端已被各自曲線函式換算成統一 0~100 內部分數，長條寬度用這個分數，原始值只放旁邊文字欄位。`breakoutMargin` 單調遞增（`computeBreakoutMarginMonotone`：0~3% 線性 40→100，>3% 封頂 100，不倒扣），UI 不需特別處理、分數本身已反映。

---

**8/31 行情覆蓋調查 → 權證誤殺修正 + 下市標記 `delistedAt`（2026-09-01）**：`docs/PLAN.md`「股票資料品質修正」。起因：核對 8/31 行情發現 2,148 檔一般股票有 207 檔缺，逐檔追查（DB 比對 + `MI_INDEX` 核對 + FinMind 逐檔查 + 網路查證）分四類：

- **權證過濾誤殺（bug，2 檔）**：`toSecurityType` 的「名稱含購/售 → warrant」排在「4 碼數字 → stock」之前，**2945 三商家購**與 **3085 新零售**被三支 pipeline 腳本（行情/法人/融資融券各持一份複製）每天跳過——行情停在 8/27（FinMind 回補塞的）、融資融券完全空白。**修法**：抽共用 `scripts/lib/security-type.ts`（三腳本改 import）、「4 碼數字 → stock」提前（權證代號一律 6 碼，無誤放風險；名稱判斷留作 put 權證等 fallback）。單測 `security-type.test.ts` 7 案；以 8/31 全市場 44,454 筆實資料跑新舊分類 diff，**僅 2945/3085 兩檔改變、零權證誤放**。
- **早已下市殭屍（約 168 檔）**：99 檔從無行情（2020 回補起點前下市）+ 約 69 檔停在 2020~2026 年中（康友-KY、中壽、新光金、京城銀…）。
- **當日零成交（約 29 檔，上櫃冷門股為主 + 正峰 1538）**：官方源開高低收 `--`，解析器跳過＝設計行為。FinMind 核對這些股 8/31 列存在但 `close=0`、量個位數～百股，沒死。
- **停牌/換股（4 檔）**：三商壽 2867（8/20 停止買賣、9/1 併玉山金永久下市）、中光電 5371（8/24 停牌、9/3 轉中光電投控 3718 上櫃 1:1 換股）、巧新 1563（現金減資 25%，9/7 復牌）、沛爾生醫-創 6949（1 拆 20 面額變更，9/7 復牌）。

**回補（修完過濾後執行）**：TWSE `fill-daily-quotes --date=2026-08-28/-08-31` 重跑（2945 兩日入庫 = bug 修好直接證據）；FinMind 補兩檔缺口至 9/1（3085 的 8/28、9/1 是零成交日，FinMind `close=0` 跳過不入庫，正確）；`calculate-technical-indicators.ts 2945 3085` 全歷史重算（3,223 筆）；`fill-margin-trading --backfill=45` 實得 45 個交易日（6/29~8/31）→ **3085 拿到完整 45 日；2945 在 `MI_MARGN` 個股明細裡不存在 = 非信用交易標的**，無資料是市場事實非 bug（`computeMarginSurgePercentile` 回 null → margin-chasing 永不觸發，行為正確）；法人 TWSE 8/28~9/1 三日重跑補齊，**3085 的 TPEx 法人缺口（7/9 起）FinMind 回 0 列補不到**，接受 degraded（修好過濾後每日更新自然接上）。

**`Stock.delistedAt DateTime?`（migration `20260901084415`）**：null = 存續；值 = 最後行情日（從無行情 = `2020-01-01` sentinel）。維護腳本 `scripts/backfill/mark-delisted.ts`（手動月頻，**不進 daily-pipeline**——復牌股行情入庫不依賴 flag，`fill-daily-quotes` 是 API 回什麼 upsert 什麼；flag 消費者只有覆蓋率分母與 FinMind backfill 名單，遲鈍幾週無害）：門檻「最後行情距 DB 最新交易日 > 60 日曆日」雙向同步（標記 + 復牌自動清除）。首輪標記 170 檔、存續 1,978 檔。抽查：巧新/沛爾/2945/3085 正確存續；**三商壽/中光電最後行情才過 ~2 週未達 60 天門檻、本輪未標記**（PLAN §3 抽查預期與自訂規則矛盾，以規則為準，下次月跑自然入列）。

**下游接線**：`getDbHealth` 分母改 `delistedAt: null`（8/31 行情覆蓋率 90.5% → **98.2%**，法人 94%、技術指標 98.3%、融資融券 94.1%；殘餘缺口＝零成交股＋停牌股）；`backfill-daily-quotes` / `backfill-institutional-trading` 撈清單排除已下市（省 FinMind 配額）；`run-signal-scan.ts` realtime 的 `Stock` 撈取加 `delistedAt: null`（省 MIS batch；eod 路徑從當日 `DailyQuote` 出發天然排除，不用動）。驗證：`tsc --noEmit` 乾淨、45 案單測全過。

---

**daily-pipeline 補當日 TAIEX（2026-09-01）**：起因是 `daily-pipeline.ts` 從沒有任何一步抓加權指數當日行情——`fill-daily-quotes.ts` 抓的是全市場個股，`MI_INDEX` 個股明細不含加權指數這筆，所以 TAIEX 的 `DailyQuote` 只會停在上次手動跑 `backfill-index-quotes.ts` 的那天，之後第 4 步技術指標對 TAIEX 沒新資料可算、第 5 步大盤濾網一直吃舊的 MA60。**選型**：實測 `MI_INDEX` 的 `tables[0]`「價格指數(臺灣證券交易所)」有帶「發行量加權股價指數」收盤，時效與個股報價同步，但**只有收盤沒有 OHL**；FinMind `TaiwanStockPrice?data_id=TAIEX` 有完整 OHLC，且 2026-09-01 當天盤後即有資料。選 FinMind——完整 OHLC 跟 `DailyQuote` 其他列一致、不用在 schema 搞特例，且與既有 `backfill-index-quotes.ts` 同 API 同 dataset。**改動**：`backfill-index-quotes.ts` 抽出 `export fillTodayIndex(isoDate, injectedPrisma?)`（比照 `fillOneDayTwse`：吃 isoDate、可傳 pipeline prisma 單例不 `$disconnect`、回 `{ processed, isNonTradingDay }`；FinMind 非交易日不回該日 row → `isNonTradingDay: true`），共用 helper `ensureIndexStock` / `upsertIndexRows`，全區間回補 `main()` 加 `import.meta.url === file://${process.argv[1]}` guard 避免被 import 時觸發。`daily-pipeline.ts` 新增**第 1.5 步**（報價後、籌碼前），**關鍵路徑**：FinMind 拋錯就 `throw PipelineStepError`；但「FinMind 尚無當日 TAIEX（資料源不同步）」只印警告 + `warningCount++` 不中斷（大盤濾網會自動降級 `step1-breadth-only`）。收尾 log 加「今日加權指數寫入筆數」。pipeline 步數描述由「七步」改「八步」。**驗證**：`tsc --noEmit` 乾淨；`backfill-index-quotes.ts` 全區間重跑寫入 1619 筆、DB 查得 2026-09-01 TAIEX `O 46177.11 / H 46948.72 / L 46081.11 / C 46948.72`。TAIEX 的 `TechnicalIndicator` 仍停在 08-31（`"latest"` 模式只 upsert 最新交易日，下次 pipeline 第 4 步會帶上 09-01）。**附帶發現（未修）**：`.env` 的 `FINMIND_API_KEY` 值前面多一個 `"` 引號，帶 token 呼叫會 400 `Token is illegal` → 一直在吃未註冊額度（300 次/小時）。

---

**Watchlist 頁卡片 gallery 改版（2026-09-01）**：依 `docs/PLAN.md`。`/watchlist` 從「三表鬆散快照表格 + 買入狀態 input」改成「卡片 gallery + 階段分頁」，視覺與資料對齊 screening 展開列。

- **階段分頁**：三 tab（首次突破 / 延續爆發 / 醞釀中，順序固定，預設「首次突破」），**即時用 `consecutiveAboveBand()` 判定**（`consecutiveDays <= 0` → pre-breakout；`1~2` → breakout-day；`> 2` → extended），**不是 `WatchlistItem.source` 靜態欄位**（source 是「當初怎麼加入」，會與現況脫節）。空 tab 顯示「此分類目前無觀察股」。`source` 欄位不動，加入清單流程不變。
- **資料源自動切換（A 方案）**：`listWatchlist()` 開頭查「DB 最新一般股票交易日 == 台北今日？」→ 相符用 DB（卡片左上 ⚡ `LightningBoltIcon`）；不相符**一進頁自動打 `fetchAllMisQuotes`**（阻塞 render，< 50 檔 = 1 批 ≈ 1~2 秒，**無按鈕、無 JSON 快取、無節流**——每次進頁 / 重整都重打）。MIS 缺成交價用 `high` 代入（`priceSource: "estimated"`，卡片「估」badge）；`z`/`h` 皆缺或整體抓取失敗 → 退回 DB 昨收。抓到即時後**左上仍 🕐 `ClockIcon`**（日期非今日交易日定案），⚡ 只給 `dataFresh: true`。volumeRatio 盤中用 `cumulativeVolume / elapsedRatio` 估全日量（比照 run-signal-scan）。
- **籌碼**：恆用 DB 最新一筆 `InstitutionalTrading`。`InstitutionalFlowPanel` 底部「尚未取得今日法人資料，以上為近日資料。」文字（`todayXxxDir` 為 null 時自動顯示）沿用——**不加時鐘**（使用者確認沿用文字區塊）。「今日方向」只有籌碼日期 == `refDate` 才算，否則 null。
- **強度 PR 欄（方案 1）**：相對強度是全市場百分位，watchlist <50 檔算不出。改**讀 `data/signal-scan-results/` 最新 `{YYYY-MM-DD}.json`（eod 定案）的 `scores.relativeStrength`**（就地寫在 `watchlist.ts`，不動 `signal-scan.ts`）。不在掃描結果裡 → 「—」；掃描結果日期 != 卡片資料日期 → `PR{n}` 旁加 🕐（`relativeStrengthStale`）。**MEMORY 提醒「committed JSON 會 lag code」——這裡只當展示值、best-effort，可接受**。
- **買入狀態 input 全移除**：`isPurchased` / `buyPrice` / `buyDate` / `targetPrice` / `stopLossPrice` / `notes` 的 UI 拿掉。**schema 欄位 + `updateWatchlistItem` action 保留不動**（YAGNI：不做移除 migration，未來可能再用）。
- **醞釀中卡片本次不做法人籌碼區塊**（跟 screening 現況一致）——使用者要另想 UI、之後補規格。因此 `run-signal-scan.ts` **完全沒改**。
- **共用元件目錄 `components/signal/`（新）**：`components/screening/` 綁「選股頁」，但 watchlist 也要法人籌碼 + 因子展示 → 開 `signal/`（對應 `scripts/lib/signal-factors/`）。內容：
  - `labels.ts`：`STAGE_LABELS` / `STAGE_PILL_CLASS` / `STAGE_ORDER` + `FACTOR_LABELS`（標籤中文名單一出處，純常數）。從 `ScreeningPanel.tsx` 搬出，`ScreeningPanel` / `BreakoutFactorBars` / `SignalDetail` 改 import 這裡。**stage 標籤順帶統一**：screening 舊「今日突破」/「已延伸」→「首次突破」/「延續爆發」（兩頁一致，`app/screening/page.tsx` 說明文字同步）。
  - `InstitutionalFlowPanel.tsx`：`git mv` 自 `components/screening/InstitutionalFlow.tsx`。改 local `Inst` 型別（脫離 `SignalScanView["results"]`，讓 watchlist 也能傳）、`export resolveInstChip` / `TONE_CHIP` / `Tone`（卡片頂部籌碼結論 chip 重用）、加 `showChip?: boolean`（watchlist 卡片頂已有 chip → 傳 false 不重複）。screening `SignalDetail` 的引用同步改名。
  - `FactorList.tsx`：卡片底排「label + 數字」（**非長條圖**——screening 的 `BreakoutFactorBars` 保留長條版；兩者只共用資料 + `FACTOR_LABELS`，不硬包成一個帶 `variant` 的元件）。breakout 階段 = 量增 / 距年高% / 強度 PR / 突破%（原始值）；pre-breakout = 量增 + K棒/力道/位階（三分數 0~100，比照 `WatchlistPerfTable` 的 `ScoreItem` 配色）。
  - `FreshnessBadge.tsx`：`fresh ? <LightningBoltIcon/> : <ClockIcon/>`（`@radix-ui/react-icons`，本次 `pnpm add`）。
- **`components/watchlist/`**：`WatchlistGallery.tsx`（"use client"：tab state + `md:2 / xl:3 / 2xl:4` 欄 grid）、`WatchlistCard.tsx`（單張卡）。舊 `WatchlistTable.tsx` `git rm`。
- **`lib/actions/watchlist.ts`**：`listWatchlist()` 回傳型別重寫成 `WatchlistCardRow`（`stage` / `dataFresh` / `priceSource` / `refDate` / `close` / `changePercent` / `volumeRatio` / `spark` / `candleScore`,`volumeScore`,`baseScore`,`degraded` / `inst` / `factors`）。計算全 import 既有純函式（`consecutiveAboveBand` / `computeCandleShape`,`VolumeStrength`,`Base` / `computeInstitutionalFlow` / `computeProximityToHigh` / `buildSparkSeries`），不重寫評分。每檔數次 `findFirst`/`findMany`（清單 < 50 可接受，延續 `getWatchlistPerformance` 現狀）。`SignalStage` 型別就地宣告（不 import `run-signal-scan.ts`，避免整個掃描管線進 bundle）。
- **驗證**：`tsc --noEmit` 乾淨、signal-factors 單測 38 案全過、`/` `/screening` `/watchlist` 三頁 curl 200。實測（2026-09-01，DB 有當日資料 → `dataFresh: true` 全走 DB / ⚡）：5 檔觀察股 → 首次突破 4 / 延續爆發 1 / 醞釀中 0；breakout 卡片正確渲染 stage pill + 籌碼結論 chip + 法人 diverging bar + `PR96`（2455）/ `PR94`(8111) / `PR78`(3706)（皆在當日掃描結果內、`scanDate == refDate` → 無 🕐）+ 距年高% / 突破% 原始值；6226 在 breakout 階段但不在掃描結果 → 強度顯「—」。**🕐 盤中路徑本次未實測**（今日非盤中，`dataFresh` 恆 true）——待交易日盤中或臨時把 `dataFresh` 判斷改 false 驗一次。

尚未開始/明確不做：**watchlist 醞釀中卡片的法人籌碼區塊**（使用者思考 UI 中，之後提規格）、**watchlist 盤中資料源快取 / 節流**（YAGNI，A 方案每次進頁重打；若之後盤中頻繁進出再加 action 層 in-memory 60 秒節流，不落檔）、估值歷史回補（`fill-gap-valuation.ts` 已支援 `--date` 隨時可補，但依計畫不主動回補）、heatScore 欄位與市值加權熱度（第一版等權即可，分數用時現算）、股本更新排程（月頻手動跑）、新聞情緒分析（`NewsArticle.sentiment`/`sentimentScore` 欄位已存在但尚未有腳本填值）、Tag/StockTag 篩選邏輯（`topic_alignment` 因子固定中性分）、AnalysisResult 產出流程（Phase D）、`screening/`（統一入口 `run-signal-scan.ts`；舊三支已於 4.5.4 退役刪除）與 `backfill/` 各支皆為獨立手動執行（不在 `daily-pipeline.ts` 內）、`run-signal-scan.ts` 的階段權重 / 法人因子曲線校準（首版全拍腦袋，靠肉眼看單日排名 + 實盤觀察調）、**整個回測系統**（3.0～3.7 已從 `main` 移除、擱置，見上方；程式碼在 `feat/backtest-ui-3.6-3.7` 分支，OOM 待修）。`archive/` 的 `calculate-screen-score.ts` / `run-screener.ts` / `fetch-candidate-details.ts` / `top20-gainers.js` 已停用不維護。

（每次進度更新，麻煩幫我一併更新這個區塊。）
