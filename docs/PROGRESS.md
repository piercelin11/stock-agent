# 目前進度

已完成：資料庫 schema（12 個 model，含 `MonthRevenue`、`FinancialStatement`、`InstitutionalTrading`、`TechnicalIndicator`）建立並套用 migration、`prisma/seed.ts` 執行完成、逐支股票用 FinMind 回補歷史報價（`backfill-daily-quotes.ts`）、技術指標計算（`calculate-technical-indicators.ts`）、全市場篩選（`run-screener.ts`）、全市場報價缺漏回補（`fill-daily-quotes.ts`，TWSE 用 `MI_INDEX` API 可補任意歷史日期，TPEx 用 TPEx OpenAPI 只能補當天）、每日排程主控腳本（`daily-pipeline.ts`，串起缺漏檢查→補齊 TWSE+TPEx→算指標→跑篩選）皆已完成並手動測試成功（2026-08-17）。`fill-daily-quotes.ts` 已排除權證/可轉債寫入，資料庫裡既有的權證/可轉債資料也已清理完畢（2026-08-17）。

**Phase B 擴充（估值/市值/產業熱度）已完成（2026-08-18）**：依 `docs/PLAN.md` 完成 schema migration（`StockValuation`、`IndustryHeatSnapshot`、`Stock.sharesOutstanding`，migration `20260818130854_add_valuation_heat_shares`）、`scripts/calculate-industry-heat.ts`（已回補 20 個交易日）、`scripts/fill-gap-valuation.ts`（TWSE + TPEx 皆可查歷史日期，已抓 2026-08-17 與 08-18）、`scripts/update-shares-outstanding.ts`（已手動跑過一次，TWSE 1095 檔 + TPEx 890 檔）。估值與熱度已整合進 `daily-pipeline.ts`（放在技術指標之後、篩選之前，失敗不中斷 pipeline），冪等性與失敗路徑皆已驗證。股本更新為月頻手動執行，不進 daily pipeline。

**Phase C（候選股深度資料抓取）已完成（2026-08-18）**：`scripts/fetch-candidate-details.ts` 讀取 `data/screener-results/{日期}.json` 的候選股清單，逐支依序抓取籌碼面（`InstitutionalTrading`）、月營收（`MonthRevenue`）、季報（`FinancialStatement`）、新聞（`NewsArticle`/`NewsStock`）四個面向，寫入資料庫。已用 2026-08-18 篩選結果（23 檔候選股）實測成功，零失敗。`NewsArticle.link` 已加上 `@unique` 約束（migration `20260818112801_add_news_article_link_unique`）。

**全市場評分腳本（screen_score）已完成（2026-08-18）**：依 `docs/PLAN.md` 完成 Step 0（`calculate-technical-indicators.ts` 新增 `volatility20d`/`maxDrawdown20d`/`atr20`/`rsi14`/`macdStatus` 五欄位，migration `20260818155230_add_screen_score_indicators`）與 Step 1（新增 `scripts/calculate-screen-score.ts`，九因子加權評分，匯出 `calculateScreenScore(date)`，結果輸出至 `data/screen-score-results/{date}.json`，不寫資料庫）。`rankScore`（橫向百分位排名函式）已用假資料單元測試三種邊界情況（全部有值/部分 null/全部 null）；開發過程中發現並修正一個方向性 bug（`lowerIsBetter=false` 時最大值誤排到最低分，已修正並用台積電市值/成交金額實測驗證為 100 分）。已用 2026-08-18 全市場資料實測（1947 檔一般股票），肉眼核對前 20 名、抽查 PE/PB 因子方向、確認估值缺值股票（如 DR 存託憑證）正確降級為 `naScore` 不崩潰。**尚未接進 `daily-pipeline.ts`**（依計畫先獨立驗證，穩定後再考慮整合）。

**突破強度篩選腳本（breakout-strength）已完成（2026-08-19）**：新增 `scripts/calculate-breakout-strength.ts`，依 `docs/PLAN.md` 實作「觸發 → 資格門檻 → 強度評分」三層架構，回答「今天市場的焦點在哪」，與 `calculate-screen-score.ts` 的九因子橫向排名互補（後者是全市場排序，前者是帶量突破訊號的獨立篩選）。匯出 `calculateBreakoutStrength(date)`，結果輸出至 `data/breakout-strength-results/{date}.json`，不寫資料庫。用 2026-08-18 資料實測（1947 檔一般股票 → 觸發 33 檔 → 通過市值/量能門檻 16 檔），完成 PLAN.md 列的 5 項驗證：候選名單肉眼核對（抽查 2605、5608 確認真實突破布林上軌且量比達標）、firstBar 抽查（確認昨日收盤在帶內對應 100 分）、stats 三數字合理遞減、同日重跑兩次輸出完全一致（純確定性）、`sharesOutstanding` 為 null 的股票會被市值門檻正確擋下且計數 log（今天無實際觸發案例撞上此分支，邏輯已審查確認不會 crash）。歷史視窗（base 位階、proximityToHigh）目前資料庫約 60 個交易日，皆已寫成「視窗上限 N 天、不足時降級並標記 `degraded`」，未來歷史補齊後不需改程式碼即可自動使用滿 240 日視窗。

**launchd 排程驗證 + 三大法人籌碼腳本 + daily-pipeline.ts 改寫（2026-08-23）**：先用一支一次性測試腳本（已刪除）驗證「launchd 定時觸發 → tsx 執行 Prisma 查詢 → 寫入 log」整條流程可行，關鍵結論：`generated/prisma/` 是 Prisma 產生的純 `.ts` 原始檔（非編譯過的 `.js`），plist 裡不能直接用 `node` 執行 `scripts/*.ts`，必須讓 `ProgramArguments` 指向 `node_modules/tsx/dist/cli.mjs`（絕對路徑）、把目標腳本當參數傳入。確認可行後新增 `scripts/fill-institutional-trading.ts`：抓取 TWSE `T86`（三大法人買賣超日報，支援任意歷史日期）+ TPEx `tpex_3insti_daily_trading`（不支援日期參數，永遠回傳「目前最新一天」，跟 TPEx 報價 API 同樣限制）寫入 `InstitutionalTrading`，只 upsert `Stock` 表已存在的一般股票代號（不像 `fill-daily-quotes.ts` 會自動新增股票——籌碼資料不該是新股票的來源）。同時把 `daily-pipeline.ts` 整支改寫成五步：補齊今日 TWSE+TPEx 報價 → 抓今日 TWSE+TPEx 三大法人籌碼 → 抓今日估值（`fillOneDayValuation`）→ 算技術指標 → 算產業熱度，移除歷史缺漏回補與 `runScreener` 兩塊（這些功能仍在，只是改回個別手動執行，不再進 daily pipeline）。

**「非當日資料」防呆邏輯（同日追加）**：起因是 2026-08-23（週日）跑 pipeline 時發現 TPEx 報價/籌碼把上一個交易日（08-21）的舊資料當成「今天」寫入 `DailyQuote`/`InstitutionalTrading`，upsert 到同一天雖無害但容易誤判「今天有交易」。修正後行為：`fillTodayTpex(expectedIsoDate?)` 與 `fill-institutional-trading.ts` 的 TPEx 那段都會比對 API 回傳日期與預期日期，**不符就跳過寫入**（不再寫入舊資料）並印出 `⚠` 開頭的 warning、回傳 `isStaleDate: true`。`daily-pipeline.ts` 在報價步驟後判斷：**TWSE 無資料（非交易日）且 TPEx 也拿不到當日資料時，直接印出訊息並提前結束整支 pipeline**（不跑籌碼/估值/技術指標/熱度，因為兩邊都沒有新資料可用時後面的計算沒有意義）；只要有任一邊有正確當日資料就繼續跑完所有步驟（TWSE、TPEx 交易日曆理論上一致，不會有一邊開盤一邊沒開盤的情況，這裡防的是 TPEx API 本身「回傳日期落後」的資料新鮮度問題，不是市場狀態不同步）。總結區塊新增「今日警告：N 則」統計（來自報價/籌碼兩處 `isStaleDate` 加總），方便一眼確認當天有沒有異常，不用逐行翻 log。log 是 append 累積寫入 `logs/daily_pipeline_stdout.log`／`_stderr.log`，不會覆蓋。

已完整跑過三次驗證：（1）平日交易日資料完整時的正常路徑 exit code 0（2148 檔股票技術指標、44-45 個產業熱度、TWSE+TPEx 籌碼皆正確寫入，抽查 2609/2892 兩檔股票籌碼數字與 API 原始回應逐位元核對正確）；（2）今日（週日）非交易日的提前結束路徑，1.7 秒內正確判斷並結束、警告數正確計為 1；（3）「TWSE 有資料但 TPEx 過期」的混合情境，以 `fillOneDayTwse("2026-08-21")` + `fillTodayTpex("2026-08-22")` 模擬驗證繼續執行的判斷邏輯正確。**尚未串接 `InstitutionalTrading` 到任何 calculate 腳本**——`calculate-screen-score.ts`/`calculate-breakout-strength.ts` 目前都不讀籌碼資料，這次寫入的資料短期內只是存底。**plist（`daily_pipeline.plist`）已建立在專案根目錄，設定每天 17:00 觸發，尚未由使用者手動複製到 `~/Library/LaunchAgents` 並 `launchctl bootstrap`**。

**盤中一次性快照篩選（intraday-snapshot）已完成（2026-08-24）**：新增 `scripts/check-intraday-breakout.ts`，把 `calculate-breakout-strength.ts` 的觸發/門檻/評分邏輯提前套用在盤中即時價格上，資料源是 `mis.twse.com.tw` 的社群逆向工程即時報價端點（非官方文件，欄位可能無預警變動）。一次性手動執行，不排程、不接進 `daily-pipeline.ts`。實作重點：批次查詢（每批 120 檔、間隔 1.5 秒節流）、上市/上櫃前綴直接查 `Stock.market` 判斷（不猜測重試）、`elapsedRatio`（開盤到查詢當下的時間比例）用查詢當下時間現算並 clip 到 `[0.05, 1.0]`，用以把累計量換算成預估全天量；下限與上限分別對應「開盤前」（印警告）與「收盤後」（不印警告，此時預估量即為完整全天量）兩種情境，不合併成一種警告。同時新增 MIS 回傳日期與資料庫最新 `DailyQuote` 日期的錯位偵測（僅做「相不相同」二分判斷，因專案無交易日曆表無法正確排除週末）。為避免兩支腳本各自維護一份評分邏輯，把 `GATES`/`WEIGHTS`/`TRIGGER_VOLUME_RATIO` 等常數與六項評分純函式（`computeVolumeStrength`/`computeBreakoutMargin`/`computeFirstBar`/`computeBase`/`computeProximityToHigh`/`computeMarketWideReturns`/`rankScore`/`clip`）、`fetchHistoryWindow` 抽到新檔案 `scripts/breakout-shared.ts`，`calculate-breakout-strength.ts` 同步改為從此檔案 import（原本的內部定義已移除，程式碼行數從 693 行降到 416 行）。結果不寫資料庫，輸出 JSON 快照至 `data/intraday-breakout-snapshots/{timestamp}.json`（檔名含完整時間戳記，同一天可執行多次不覆蓋）。已實測執行成功（1727 檔查詢、45 檔觸發、22 檔通過門檻），輸出分數與統計數字合理遞減。執行：`npx tsx scripts/check-intraday-breakout.ts`（無參數，本質是「現在」的快照，不支援 `--date`）。

**突破評分機制調整：新增 K 棒型態評分、權重重分配、市值門檻調降（2026-08-24）**：依 `docs/PLAN.md`（已改寫，原「盤中一次性快照篩選」計畫已完成並被此新計畫取代）調整 `scripts/breakout-shared.ts` 共用邏輯，供 `calculate-breakout-strength.ts` 與 `check-intraday-breakout.ts` 共用。改動四項：
  1. **新增 `computeCandleShape`**：依當日 K 棒的 open/high/low/close 算「上影線比例」+「收盤位置」型態分數（無上影線且收最高 = 100 分，floor 40 分），並對收黑（close < open）額外封頂 50 分——修正舊版評分只比較收盤價、無法辨識「爆量突破但收黑或留長上影線」賣壓訊號的結構性缺陷。open/high/low 缺值時降級給 50 分並標記 `degraded`；`high === low`（一字線，如鎖漲停無量）視為最強型態給 100 分。
  2. **`WEIGHTS` 重分配**（方案一：保守等比例縮放）：原六項權重乘 0.85 讓出 0.15 給新增的 `candleShape`，七項總和仍為 1.0（`candleShape: 0.15`、`volumeStrength: 0.17`、`breakoutMargin: 0.1275`、`firstBar: 0.17`、`base: 0.17`、`proximityToHigh: 0.1275`、`relativeStrength: 0.085`）。
  3. **`computeBreakoutMargin` 改為鐘型曲線**：乖離 ≤3% 維持原本 40~100 分線性遞增；超過 3% 後改為每多 1% 扣 5 分（下限 60 分），避免舊版「乖離越大分數越高」邏輯獎勵跳空/追高風險。
  4. **`GATES.minMarketCap` 從 50 億調降至 30 億**（`minVolumeShares` 1000 張維持不變）。
  `firstBar` 邏輯刻意維持不動（使用者確認保留「已站上軌 3 天以上仍給 20 分」緩衝，不做剔除）；盤中量能線性外推的既有偏誤問題本次不處理（優先度最低）。`calculate-breakout-strength.ts` 用收盤 `DailyQuote.open/high/low` 計算；`check-intraday-breakout.ts` 用 MIS 即時回傳的 `o`/`h`/`l`（盤中至今最高/最低）計算，兩者皆為 `computeCandleShape` 的合法輸入但語意不同（後者收盤前仍可能變動）。

三大法人歷史回補腳本 `scripts/backfill-institutional-trading.ts`（2026-08-24 建立）：逐支一般股票用 FinMind `TaiwanStockInstitutionalInvestorsBuySell` 回補歷史三大法人買賣超至 `InstitutionalTrading`，寫法比照 `backfill-daily-quotes.ts`（逐支 API 呼叫、`REQUEST_DELAY_MS` 節流、`BACKFILL_LIMIT` 環境變數測試用）。用意是補上 `fill-institutional-trading.ts` 只能抓當天資料的歷史缺口。**首輪（2026-08-26）**：2025-05-02 起 324 個交易日、589,786 筆、1989 檔。**6 年回補（2026-08-27，見下方「兩支回補腳本改為固定起始日」段落）後，2026-08-28 重新查 DB 確認**：`InstitutionalTrading` 涵蓋 **2020-01-02 → 2026-08-28、約 1476 個交易日、2,662,485 筆**（逐年：2020=349,547／2021=376,427／2022=388,570／2023=392,978／2024=429,336／2025=432,544／2026 至 08-28=293,083；每日檔數 2020 約 1808、2025 約 1983，早期較少屬正常）。與 `DailyQuote`（3,066,466 筆）、`TechnicalIndicator`（3,060,066 筆）同起點，accumulation 回測區間可拉滿 6 年。

**盤後選股 v2「投信吃貨訊號」（accumulation-score）已完成（2026-08-27）**：ROADMAP 第 1 階段。新增 `scripts/calculate-accumulation-score.ts` + `scripts/accumulation-shared.ts`，找「還沒出現第一根突破、但籌碼/技術面在醞釀」的股票，與 `calculate-breakout-strength.ts`（找已發生的突破）互補。

設計經過一輪 LLM 檢視後定案，關鍵決策：
  1. **計分改為乘法結構**（非四因子等權加總）：`最終分數 = 籌碼分數 × 技術就緒係數`。籌碼是主因、技術面只放大或抑制它、不能取代——避免「零籌碼但盤整量縮」的股票靠 c/d 兩項就衝進前排。
  2. **籌碼分數 = 投信分數 × 0.7 + 其他法人分數 × 0.3**。投信分數：近 20 日「買超頻率」（淨買超天數佔比，取代脆弱的連續買超天數——投信實務常穿插一天小賣超，嚴格連續會歸零）＋「買超淨額佔發行量比例」（帶正負號），兩子項各半權重、各自 `rankScore`。其他法人分數：**排除投信**，只算 `sum(外資+自營商淨買超) ÷ sum(volume)`（帶正負號、視窗加總而非逐日比例平均）——排除投信是為了從結構上消除與投信分數的雙重計數，不用事後正交化。
  3. **技術就緒係數**：`壓縮度 × 0.5 + 窒息量 × 0.5`（0~100）線性映射到 `READINESS_FLOOR`(=0.5)~1.0。下限 0.5 刻意設計：技術面完全沒收斂只把籌碼分數打對折、不歸零、不從排名抹掉（連續係數取代 cliff 式剔除，符合「不用硬門檻」原則）。壓縮度分數直接沿用 `breakout-shared.ts` 的 `computeBase`（帶寬歷史百分位＋低帶寬持續天數），與突破腳本口徑一致；窒息量用近 5 日 `volume/volumeMa20` 平均量比（多日平均濾單日雜訊）。
  4. **候選池資格篩選**（pool eligibility，非訊號門檻）：剔除「今日 close > 布林上軌」（與 `calculate-breakout-strength.ts` 觸發條件互斥，兩份清單天生不重疊）與「近 20 日均量 < `MIN_AVG_VOLUME_SHARES`(=500 張)」（避免長期無量的死股因「量/均量」比值天生不穩而系統性霸榜）。`sharesOutstanding` 為 null 不剔除，只讓投信分數的佔比子項降級。

所有待校準參數（`CHIP_WEIGHTS`/`TRUST_SUB_WEIGHTS`/`TECH_WEIGHTS`/`READINESS_FLOOR`/`MIN_AVG_VOLUME_SHARES` 等）集中在 `accumulation-shared.ts`。結果不寫資料庫，輸出至 `data/accumulation-score-results/{date}.json`，`code`+`date` 欄位格式對齊突破腳本輸出——方便日後寫比對腳本驗證「冷水區 top-N 之後 N 天內有幾支真的出現在突破結果裡」的預測力。

**兩支回補腳本改為固定起始日 + 6 年回補接力（2026-08-27）**：`backfill-daily-quotes.ts` 與 `backfill-institutional-trading.ts` 原本用「執行當天往前推 N 個日曆天」（480 / 120），分兩天跑時起始日會不一致。改為讀 `BACKFILL_START_DATE` 環境變數（預設 `2020-01-01`），兩支共用同一個值，`endDate` 仍為執行當天。`PROGRESS_INTERVAL` 由 50 降到 25。
  - 事前實測 FinMind 單支查詢 6 年是否被截斷（2330，`2020-08-01`~`2026-08-27`）：`TaiwanStockPrice` 回 1477 筆＝1477 個交易日、`TaiwanStockInstitutionalInvestorsBuySell` 回 7380 筆＝1476 交易日 × 5 法人分類（比數精確吻合 5，對照組 1 年 260 天 × 5 = 1300 亦吻合），確認「單一 `data_id` + 日期區間」查詢無會影響本專案的筆數上限，**不需要加逐年分段查詢**。
  - 1-stock 冒煙測試：`2020-01-01` 起兩支各寫 1616 / 1614 筆，零失敗。
  - 新增 `scripts/backfill-6y.sh`：四步接力（報價 → 三大法人 → `calculate-technical-indicators.ts` → `calculate-industry-heat.ts --backfill 1600`）。技術指標腳本天生對每支股票的全部 `DailyQuote` 逐日重算、產業熱度用 `--backfill 1600` 涵蓋 6 年約 1477 個交易日，兩者都會把整段 6 年算出來。`set -e` 任一步失敗即停。log 寫到 `logs/backfill_6y_{timestamp}.log`（`tee` 同時輸出）。用 `nohup ... &` 背景執行，休眠／闔蓋只暫停 process、開蓋接著跑（`upsert` 冪等，最壞情況重跑也只是重複打已完成股票的 API）。預估總時長約 7~8 小時。
  - **實際執行（2026-08-27 14:36 → 2026-08-28 02:28，約 12 小時，中途一次休眠斷線 + 一次手動 `SIGSTOP` 暫停半小時）**：步驟 1 報價寫入 3,057,682 筆／失敗 1 支（4104，休眠斷線時正在打該支 API，被 `try/catch` 接住記入失敗清單、腳本續跑）；步驟 2 三大法人寫入 2,661,693 筆／失敗 0；步驟 3 技術指標寫入 3,058,014 筆（22 分鐘）；步驟 4 產業熱度從 2020-02-04 起逐日算到最新（2 分鐘）。
  - **4104 事後修補**：`backfill-6y.sh` 跑完後 4104 的 `DailyQuote` 只有 326 筆（三大法人 1616 筆是完整的），單獨用 FinMind 重抓補回 1616 筆。技術指標步驟 3 是在 4104 只有 326 筆報價時算的，所以順帶把 `calculate-technical-indicators.ts` 加了「可選股票代號參數」（`calculateTechnicalIndicators(codes?: string[])`；CLI 可傳 `npx tsx scripts/calculate-technical-indicators.ts 4104` 只重算那幾支），用它把 4104 的指標補回 1616 筆。無參數時行為不變（跑全市場一般股票）。產業熱度單支缺料對等權平均影響極小，不重算。
  - **休眠／暫停實務**：`SIGSTOP`（`pkill -STOP -f "backfill-institutional"`）凍結 process、`SIGCONT` 解凍，適合幾分鐘～半小時的短暫中斷（要關網路做別的事很適合，凍結期間不會發任何請求）；長時間（過夜）不建議用此法，FinMind keep-alive 連線與 Prisma 連線池閒置太久會失效，恢復後至少丟一支、最壞 `upsert` 連線錯誤讓 process exit 1。實測半小時暫停後解凍無失敗行、正常續跑。`nohup` 起的 job 可直接關終端（保險起見先 `disown -a`）。

實測（2026-08-26）：1955 檔一般股票 → 剔除站上上軌 97、低流動性 1131 → 候選池 727 檔全數評分、零降級。手動驗算 rank 1（3706 神達）的 `finalScore = chipScore × readinessCoef`、`chipScore = trustScore×0.7 + otherInstScore×0.3`、`readinessCoef = 0.5 + 0.5×(squeeze×0.5+quiet×0.5)/100` 三條公式皆逐位元吻合。非交易日路徑（`--date=2026-08-23`）正確印「無 DailyQuote，跳過」並 exit 0。`npx tsc --noEmit` 對新檔案無錯（既有的 `prisma.config.ts` 錯誤與本次無關）。**已知問題待校準**：首版前 30 名偏大型股（台積電、華碩、緯創、奇鋐等），主因是「其他法人集中度」子項對大型權值股的外資穩定流入給高百分位。需依實際排名調整 `CHIP_WEIGHTS` / `READINESS_FLOOR` 等常數，此為 ROADMAP 第 1 階段最後一個待辦（其餘子項已打勾）。**獨立手動執行，不進 `daily-pipeline.ts`**。

**daily-pipeline 對外 fetch 加上 retry + timeout（2026-08-28）**：起因是當天 launchd 跑 `daily-pipeline.ts` 在第一步 `[補齊今日報價]` 抓 TWSE `MI_INDEX`（2026-08-27）時 TLS 連線被中途切斷（`terminated` / `cause: read ECONNRESET`），單一暫時性網路錯誤讓整條 pipeline exit 1。手動重跑即成功（報價 2362、籌碼 1858、估值 1968、技術指標 2148 檔、產業熱度 48 個、0 警告），確認非程式 bug。
  - 新增 `scripts/http.ts`（純函式庫），匯出 `fetchJson<T>(url, options?)`：每次嘗試帶 `AbortSignal.timeout`（預設 30s），對「網路層拋錯（`ECONNRESET`/`terminated`/timeout）」與「5xx / 429」自動退避重試（預設 3 次嘗試，間隔 2s→4s），對「其他 4xx」不重試（用私有 `NonRetriableHttpError` 立即往外拋，不落入 catch 重試迴圈）。全部失敗後拋一個帶 `cause` 與 URL 的 `Error`，讓 `daily-pipeline.ts` 的 `PipelineStepError` cause 仍有意義。
  - 改寫 6 個呼叫點：`fill-daily-quotes.ts`（`fetchTwseQuotes` MI_INDEX、`fetchTpexQuotes`）、`fill-institutional-trading.ts`（`fetchTwseInstitutional` T86、`fetchTpexInstitutional`）、`fill-gap-valuation.ts`（`fetchTwseValuations` BWIBBU_d、`fetchTpexValuations` peQryDate）。各檔原本「非 200 拋中文訊息」由 `fetchJson` 統一處理；**回應內容判斷（非交易日 `stat` / `totalCount` / 空陣列 / 日期核對）仍留在各呼叫端**，`fetchJson` 只負責「拿到 2xx 並 `res.json()`」。
  - 範圍刻意只涵蓋 `daily-pipeline.ts` 會呼叫到的三支。`backfill-*.ts`（逐支 loop、單支失敗不中斷、有人在旁邊看）、`check-intraday-breakout.ts`、`fetch-candidate-details.ts`（皆手動、非排程）不接，要用可自行 import。
  - 驗證：`npx tsc --noEmit` 對新檔案與 6 個改動點無錯（既有 `prisma.config.ts` 錯誤與本次無關）。三支腳本 `--date=2026-08-27` 實跑正常寫入、無多餘 retry log。retry 行為用 `timeoutMs: 1` 強制逾時測試，確認會退避重試 2 次（2s→4s，實測用 `baseDelayMs: 100` 壓縮）後才拋最終錯誤，每次重試印一行 `⚠ fetch 第 N 次重試 {url}`。**注意**：TWSE 對不存在的路徑會回 HTTP 200 + HTML 錯誤頁，此時 `res.ok` 為 true，會在 `res.json()` parse 失敗並進入重試——這是可接受的預設（暫時性維護頁重試合理），非 regression。

**回測前置：0050 基準回補 + scripts/ 目錄重整（2026-08-28）**：ROADMAP 第 3 階段（歷史回測）的前置整理。
  - **新增 `backfill-benchmark-quotes.ts`**：回測要用大盤基準算「選股 vs 大盤」超額報酬，但 `Stock` 表的 `TAIEX` 一筆 `DailyQuote` 都沒有、0050 只有最近 pipeline 寫的 9 筆（`backfill-daily-quotes.ts` 只跑 `securityType=stock`，ETF 從沒被回補）。獨立寫一支專補基準標的（`BENCHMARK_CODES = ["0050"]`），FinMind `TaiwanStockPrice` 抓 2020-01-01 起，只寫 `DailyQuote`、不碰 `TechnicalIndicator` / `InstitutionalTrading`（基準只需要收盤價）。已執行：0050 補了 1612 筆（2020-01-02 ~ 2026-08-28），零失敗。已知精度限制：0050 未還原股價，除息日 `close` 含假跌幅；若基準報酬對除息敏感，日後改抓 `TaiwanStockPriceAdj`。
  - **`scripts/` 依用途分子資料夾**：原本 21 個檔案全平鋪在 `scripts/`。重整為 `pipeline/`（6 支，每日自動）、`screening/`（3 支，選股手動）、`backfill/`（4 支，偶爾手動回補）、`lib/`（3 支純函式庫：`http.ts` / `breakout-shared.ts` / `accumulation-shared.ts`）、`archive/`（4 支已停用，不維護）。用 `git mv` 保留歷史；import 路徑：`../generated` → `../../generated`、pipeline 內 `./http.js` → `../lib/http.js`、screening 內 `./breakout-shared.js` → `../lib/breakout-shared.js`；`data/` 輸出路徑 `join(__dirname, "..", "data")` → `join(__dirname, "..", "..", "data")`。`package.json` 的 `"main": "top20-gainers.js"`（單腳本時代遺留、無意義）順手移除。
  - **移到 `archive/` 的 4 支**：`calculate-screen-score.ts`（全市場九因子排序，已被 breakout + accumulation 取代）、`run-screener.ts` + `screener-conditions.json`（選股 v1 條件式篩選，已被 `calculate-breakout-strength.ts` 三層邏輯取代）、`fetch-candidate-details.ts`（讀 v1 輸出做深度抓取，輸入來源已停用）、`top20-gainers.js`（最早的一次性玩具）。**移除**：`backfill-6y.sh`（6 年回補已一次性完成，四步接力不會再跑）。
  - 驗證：`npx tsc --noEmit` 對移動後的檔案零新增錯誤（既有 `prisma.config.ts` 錯誤與本次無關）；`daily-pipeline.ts` + 5 個 step 模組 standalone typecheck 通過（import graph 解析正確）；`calculate-breakout-strength.ts --date=2026-08-27` 實跑，輸出正確落在專案 `data/breakout-strength-results/`（非 `scripts/screening/data/`）。CLAUDE.md 與 README.md 的腳本清單與使用範例已同步改成新路徑。

**前端 scaffolding（Next.js + Prisma）已完成（2026-08-28）**：ROADMAP 第 2 節。依 `docs/PLAN.md` 搭前端骨架，只搭殼、無業務頁面。五個子項全數完成。

- **步驟 0 煙霧測試（切風險）先做**：純 `app/page.tsx`（不碰 Prisma/CSS）+ 最小 `next.config.ts` + `layout.tsx`，`npm run dev` 出現 "ok"、`npm run build` 過。**結論：Next.js 16.3.3（Turbopack）+ React 19 + TS 7（`^7.0.2`）+ Prisma 7 + ESM（`type: "module"`）這組合直接可跑，不需要降 `typescript` 版本、不需要 `--webpack`。** PLAN §4.5 列的「TS 7 × Next 不相容」風險未發生。Next 首次 `dev` 會自動改寫 `tsconfig.json`（加 `moduleResolution: "bundler"`、`resolveJsonModule`、`esModuleInterop`、`plugins: [{name:"next"}]`、`allowJs`/`noEmit`/`incremental`、`include`），`build` 時再強制 `jsx: "react-jsx"`。
- **`tsconfig.json` 收斂**：採 PLAN §4.3 方案 A——不建 `app/tsconfig.json`，一份根 config 同時服務 `scripts/`（tsx 執行，不看 `moduleResolution`）與 `app/`（Turbopack，需 `bundler`）。手動把 `module` 從 `nodenext` 改成 `esnext`（配合 Next 塞的 `moduleResolution: bundler`，兩者不相容會報錯），`lib` 補 `dom`/`dom.iterable`。`npx tsc --noEmit` 對 `scripts/` 零新增錯誤（比對基準：僅 `prisma.config.ts:12` 一個既有錯誤）。
- **`prisma.config.ts:12` 既有錯誤順手修掉**：`url: process.env["DATABASE_URL"]`（`string | undefined` 不符 `exactOptionalPropertyTypes`）本來 `npx tsc --noEmit` 忽略即可，但 `next build` 對整專案跑 typecheck 會把它當致命錯誤擋住 build。改成 `?? ""`（語意等價——`DATABASE_URL` 沒設本來連線就會失敗；此為 Prisma 官方新版模板寫法）。使用者確認採此方案。
- **`generated/prisma` 的 `importFileExtension = ""`**（PLAN §3/§4.5 預留的坑）：generated client 內部用 `import * as $Enums from "./enums.js"` 這種 nodenext 風格 specifier，Turbopack 打包時不會把 `.js` 映射到實際的 `.ts` 檔 → `Module not found: Can't resolve './enums.js'`。`serverExternalPackages` 只吃套件名、不吃 `generated/prisma` 這種本地路徑，無法用它繞過。解法：`schema.prisma` 的 `generator client` 加 `importFileExtension = ""` 讓內部 import 不帶副檔名，tsx（nodenext，容忍 extensionless）與 Turbopack（bundler）都能解析，`npx prisma generate` 重生。連帶：本專案 import client 一律寫 `from "../generated/prisma/client"`（不帶 `.js`），舊腳本殘留的 `.js` 仍能跑但不一致。tsx 冒煙測試（`import { PrismaClient }`）與全市場 `calculate-breakout-strength.ts --date=2026-08-27` 實跑確認 scripts 未受影響。
- **目錄**：repo 根目錄 `app/`（PLAN §2，不開 `web/`、不做 monorepo）。新增 `app/`（`layout.tsx` 側邊欄殼 + `page.tsx` dashboard + `globals.css` + `globals.css.d.ts`）、`lib/`（`prisma.ts` 單例 + `actions/health.ts` + `actions/poc.ts`）、`components/`（`ui/Card.tsx` + `ChartSmoke.tsx` + `PocRunner.tsx`）、`scripts/_poc/long-task.ts`。
- **Prisma 單例**（`lib/prisma.ts`）：`globalThis` 快取 + `PrismaPg` adapter，檔頭 `import "server-only"`（裝了 `server-only` 套件，<1KB，是唯一能在 build 時擋「client component 誤 import」的機制）。hot reload 連線數測試：連續 touch `app/page.tsx` 觸發 5 次 reload，`pg_stat_activity` 連線數維持個位數不持續增長（每次 `tsx` 檢查腳本本身也會開 1~2 條瞬時連線，非 pool 洩漏）。
- **Server Actions**（PLAN §5，不建 REST/GraphQL）：`lib/actions/health.ts` 的 `getDbHealth()` `Promise.all` 三個 count/findFirst，回傳可序列化 plain object（`Date` 在邊界轉 `YYYY-MM-DD` 字串）。首頁 Server Component 直接 `await`，渲染出 **DailyQuote 3,066,466 / Stock 2,799 / 最新報價 2026-08-28**，與 `npx tsx` 直查 DB 完全一致（核心驗收）。
- **`app/page.tsx` 加 `export const dynamic = "force-dynamic";`**：否則 `next build` 靜態預渲染此頁會在 build time 連本機 Postgres。加了之後 `next build` 成功，`/` 標為 `ƒ (Dynamic)`、build 期間零 DB 連線、零型別錯誤。
- **`import "./globals.css"` × `noUncheckedSideEffectImports`**（PLAN §4.5、§8）：採方案 ①，新增 `app/globals.css.d.ts` 內容 `declare module "*.css";`。不關該旗標。
- **圖表庫定案 Recharts 3**（PLAN §7）：`components/ChartSmoke.tsx`（`"use client"`）畫寫死資料的小折線圖。HTML 內確認 recharts client chunk（`node_modules_recharts_es6_*`）有被載入、`ChartSmoke` client boundary 有 SSR 出來；圖本身因 `ResponsiveContainer` 需要瀏覽器測量 DOM，在 hydration 後才畫出（初始 HTML 沒有 SVG 屬正常）。
- **樣式/版面定案 Tailwind CSS v4**（PLAN §8）：手動裝 `tailwindcss @tailwindcss/postcss postcss` + `postcss.config.mjs` + `app/globals.css` 的 `@import "tailwindcss";`。元件先不引入元件庫，手刻 `components/ui/Card.tsx`（`Card` + `Stat`）。導覽：`app/layout.tsx` 極簡側邊欄，三個佔位連結（Dashboard / Backtest / Screening，後兩個路由還不存在）。固定淺色主題，不做深淺切換。
- **背景任務機制定案**（PLAN §9，只做 PoC）：「Server Action `spawn` detached 子進程 → 子進程每步覆寫 `data/_poc/progress.json` → Server Action 輪詢讀檔 → client `setInterval` poll」。`lib/actions/poc.ts` 的 `startPocTask()` 用 `spawn(process.execPath, [<絕對路徑>/node_modules/tsx/dist/cli.mjs, scripts/_poc/long-task.ts], { detached: true, stdio: "ignore" })` + `child.unref()`（**不用 `npx tsx`**，沿用 `daily_pipeline.plist` 的教訓）。`scripts/_poc/long-task.ts` 跑 10 秒假迴圈每秒寫進度。實測（直接模擬 spawn）：progress.json 從 `current:0` 走到 `current:10 status:"done"`，exit 0。`components/PocRunner.tsx` 按鈕 + 進度條放首頁一角。**`_poc` 整組（`scripts/_poc/` + `lib/actions/poc.ts` + `components/PocRunner.tsx`）在下一份回測 PLAN 開始時刪除，換成真的 Layer 0 runner。** `data/_poc/` 已加進 `.gitignore`。
- **`.gitignore`** 追加 `/.next/`、`/next-env.d.ts`、`/out/`、`/data/_poc/`。**`.env.example`** 新增（列 `DATABASE_URL` + `FINMIND_API_KEY`）。
- **`package.json`** `scripts` 加 `dev` / `build` / `start`，移除無意義的 `test`。新增 deps：`next@16.3.3`、`react@19` / `react-dom@19`、`recharts@3`、`server-only`；devDeps：`@types/react*`、`tailwindcss@4` / `@tailwindcss/postcss` / `postcss`。
- **驗收清單（PLAN §11）逐項過**：步驟 0 煙霧測試 ✓、首頁真實 DB 數字與直查一致 ✓、hot reload 連線數不增長 ✓、`npm run build` 成功無型別錯誤 ✓、`globals.css` 不再報 side-effect 錯 ✓、`tsc --noEmit` 對 `scripts/` 無新增錯誤 ✓、Recharts smoke 圖 SSR+hydration 無 error ✓、背景任務 PoC 進度 0→10 ✓、`calculate-breakout-strength.ts` 既有行為未變 ✓、四份文件同步 ✓。
- **已知 TODO（留給後續）**：`scripts/screening/` 兩支的頂層 `new PrismaClient()`（會多開一個不受 `lib/prisma.ts` 單例管理的連線池）本 PLAN 未動——留給 ROADMAP 3.1「腳本參數化」連同 `check-intraday-breakout.ts` 無匯出一起重構成 `calculateX(date, { prisma, config })` 注入式。因此 health action 只碰 Prisma、不 import 選股函式（不是會爆，是沒必要在 scaffolding 階段引入多開連線池的雜訊）。`_poc` 檔案待刪（見上）。

**套件管理器從 npm 換成 pnpm（2026-08-28）**：使用者要求。步驟：`.npmrc` 加 `node-linker=hoisted`（扁平 `node_modules`，保 `generated/prisma` client / tsx 腳本 / 未來 plist 的絕對路徑照舊解析，代價是放棄 pnpm 嚴格 phantom-dependency 檢查）→ `package.json` 加 `packageManager: "pnpm@8.15.4"`（corepack 鎖版）+ `engines.node: ">=22"` → `pnpm import`（`package-lock.json` → `pnpm-lock.yaml`）→ `rm -rf node_modules package-lock.json` + `pnpm install`（221 套件、4.3 秒、Prisma/esbuild postinstall 正常執行）。驗證全過：`node_modules/tsx/dist/cli.mjs` 仍在扁平路徑（plist 模式不受影響）、`pnpm prisma generate` OK、`pnpm tsx scripts/screening/calculate-breakout-strength.ts --date=2026-08-27` 實跑輸出正確、`pnpm exec tsc --noEmit` EXIT 0、`pnpm build` + `pnpm dev` 首頁 DB 數字照舊（3,066,466 / 2,799 / 2026-08-28）。CLAUDE.md（開發慣例加「套件管理器是 pnpm」段 + 全文 `npx`→`pnpm`）、README.md（環境需求加 pnpm、使用方式加 `pnpm install`、指令全改）已同步。本 PROGRESS 舊段落的 `npx tsx ...` 指令保留原樣（當時屬實，是歷史紀錄）。pnpm 8.15.4 提示可升 11.x，暫不升。

尚未開始/明確不做：估值歷史回補（`fill-gap-valuation.ts` 已支援 `--date` 隨時可補，但依計畫不主動回補）、heatScore 欄位與市值加權熱度（第一版等權即可，分數用時現算）、股本更新排程（月頻手動跑）、新聞情緒分析（`NewsArticle.sentiment`/`sentimentScore` 欄位已存在但尚未有腳本填值）、Tag/StockTag 篩選邏輯（`topic_alignment` 因子固定中性分）、WatchlistItem 操作介面、AnalysisResult 產出流程（Phase D）、`screening/` 三支（`calculate-breakout-strength.ts` / `calculate-accumulation-score.ts` / `check-intraday-breakout.ts`）與 `backfill/` 各支皆為獨立手動執行（不在 `daily-pipeline.ts` 內）、`calculate-accumulation-score.ts` 的參數校準（首版未校準，前段偏大型股，併入回測階段做）、前端業務頁面（回測 UI / 選股 / 觀察清單頁全留給後續 PLAN；scaffolding 只搭殼）、`scripts/screening/` 兩支的頂層 `new PrismaClient()` 注入式重構（ROADMAP 3.1）、`_poc` 背景任務檔案（回測 PLAN 開始時刪）。`archive/` 的 `calculate-screen-score.ts` / `run-screener.ts` / `fetch-candidate-details.ts` / `top20-gainers.js` 已停用不維護。

（每次進度更新，麻煩幫我一併更新這個區塊。）
