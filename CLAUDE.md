# Stock Agent

## 專案目的

台股觀察/分析 agent 的後端資料層。抓取證交所（TWSE）與櫃買中心（TPEx）公開資料與 FinMind API，存進 PostgreSQL，之後透過 Claude Code 互動式地做篩選、族群分析、新聞整合等工作。

## 資料表結構總覽

- **Sector**：正式產業別，一支股票對應一個（`Stock.sectorId`）。
- **Stock**：股票基本資料（代號、名稱、市場、證券類型、所屬產業別）。是大部分其他表的關聯核心。另有 `delistedAt DateTime?` 下市標記（null = 存續；值 = 最後行情日，從無行情者為 `2020-01-01` sentinel）——由 `scripts/backfill/mark-delisted.ts` 手動低頻維護（雙向：標記 + 復牌自動清除），消費者是 `getDbHealth` 覆蓋率分母、兩支 FinMind backfill 的股票清單、`run-signal-scan.ts` realtime 的 MIS 抓取清單；**一律用標記不刪資料**（歷史行情參與過全市場計算、seed 重跑會復活）。
- **Tag**：概念題材標籤，會動態變動，與 Stock 多對多。
- **StockTag**：Stock 與 Tag 的多對多關聯表。
- **DailyQuote**：每日報價快取（開高低收、量、漲跌），依 `stockCode + date` 唯一。
- **NewsArticle**：新聞文章（標題、連結、來源、發布時間、情緒分析結果）。
- **NewsStock**：NewsArticle 與 Stock 的多對多關聯表。
- **WatchlistItem**：手動維護的正式觀察名單，一支股票最多一筆。除 `stockCode`/`addedAt`/`notes` 外，另有買入狀態欄位：`isPurchased`（Boolean，預設 false）、`buyPrice`/`targetPrice`/`stopLossPrice`（`Decimal(10,2)`，nullable）、`buyDate`（`@db.Date`，nullable）；以及 `userStage`（`enum SignalStage { setup breakoutDay extended }`，nullable，PLAN 6）——使用者手動指定的階段分類，`addToWatchlist` 加入時寫入當下 `autoStage`，之後可在 `/watchlist` 卡片手動改。既有（PLAN 6 之前加入）資料為 null，消費端一律 `userStage ?? autoStage` fallback。**舊 `source` 欄位（`"breakout"`/`"accumulation"`/`"manual"`）已於 PLAN 6 移除**（只有寫入零讀取，`userStage` 是上位替代）。
- **AnalysisResult**：AI 產出的分析結果歷史紀錄（screener / watchlist_summary / sector_ranking 等）。
- **StockValuation**：個股每日估值（本益比/股價淨值比/殖利率/收盤價），依 `stockCode + date` 唯一。TPEx 來源沒有收盤價（`closePrice` 為 null）；虧損公司 `peRatio` 為 null。
- **IndustryHeatSnapshot**：產業熱度每日快照（等權平均漲跌幅、漲跌家數、當日排名），依 `sectorId + date` 唯一。**不存 heatScore**——熱度分數要用時從原始欄位現算，避免公式調整後需要重刷歷史。
- **MarginTrading**：個股信用交易餘額（融資、融券），來源 TWSE `MI_MARGN` / TPEx `margin/balance`，依 `stockCode + date` 唯一。**單位一律「股」**（原始資料源以「張」計，入庫 ×1000）。`marginBalance`/`marginBalancePrev`/`shortBalance`/`shortBalancePrev` non-null（沒信用交易的股票是 `0` 不是缺值）；`marginQuota`（融資限額）/`offsetting`（資券互抵）nullable。`*Prev`（前日餘額）直接取 API 回應的「前日餘額」欄，不自己 join。**不存 change（今日−前日）**（同 `heatScore` 理由，要用時現算）；**不存「融資金額（仟元）」**（那是全市場彙總列的東西，個股要金額用 `marginBalance × 當日 close` 現算）。涵蓋一般股票 + 特別股。**下游**：`run-signal-scan.ts` 的 `margin-chasing` 警示（`computeMarginSurgePercentile`（融資餘額近 5 日累積變化率 vs 自己 40 天歷史百分位）→ `computeInstitutionalFlow` 回 `marginChasing` → `SignalResult.warnings`）。**只在 `breakoutDay`/`extended` 階段、且突破當日三大法人淨賣超時觸發，只標記不動分數**。
- **Stock 的股本欄位**：`sharesOutstanding`（已發行普通股數）與 `sharesOutstandingUpdatedAt`。市值不落地存欄位，要用時以 `sharesOutstanding × 當日收盤價` 現算。
- **`SecurityType.index` / `TAIEX` 這筆特殊 `Stock`**：`SecurityType` enum 有 `index` 值，目前唯一一筆是 `Stock{ code: "TAIEX", name: "發行量加權股價指數", market: "TWSE", securityType: "index", sectorId: null }`（由 `backfill-index-quotes.ts` 的 `ensureIndexStock` upsert 自建，不在 seed 清單裡）。**只給大盤濾網（`market-regime.ts`）算 MA60 / 帶寬用，不是個股、不進任何選股候選池**——`run-signal-scan.ts` / `getDbHealth` 等都篩 `securityType="stock"` 天然排除它；唯一例外是 `calculate-technical-indicators.ts` 的 `where` 用 `OR: [{ securityType: "stock" }, { code: "TAIEX" }]` 特意把它納入。`DailyQuote.volume` 存的是 FinMind 回的全市場成交量（對 TAIEX 無意義但無害）。**當日 TAIEX 行情由 `daily-pipeline.ts` 第 1.5 步（`fillTodayIndex`，FinMind）補**，歷史缺漏用 `backfill-index-quotes.ts` 全區間回補——兩者共用同一組 upsert helper，都寫完整 OHLC。
- **數量單位一律「股」**：`DailyQuote.volume`、`Stock.sharesOutstanding`、`InstitutionalTrading.*NetBuy`、`TechnicalIndicator.volumeMa20`，以及日後任何「數量」欄位，統一存「股」。原始資料源若以「張」計（1 張 = 1000 股，如融資融券的 TWSE `MI_MARGN` / TPEx `margin/balance`），入庫時 ×1000 轉「股」，並在該 model 的欄位註解標明「原始為張，入庫 ×1000」。這樣跨欄位計算（如「融資餘額 ÷ volumeMa20」）不需再換算。

## 資料來源與限制

- **FinMind API**：未註冊 300 次/小時，註冊後 600 次/小時；大部分 dataset 需要指定 `stock_id`（例外：`TaiwanStockInfo` 可一次拿全部股票清單）。`TaiwanStockNews` 只有標題/連結/簡短描述，沒有全文。`TaiwanStockInfo` 的 `type` 欄位有 `twse`、`tpex`、`emerging`（興櫃）三種值，目前 `Market` enum 只涵蓋 `TWSE`/`TPEx`，seed 時會跳過 `emerging`。
- **證交所 OpenAPI**（`openapi.twse.com.tw`）：免註冊免金鑰，一次可拿全上市市場當日資料（`STOCK_DAY_ALL`），官方未公布明確 rate limit，自行節流即可。
- **櫃買中心 OpenAPI**（`www.tpex.org.tw/openapi`）：同上，但欄位命名跟證交所不同，且回傳資料混合了股票/ETF/權證/可轉債，需要過濾。
- **證交所舊版報表 API `MI_INDEX`**（`www.twse.com.tw/exchangeReport/MI_INDEX`）：可查「指定單一天」的全上市市場資料（`STOCK_DAY_ALL` 只給當日），已於 2026-08-17 實測驗證可用。用法重點：
  - `date` 參數直接傳西元 `YYYYMMDD` 即可（如 `20260814`），API 內部會自動轉換民國年，不需要自己轉換。
  - `response=json&type=ALL` 即可直接拿到 JSON，不需要 fallback 成 HTML 解析。
  - 回應是 `tables` 陣列，混雜指數/大盤統計/個股明細等 10 個表格，**個股明細在 index 8**（標題「每日收盤行情(全部)」），欄位為 `[證券代號, 證券名稱, 成交股數, 成交筆數, 成交金額, 開盤價, 最高價, 最低價, 收盤價, 漲跌(+/-), 漲跌價差, ...]`；`漲跌(+/-)` 欄位是含 HTML 顏色標記的字串（`color:red`=漲、`color:green`=跌），要另外解析正負號。
  - 非交易日（週末/假日）回應不含 `tables` 欄位，只有 `{"stat": "很抱歉，沒有符合條件的資料!"}`，可用這個判斷跳過。
  - 這支 API 的「全部」個股明細包含股票/ETF/權證/可轉債等所有證券類型（權證數量可達上萬筆），不是只有一般股票，寫入 `Stock` 前要套用過濾規則分類。
- **過濾規則**（單一出處 `scripts/lib/security-type.ts` 的 `toSecurityType`，三支 pipeline 抓取腳本共用）：ETF 代號開頭為 `00`；一般股票為恰好 4 碼數字；特別股代號為 4 碼 + 1 碼英文字母（共 5 碼）；權證代號為 6 碼數字，或名稱含「購」「售」（fallback，攔 put 權證等）；可轉債（bond）代號為 5 碼數字；其餘歸類為 other。**判斷順序是刻意的：「4 碼數字 → stock」必須排在名稱購/售判斷之前**——存在名稱帶「購」「售」的一般股票（2945 三商家購、3085 新零售），名稱判斷排前面會誤殺（2026-09-01 修正的 bug，曾讓這兩檔被三支抓取腳本每天跳過）；權證代號一律 6 碼，此順序無誤放風險（以 8/31 全市場 44,454 筆實資料驗證過）。
- **權證（warrant）與可轉債（bond）不存進資料庫**：這個專案的分析目標不涉及衍生性金融商品。`fill-daily-quotes.ts` 的 `writeRows` 分類完 `securityType` 後，若為 `warrant` 或 `bond` 會直接跳過（不建立 Stock、不寫入 DailyQuote），並在統計輸出裡回報跳過筆數。2026-08-17 已一次性清理過資料庫裡既有的 18,041 筆 warrant/bond Stock 記錄與對應的 29,382 筆 DailyQuote。
- **TPEx OpenAPI 個股行情端點不支援歷史日期查詢**：`tpex_mainboard_daily_close_quotes`、`tpex_mainboard_quotes`、`tpex_delayed_stock_close` 等端點的 swagger 定義 `parameters` 都是空陣列，2026-08-17 實測不論傳什麼查詢參數，回應的 `Date` 欄位永遠是「目前最新一天」，無法像證交所 `MI_INDEX` 一樣指定任意歷史日期。因此上櫃股票的「補歷史缺漏」目前無解，只能補當天（`fill-daily-quotes.ts` 的 `fillTodayTpex`）或靠 FinMind 逐支回補（`backfill-daily-quotes.ts`）。
- **`STOCK_DAY_ALL` 有發布延遲，不適合當作「今天資料是否已可取得」的判斷依據**：2026-08-17 實測，`MI_INDEX` 對當天日期已回應 `stat: OK`（資料齊備）的同一時間點，`STOCK_DAY_ALL` 回傳的 `Date` 仍是前一個交易日，代表兩支 API 的資料更新時間點不同步。因此 `fill-daily-quotes.ts` 的 TWSE 部分固定用 `MI_INDEX`（可指定日期、資料較即時），沒有改用 `STOCK_DAY_ALL`。
- **TPEx `tpex_mainboard_daily_close_quotes` 回應細節**：`Date` 欄位是民國年（如 `1150817`，需 `+1911` 轉西元），欄位名稱為 `SecuritiesCompanyCode`/`CompanyName`/`Close`/`Open`/`High`/`Low`/`TradingShares`/`Change`（`Change` 直接是帶正負號的數字字串，不像 `MI_INDEX` 用 HTML 顏色標記）。回應同樣混雜股票/ETF/權證/可轉債，需套用同一套過濾規則。
- **TWSE 估值 API `BWIBBU_d`**（`www.twse.com.tw/exchangeReport/BWIBBU_d?response=json&date=YYYYMMDD&selectType=ALL`）：全上市普通股的本益比/殖利率/股價淨值比，可查任意歷史日期（2026-08-18 實測，回應 `date` 欄位直接是西元 `YYYYMMDD` 可拿來核對）。欄位順序 `[證券代號, 證券名稱, 收盤價, 殖利率(%), 股利年度, 本益比, 股價淨值比, 財報年/季]`；本益比缺值（虧損公司）為 `"-"`；數字可能含千分位逗號。非交易日 `stat` 不是 `"OK"`。回傳約 1080 筆，比 `Stock` 表的 TWSE 普通股（約 1220 檔）少約 12%——估值端點僅涵蓋有估值資料的普通股，此差異屬正常。
- **TPEx 估值端點支援歷史日期查詢**（與行情端點不同！）：`www.tpex.org.tw/www/zh-tw/afterTrading/peQryDate?date=YYYY/MM/DD&id=&response=json`，`date` 收「西元」年斜線格式（如 `2026/06/01`），2026-08-18 實測可查任意歷史日期。回應在 `tables[0]`，`date` 欄位是民國年斜線格式（`115/06/01`）要 `+1911` 核對；欄位順序 `[股票代號, 公司名稱, 本益比, 每股股利, 股利年度, 殖利率(%), 股價淨值比, 財報年/季]`（**與 TWSE 不同且沒有收盤價**），公司名稱尾端帶補位空白要 trim。非交易日 `totalCount` 為 0。OpenAPI 版 `tpex_mainboard_peratio_analysis` 只給最新一天，不用。
- **TWSE 融資融券 API `MI_MARGN`**（`www.twse.com.tw/rwd/zh/marginTrading/MI_MARGN?response=json&date=YYYYMMDD&selectType=ALL`）：全上市個股信用交易餘額，`date` 傳西元 `YYYYMMDD`，**支援任意歷史日期**（2026-08-31 實測）。回應 `{ stat, date, tables }`，`stat === "OK"` 且有 `tables` 才是交易日；非交易日 `stat` 為「很抱歉，沒有符合條件的資料!」、無 `tables`。`body.date` 回西元 `YYYYMMDD` 可核對。**個股明細在 `tables[1]`**（`tables[0]` 是全市場彙總「信用交易統計」）；`tables[1].fields` 16 欄，重點欄位 index：`[0]代號 [1]名稱 [5]融資前日餘額 [6]融資今日餘額 [7]融資次一營業日限額 [11]融券前日餘額 [12]融券今日餘額 [14]資券互抵`。數字含千分位逗號，單位是**張**（入庫 ×1000）。`tables[1].data` 混 ETF（`00` 開頭，實測有 `00400A` 主動式 ETF）/ 特別股 / 一般股票，套過濾規則。
- **TPEx 融資融券 API `margin/balance`**（`www.tpex.org.tw/www/zh-tw/margin/balance?date=YYYY/MM/DD&id=&response=json`）：`date` 收西元斜線格式，**支援任意歷史日期**（與 TPEx 行情端點不同，比照估值端點）。回應 `{ date, tables: [{ date: "115/08/28", totalCount, fields, data }] }`，`tables[0].date` 是民國年斜線格式要 `+1911` 核對；非交易日 `totalCount` 為 0 / `data` 空。`tables[0].fields` 20 欄，重點欄位 index：`[0]代號 [1]名稱 [2]前資餘額(張) [6]資餘額 [9]資限額 [10]前券餘額(張) [14]券餘額 [18]資券相抵(張)`。數字含千分位逗號，單位是**張**（入庫 ×1000）。`data` 同樣混 ETF（`00679B` 債券 ETF 等）/ 一般股票，套過濾規則。
- **MOPS 股本 CSV**（`mopsfin.twse.com.tw/opendata/t187ap03_L.csv` 上市 / `t187ap03_O.csv` 上櫃）：UTF-8 含 BOM，最後一欄「已發行普通股數或TDR原股發行股數」直接就是股數。**絕對不要用「實收資本額 ÷ 面額」推算**——面額不是每家都 10 元（實測案例：國巨 2327 面額 2.5 元）。CSV 只涵蓋現存上市/上櫃公司，`Stock` 表裡的已下市/KY 等標的不在其中（約 TWSE 135 檔、TPEx 34 檔），跳過不動原值。

## 開發慣例

- **分支只是習慣、不追求 PR**：功能寫好、驗證過，就可以直接 merge 進 `main`（`git merge` 即可，不需要開 PR、不需要 code review 流程）。「提交並合併」= commit + merge to main。
- **每個新任務開始前先 `git branch --show-current` 確認分支**：
  - **一律從 `main` 開新分支——不要在既有 feature 分支上「疊」新分支**。開新分支前先確認**目前不在某個未合併的 feature 分支上**：若在，代表上一份工作還沒收尾，先照下面規則處理，不要 `git checkout -b` 從當前 HEAD 直接切（2026-09-04 事故：PLAN 8 從 PLAN 6/7 未合併的分支頂端切出，導致「已完成」與「審查中」的工作混在同一條分支、`main` 落後兩份 PLAN）。
  - 分支名跟新任務主題**明顯不符**（例如在 `feat/watchlist-flow-4` 上要開始做「盤中提醒」），或**當前分支還有未合併的完成工作** → 停下來問使用者要哪一種，得到答覆才動手：
    (B) 先把當前分支 merge 回 `main`（`git checkout main && git merge --ff-only <branch>`），再從 `main` 開新分支 ← **預設建議這個**；
    (C) 就在當前分支繼續改（僅當新任務其實是同一主題的延續）；
    (A) 從當前分支直接切新分支（新工作疊在現有未合併的工作上）← **僅在使用者明確要求時才用**，且要跟使用者講清楚「這樣兩份工作會綁在同一條分支、要一起 merge」。
  - 分支名還算相符、當前分支無未合併完成工作、或使用者已說「就在這改」→ 直接做，不用每次問。
  - **merge 時機是使用者才能下的決定**——不要在使用者沒說的情況下自己 merge；但使用者一說「合併 / merge / 提交並合併」就直接做，不用再追問「PR 還是 merge」。
  - **merge 後順手刪分支**：`git merge --ff-only <branch>` 進 `main` 後，`git branch -d <branch>` 把已合併的分支標籤清掉（`-d` 小寫安全刪，有獨立 commit 會拒刪）。不要留一堆「已合併但沒刪」的殘枝（2026-09-04 曾累積 11 個）。唯一例外 `feat/backtest-ui-3.6-3.7`（回測凍結，刻意保留供參考，見「回測系統 — 已放棄」段）。
  - **在 `main` 上絕不直接改**——一定先開 feature 分支（既有規範，這裡重申）。
- Prisma model 單數命名（`Stock` 不是 `Stocks`），欄位用 camelCase（`stockCode` 不是 `stock_code`）。
- **套件管理器是 pnpm**（2026-08-28 從 npm 轉換）。`package.json` 的 `packageManager` 欄位鎖 `pnpm@8.15.4`（corepack）。`.npmrc` 設 `node-linker=hoisted`——扁平 `node_modules` 佈局，讓 `generated/prisma` client、tsx 腳本、plist 的絕對路徑都能照舊解析，代價是放棄 pnpm 的嚴格 phantom-dependency 檢查。指令一律 `pnpm ...`（`pnpm dev` / `pnpm tsx scripts/...` / `pnpm prisma ...` / `pnpm exec tsc --noEmit`）。lockfile 是 `pnpm-lock.yaml`，`package-lock.json` 已刪。
- 新增/更新基礎資料（股票清單、產業別）用 `pnpm prisma db seed`（會執行 `prisma/seed.ts`），不要每次手寫新腳本。
- `.env` 的資料庫連線變數名稱是 Prisma 預設的 `DATABASE_URL`。
- **Prisma 版本為 7.9.1**（比原始規劃文件 `docs/PLAN.md` 假設的版本新），與舊版 Prisma 有以下差異，之後新增功能時要注意：
  - 連線字串不寫在 `schema.prisma` 的 `datasource.url`，而是在 `prisma.config.ts` 的 `datasource.url`（仍然讀取 `DATABASE_URL`）。
  - `PrismaClient` 需要搭配 driver adapter 初始化，本專案用 `@prisma/adapter-pg`：
    ```ts
    import { PrismaPg } from "@prisma/adapter-pg";
    import { PrismaClient } from "../generated/prisma/client";
    const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL });
    const prisma = new PrismaClient({ adapter });
    ```
  - Prisma Client 產出位置是 `generated/prisma`（非預設的 `node_modules/@prisma/client`），且是 ESM-only（用了 `import.meta.url`），因此整個專案 `package.json` 設定為 `"type": "module"`。
  - **`schema.prisma` 的 `generator client` 設 `importFileExtension = ""`**（2026-08-28 加）：讓 generated client 內部的相對 import 不帶副檔名，這樣 tsx（`scripts/`，nodenext）與 Turbopack（`app/`，bundler resolution）都能解析。原本預設帶 `.js` 會讓 Turbopack 找不到 `./enums.ts` 等檔。**因此 import client 時也不要帶 `.js`**（`from "../generated/prisma/client"`，舊腳本裡殘留的 `.js` 仍能跑但不一致）。改 schema 後要 `pnpm prisma generate`。
  - TS 執行工具用 **tsx**，不是 `ts-node`（`ts-node` 與剛發布的 TypeScript 7 新架構不相容）。
  - Prisma seed 指令設定在 `prisma.config.ts` 的 `migrations.seed`，不是 `package.json` 的 `"prisma"` 欄位（後者在 Prisma 7 已不生效）。
  - `prisma.config.ts` 的 `datasource.url` 寫 `process.env["DATABASE_URL"] ?? ""`（`?? ""` 是為了滿足 `exactOptionalPropertyTypes`，否則 `next build` 的整專案 typecheck 會失敗）。

## 前端（Next.js + Prisma）

2026-08-28 建立的前端骨架（ROADMAP 第 2 節）。2026-08-30 加上首批業務頁面（ROADMAP 第 4 節：選股 → 挑股 → 觀察清單）。2026-08-31 整站切固定深色 + 重寫 Dashboard。

- **技術棧**：Next.js 16.3.3（App Router，Turbopack）+ React 19 + Tailwind CSS v4 + Recharts 3（目前僅 `package.json` 保留、Dashboard 已不用）+ `clsx` / `tailwind-merge`（`lib/cn.ts` 的 `cn()`）+ `class-variance-authority`（`Button.tsx` 的 `cva`）。TS 7 / Prisma 7 / ESM 這組合實測直接可跑，沒有降版或改 webpack。
- **全站固定深色（無 light/dark toggle）**：`app/globals.css` `:root { color-scheme: dark }` + `body` `@apply bg-background text-foreground`。配色 = `app/globals.css` 的 **角色化 semantic color token 單一出處**（比照 `../quick-talk` 的 shadcn 模式，但不引入 shadcn；值為 HEX，方便 VS Code 原生色盤微調；`:root` 用不帶前綴的名字 `--background` `--muted-foreground` …，`@theme inline` 接成 Tailwind color utility `--color-*` → 生成 `bg-background` / `bg-card` / `bg-muted` / `text-foreground` / `text-muted-foreground` / `border-border` / `border-input` / `bg-primary` / `text-up` / `text-warning` …）。**token 代表「用途 / 角色」不是「色階深淺」**——要更淡的字用透明度修飾（`text-foreground/80`、`text-muted-foreground/70`、`text-muted-foreground/50`）在 base 角色上打折，不預先定義一排灰。共 14 個 token：
  - **背景/前景（成對，每個底配 -foreground）**：`--background`（頁底）/ `--foreground`（主字）/ `--card` `--card-foreground`（卡片、側欄）/ `--muted` `--muted-foreground`（弱化區塊底 / badge 底 / 表頭；弱化的字 / label）/ `--border`（分隔線）/ `--input`（輸入框邊框）/ `--ring`（focus 環）。
  - **主色（成對）**：`--primary` / `--primary-foreground`（主按鈕、選中列、tab 底線）。primary 按鈕 hover = `bg-primary/90`。
  - **台股漲跌（專屬，獨立）**：`--up`（漲紅）/ `--down`（跌綠），深色底提亮一階。
  - **狀態語意（各一個 base，字/邊框/底用透明度變體 `/10` `/30` …）**：`--destructive`（錯誤 / 風險 /「追」badge / danger 按鈕 / 號誌偏空）、`--warning`（估 / 提示框 / 覆蓋率黃 / 號誌中性）、`--success`（燈號已更新 / 完成 / 高分 ≥70 / 號誌偏多）。提示框統一寫 `border-X/30 bg-X/10 text-X`。**大盤號誌燈直接複用這三個**（`RegimeBanner` 的 `DOT_CLASS`）；台股漲跌色語意不同、不複用。
  - **元件不直接寫 `slate-*` / `blue-*` / `rose-*` 等色階數字**（grep `(text|bg|border)-(slate|blue|rose|emerald|amber)-[0-9]` 對 `app/` `components/` 應回空）。唯一例外 = `components/ui/Sparkline.tsx` 的 SVG `stroke` 硬編 HEX 常數 `SPARK_UP`(#fb7185) / `SPARK_DOWN`(#34d399) / `SPARK_BASELINE`(#475569)，Server Component 不能 `getComputedStyle`，改色需同步 globals.css。
- **`Button.tsx` 用 `cva`**：`variant` 維度（`primary` / `secondary` / `danger`），`primary` = `bg-primary text-primary-foreground hover:bg-primary/90`、`secondary` = `border border-border bg-muted text-foreground/80 hover:bg-muted/70`、`danger` = `bg-destructive/10 text-destructive hover:bg-destructive/20`（quick-talk `destructive` 標準：淡底無邊框）。三者 disabled 一律 `disabled:opacity-50`。要加 `size` 維度再擴 cva。`cn()`（`lib/cn.ts` = `twMerge(clsx(...))`）用於條件拼 class、讓外部傳入的 `className` 能覆蓋元件內建同類 class。日後要主題切換另開 PLAN（`:root` 只一份 dark 值）。
- **目錄**：前端在 repo 根目錄 `app/`（**不開 `web/` 子目錄、不做 monorepo**）——前端要 import 的 `generated/prisma`、`scripts/screening/*`、`scripts/lib/*` 都在根目錄，開子目錄只會讓 import path 變複雜。
- **`lib/`（根目錄，新）vs `scripts/lib/`（既有）**：`lib/` 放 **Next/React 世界**的共用碼（Prisma 單例、Server Actions、格式化 helper）；`scripts/lib/` 維持是**純 Node 函式庫**（`http.ts` / `signal-factors/` / `mis-quotes.ts` / `market-regime.ts` / `types.ts`），不 import React。兩個名字相近但職責分明。
- **`lib/latest-scan.ts`（PLAN 2）**：掃描結果 JSON（`data/signal-scan-results/`）的**唯一讀取入口**。**非 `"use server"`**（純檔案 IO + 純函式，供多個 action import；比照 `lib/dashboard-spark.ts`）。`readLatestScan(options?)` → `LatestScan | null`（`scanDate` / `source` / `timestamp`(=`queriedAt`) / `prByCode`(relativeStrength——**PLAN 7 起 setup 列也有**) / `preInstByCode`(**PLAN 7 起「有 `r.preInst` 就收」不再限 setup**——breakout 列現在也帶插值 `preInst`；改讀 `r.preInst.*` 不讀 `r.scores.trustScore`) / `resultByCode`(每檔完整 `SignalResult`) / `watchlistQuotesByCode`)。`options.preferRealtime` 預設 false = 只讀 `{YYYY-MM-DD}.json`（eod 定案，watchlist / dashboard 用）；true = 比較最新 eod 與**今日** `{YYYY-MM-DD}-intraday.json` 的 `queriedAt` 回較新者（PLAN 3 的 `intraday` 模式用）。**PLAN 5**：realtime 檔改成固定檔名（每 30 分原子覆蓋），不再 `readdir + sort` 時間戳；`options.todayIso`（純讀檔沒有 `now`，由 `data-context` 傳 `t.iso`；省略則本檔自算 UTC+8）決定讀哪天的 `-intraday.json`。跨日殘留防呆：讀不到今日的就當沒有（走 `stale`），不 fallback 讀昨天的。私有 `parseScanFile()` export 供 `signal-scan.ts` 重用（`getScreeningResult()` 的 intraday/stale 分支另有自己的 `readLatestRealtimeFile()`，只讀今日 `-intraday.json` 不比 eod，職責不同）。
- **`lib/data-context.ts`（PLAN 2）**：`resolveDataContext(prisma, now?)` → `DataContext { mode, asOfDate, latestEodDate, latestScan }`，**「盤中該用 DB 昨收還是 MIS 即時」判斷的單一真相來源**。**非 `"use server"`**（被多個 action 呼叫；收 `prisma` 外部傳前端單例，比照 `market-regime.ts`）。三態 `mode`：`eod`（DB 最新一般股票交易日 == 台北今日 → DB 盤後定案）/ `intraday`（非今日 + **台北平日 09:00–17:00** + 有 `source==="realtime"` 的掃描 JSON 且夠新 → 用 JSON）/ `stale`（非今日 + 盤外 / 無夠新 JSON → DB 昨收，卡片標「收盤定案 {date}」）。**staleness 依是否已收盤分兩段**（PLAN 3 §3.6）：盤中（≤13:30）`INTRADAY_STALE_MS`(35 分，對齊 launchd 每 30 分掃描)；收盤後（13:30–17:00）`CLOSED_STALE_MS`(4 小時)——13:30 launchd 最後一次掃描產出的是「當天定案盤中值」（估全日量已 = 實際全日量、收盤即時價已定），撐到 17:00 pipeline 跑完切 `eod`，不中途掉回昨收。`asOfDate` = 畫面該顯示的「截至」日期。呼叫端：`watchlist.ts` `listWatchlist`（`dataFresh = mode === "eod"`）、`signal-scan.ts` `getScreeningContext` / `getScreeningResult`（PLAN 4：`mode === "eod" ? eod分支 : realtime分支`）、`dashboard.ts` `getWatchlistPerformance`（`refDate` 欄用 `asOfDate`，計算仍用各檔實際 `quote.date`）。
- **Prisma 單例**：`lib/prisma.ts` 把 `PrismaClient` 掛 `globalThis` 快取（dev hot reload 不會爆連線池），保留 `PrismaPg` driver adapter 寫法。**檔頭 `import "server-only"`**——`scripts/lib/` 與 `lib/` 名字太像，這是唯一能在 build 時擋下「client component 誤 import `lib/prisma.ts`」的機制。只能在 Server Component / Server Action import。
- **Server Actions**（不建 REST/GraphQL）：檔案放 `lib/actions/*.ts`，檔頭 `"use server"`。action 只做「呼叫 Prisma / 純函式 → 回傳可序列化 plain object」。回傳 `Date` 要先轉字串、`Decimal` 要 `.toNumber()` / `.toString()`、`BigInt`（`volume` / `*NetBuy`）要 `Number(...)`，在 action 邊界轉掉（`Decimal` 直接丟 client component 會壞）。現有：`health.ts`（`getDbHealth` → `DbHealth`：`today`（Asia/Taipei）/ `latestQuoteDate` / `quoteFresh` / `stockCount`（`securityType="stock"` 且 `delistedAt: null` 檔數——分母排除已下市，否則 ~170 檔殭屍股把覆蓋率天花板壓在 ~90%）/ `coverage.{quote,institutional,technical,margin}.{count,pct}`。燈號 = `latestQuoteDate === today`；覆蓋率基準日 = DB 最新交易日、分母 = `stockCount`（`margin` 另含約十幾檔特別股，pct 微幅偏高 <1%，可接受））、`signal-scan.ts`（**PLAN 4 精簡後**：`getScreeningContext` / `getScreeningResult({force?})` / `getSignalSpark(code)`——ROADMAP 4.5.3 統一選股，取代舊 `screening.ts` + `intraday.ts`。`getScreeningContext` 透傳 `resolveDataContext()` 的 `{ mode, asOfDate, latestEodDate }` + `hasScan`（eod → 有 `latestEodDate`；intraday/stale → `data/signal-scan-results/` 有今日 `{date}-intraday.json`）+ `willRefetch`（**PLAN 5**：非 eod + 有 `-intraday` 檔但 `failedCount / totalStocks > 0.10` → 進頁會同步重抓 ~30 秒，前端 loading 文案據此顯示「盤中資料不完整，正在重新抓取…」而非「載入中…」）。`getScreeningResult({force?})` = **單一入口**：`force ?? (ctx.mode === "eod" ? "eod" : "realtime")` → `eod` 分支 `findFirst` DB 最新一般股票交易日後**先讀 `data/signal-scan-results/{date}.json`（無時間戳）**，存在就直接回傳不重算；不存在或 `force:"eod"` 才同步 `runSignalScan(date, { prisma, source: "eod" })`（秒級，原 `runSignalScanEod` body 併入）並覆寫該檔——**`daily-pipeline.ts` 不會自動產出這份檔**（`run-signal-scan.ts` 獨立手動執行不進 pipeline），沒有這層快取的話每次進頁都會重新算一次全市場因子；`realtime` + `force` 同步 `runSignalScan(new Date(), { prisma, source: "realtime" })`（**卡 UI ~30 秒**，寫 `{date}-intraday.json`）；`realtime` 不 force → 讀今日 `{date}-intraday.json`（私有 `readLatestRealtimeFile()`），**PLAN 5**：若該份 `failedCount / totalStocks > 0.10`（明顯缺失，`isGrosslyIncomplete()`；只看 `failedCount` 佔比不看 `estimatedCount`——缺 z 用 h 代入是 MIS 常態）→ 同步 `runSignalScan(realtime)` 重跑覆蓋回新結果（不做背景 spawn + 輪詢）。`SignalScanView` 透傳 `watchlistQuotes?`；`getSignalSpark` 給展開列線圖按需撈近 `SPARK_WINDOW` 筆走勢，用 `lib/spark-series.ts` 的 `buildSparkSeries`。**PLAN 4 刪除**：`getScanMode` / `runSignalScanEod` / `startSignalScan` / `getSignalScanProgress` / `SignalScanProgress` / `getLatestScanMeta` / `getSignalScanResult`（併入 `getScreeningResult`））、`watchlist.ts`（`listWatchlist` → `WatchlistCardRow[]`：`stage` = `userStage ?? autoStage`（PLAN 6 手動分類優先），`autoStage` = `consecutiveAboveBand()` 即時判定 + 資料源三分支 `buildCardRow(item, ctx)`——`ctx.mode` ∈ `eod`(讀 DB 當日 ⚡) / `intraday`(讀 `ctx.latestScan.watchlistQuotesByCode` 🕐) / `stale`(讀 DB 昨收 🕐 +「收盤定案」)，**進頁不打 MIS**（PLAN 3）；`dataFresh` = `mode === "eod"`；卡片因子 `spark` / `volumeRatio`(一律現算)。**PLAN 7：`buildCardRow` 不再按 stage 分支**——`inst`(法人 diverging bar) / `factors`(距年高 / 突破幅度 / `relativeStrength`) / `preInst`(20 格日曆 + 兩條進度條) 一律計算，使用者手動改 `userStage` 後三區塊都有資料；`WatchlistCardRow` 的這三欄型別去 `| null`（degraded 用欄位內 `degraded: boolean` 表達）。`inst`/`factors`/`preInst` 三者共用的 DB 查詢在分支前就撈（`instWindow` 加 `dealerNetBuy`、`quoteWindow` 取前 20 筆 `volume` 當 `otherInstRatio` 現算分母，0 新查詢）。PR / 醞釀籌碼統一走 `ctx.latestScan`(`preferRealtime`) 的 `prByCode` / `preInstByCode`。**醞釀中卡片底排仍只顯示「量增」**（K棒/力道/位階三分數 PLAN 3 移除，PLAN 7 不加回）——`FactorList` 底排因子維持按 `row.stage` 分支，統一的是「法人籌碼區塊」+ `factors` 物件本身有值。`addToWatchlist({ codes })`（PLAN 6：廢 `source`，加入時 `computeAutoStages()` 算 `autoStage` 寫進 `userStage`）／ `removeFromWatchlist` ／ `updateWatchlistItem`（PLAN 6 加 `userStage?: SignalStage` 參數，手動分類 UI 用），mutation 後 `revalidatePath("/watchlist")`）、`dashboard.ts`（`getWatchlistPerformance` → `WatchlistPerfRow[]`：對 watchlist 每檔用 `scripts/lib/signal-factors/` 的 `computeCandleShape` / `computeVolumeStrength` / `computeBase` 現算 K棒 / 力道 / 位階分數 + 當日動能 / 籌碼 + `spark: SparkPoint[]`（60 日走勢，見下方 Dashboard 頁）。**不重跑 `calculateBreakoutStrength`**——它的 gate 會濾掉未突破的觀察股，交集太少。`computeBase` 吃布林帶寬序列（`TechnicalIndicator.bollingerBandwidth`）不是 close 序列；走勢圖另撈 `bollingerMid` 對每日 close 算偏離比例）、`market-regime.ts`（`getMarketRegime` → `MarketRegimeView`：**純讀檔不碰 DB**——讀 `data/market-regime/` 最新 `{date}.json`、組三段部位建議文字 `advice`。只 `import type`，不 import `scripts/lib/market-regime.ts` 本體）、`pipeline.ts`（`runDailyPipeline` / `getDailyPipelineStatus` → `DailyPipelineStatus`：首頁「立即更新資料」按鈕。spawn 未改造的 `daily-pipeline.ts` 本體、`stdio` 落 `data/daily-pipeline-runs/{stamp}.log`、`progress.json` 粗粒度狀態、parent `child.on("exit")` 寫終態、`STALE_MS`(20 分) 兜底 hot-reload 丟 listener。純檔案 IO + spawn，不 import 任何 `scripts/` 東西。`tailLines` 只吃 log 基本檔名並 `join(RUN_DIR, ...)`（靜態 scope，否則 Turbopack build 追蹤整個專案報 warning））。
- **`/` Dashboard 頁**：`<h1>` 下先一條 `<RegimeBanner regime={getMarketRegime()} />`（大盤濾網橫幅，見下），再兩張 Card。①「資料狀態」= `getDbHealth()` 的三欄（今日行情燈號：`quoteFresh` `bg-success` / 否 `bg-destructive` + DB 最新日 vs Asia/Taipei 今日；一般股票檔數；當日四表覆蓋率 `{pct}% ({count}/{stockCount})`，`<50` `text-destructive` / `<90` `text-warning`）+ 卡片底部 `<PipelineRunner />`（client component：「立即更新資料」按鈕 → `runDailyPipeline()` spawn 背景 pipeline → `setInterval(3s)` 輪詢 `getDailyPipelineStatus()` 顯示「已 N 秒」+ log 尾 → `done` 時 `window.location.reload()`。切走停輪詢不中斷子進程、切回若還 `running` 接管。`progress.json` 單檔鎖，同時只一個）。②「觀察類股今日表現」= `<WatchlistPerfTable />`（Server Component，`await getWatchlistPerformance()`，純展示無互動）：**卡片式 grid**（`sm:2 / xl:3` 欄）。每張卡左側一張 **60 日走勢 SVG**（200×88 viewBox，`h-22 w-50`；Y 軸 = 收盤相對當日布林中軌的偏離比例 `(close − bollingerMid)/bollingerMid`，固定夾 ±`SPARK_CLIP`(=0.25)，所有卡共用同一刻度 → 用布林帶寬正規化，盤整期線壓中線窄帶、噴出頂到邊界，卡跟卡之間絕對起伏可比、不被個股絕對振幅誤導；`PAD=8` 讓夾邊的點不貼框；線色依當日漲跌 `SPARK_UP`/`SPARK_DOWN`（硬編 HEX 常數，對應 `--up`/`--down`）+ 線下漸層；有效點 < 2 顯示「資料不足」）；右側 `代號(text-3xl) 漲跌%(text-up/down) [突破pill: bg-primary/10 text-primary] / 名稱 收盤 refDate` + 底排 K棒·力道·位階三分數（`>=70` `text-success` / `40~70` `text-foreground/80` / `<40` `text-muted-foreground/70`，null 顯「—」+ degraded 加「不足」tag）+ 籌碼合計/投信（張）。**走勢圖元件 = `components/ui/Sparkline.tsx`（共用元件單一出處，dashboard + screening 展開列都用；HEX 常數也在該檔）**；序列建構 = `lib/spark-series.ts` 的 `buildSparkSeries(quoteWindow, midByDate)` 純函式（`getWatchlistPerformance` 與 `getSignalSpark` 共用）。`SPARK_CLIP` / `SPARK_WINDOW` / `SparkPoint` 型別在 `lib/dashboard-spark.ts`（**`lib/actions/dashboard.ts` 檔頭 `"use server"` 不能匯出 const / 值，只能匯出 async function**——常數要抽獨立非-server 檔給 action 與元件共用）。空清單有提示文案。
- **`components/ui/Card.tsx` 的 `<FieldLabel>`**：全站「次級說明文字」（卡片欄位名 / 燈號 label / 分數 label…）的**單一出處**，`text-sm text-muted-foreground` + 可選 `className`（透過 `cn()`）覆蓋顏色。`Stat` 內部用它，`app/page.tsx` 資料狀態卡、`WatchlistPerfTable` 分數 / 籌碼 label 都用它。要整體調這類字級只改這個元件。
- **`components/dashboard/RegimeBanner.tsx`**：大盤濾網（市場狀態燈號）橫幅，Server Component 純顯示。`variant="banner"`（首頁：完整橫幅 + `<details>` 三維度明細，`bearish` 時 Card `border-destructive/40`）／`variant="strip"`（`/screening` 頁頂：圓點 + `大盤：偏空` + `advice` 一行）。兩 variant 都標「截至 {regime.date}（收盤）」（PLAN 2）——明確告知盤中顯示的是上一交易日收盤定案值；`regime.available === false` 時不加。**配色 = 直接複用狀態語意 token**（`bg-success`=偏多 / `bg-warning`=中性 / `bg-destructive`=偏空 / `bg-muted-foreground/50`=無資料），刻意跟個股漲跌色（`--up`/`--down`，台股漲紅跌綠）**不同語意**（不同區塊不混淆，元件註解寫明）。資料來源 `getMarketRegime()`（`data/market-regime/{date}.json`，盤後 pipeline 每日一次；盤中看到的是上一交易日收盤定案值）。
- **`/screening` 頁（ROADMAP 4.5.3：單頁 + 階段 filter；PLAN 4 簡化——移除 toggle / 背景任務 / 自動刷新）**：Server Component 外殼 `async`，頁面標題下、`ScreeningPanel` 之上放 `<RegimeBanner variant="strip" />`。
  - **進頁載入**（跟 watchlist 同一心智模型——`resolveDataContext()` → 讀一次就定住）：單一 `useEffect` 掛載時 `getScreeningContext()` → `setCtx` → `getScreeningResult()` → `setView` + `resetTableState()`。`eod` 模式進頁**先讀 `{date}.json`**，同一天內第二次以後進頁直接讀檔（不重算，**使用者不用按任何按鈕、無「昨」badge**）；沒有該檔（當天第一次進頁）才同步跑一次盤後掃描並寫入（秒級）。`intraday`/`stale` 讀 launchd（`intraday-scan.ts` 每 30 分）產出的今日 `{date}-intraday.json`（**PLAN 5** 單一檔原子覆蓋，不再累積時間戳；`failedCount` 佔比 > 10% 進頁會同步重抓，`ctx.willRefetch` 讓 loading 文案準確），無 JSON → `view` null + 提示「尚無盤中掃描結果——稍候或按『立即掃描』」。
  - **一顆按鈕** `rerun()`：`ctx.mode === "eod"` → 文案「重跑盤後掃描」、`getScreeningResult({ force: "eod" })`（秒級）；否則 → 文案「立即掃描」、`getScreeningResult({ force: "realtime" })`（**Server Action 內同步跑全市場 MIS 掃描、卡 UI ~30 秒**，`useTransition` isPending 顯示「掃描中…（約 30 秒）」；不再 spawn 背景子進程 + 輪詢 progress.json）。
  - **狀態行**（取代舊 toggle）：`eod` → 「盤後定案 · {asOfDate}」；`intraday` → 「盤中即時 · 背景每 30 分更新，重整頁面看最新 · 截至 {asOfDate}」；`stale` → 「盤後未跑，顯示最近一次掃描（{asOfDate}）」或「尚無掃描結果」。realtime 常駐警語框保留精簡版。**無自動輪詢**——要看 launchd 產出的新掃描得手動重整頁面（跟 watchlist 一致）。
  - **階段 filter tab**（三個，順序固定 **今日突破 → 已延伸 → 醞釀中**，預設「今日突破」；「全部」tab 已於 UI 改版移除）= 純前端 `results.filter(r => r.stage === f)`。每 tab 內 `rank` 是該階段內名次，預設 `sortKey="rank"` / `asc`。一張可排序表格 + 點列展開三欄明細 + 勾選加入。
  - **PLAN 4 移除的東西**（都是「昨」badge 事故根因或已被 launchd 取代）：盤後/盤中 toggle（`chosenMode` state + 兩顆按鈕）、realtime 背景任務（`startSignalScan()` spawn `_run-signal-scan.ts` + 每 2 秒輪詢 `progress.json` + 切走再回來「接管輪詢」+ 進度條 UI）、自動刷新（PLAN 3 §4.2 的 `setInterval(60_000)` 比 `getLatestScanMeta()` timestamp）+ 進頁「`done` 殘檔撿回」useEffect（`DONE_STALE_MS` 那段）。
  - **表格欄位**（UI 改版後）：排名 / 代號 / 名稱 / **價格**（`estimated` 尾隨琥珀「估」badge）/ 漲跌%（百分比、漲紅跌綠）/ **狀態**（階段中文 pill，`breakoutDay`=`bg-destructive/10 text-destructive`、`extended`=`bg-warning/10 text-warning`、`setup`=`bg-muted text-muted-foreground`）/ **量增**（`x{volumeRatio}`，醞釀中 `—`）/ **籌碼**（一句話結論 chip，醞釀中 `—`，盤中沿用昨日籌碼附「昨」badge）/ 總分。`Column` 有 `sortable?: boolean`（`inst` 欄 `false`，不可排序）。舊「追」badge 從價格欄移到籌碼 chip（`margin-chasing` → 紅「融資追價」）。舊獨立「降級項目」欄移到展開列底部。
  - **展開列**（`components/screening/SignalDetail.tsx`，原 `Detail` function 搬出）：`setup` → 兩欄卡（`grid-cols-1 lg:grid-cols-2`）：左 `<SignalSparkPanel>` / 右 `<PreBreakoutInstitutional preInst>`（醞釀階段法人籌碼區塊，與 watchlist 卡片共用，見 `/watchlist` 段）。`SignalResult.preInst` 由 `run-signal-scan.ts` 的 `preBreakoutExtras()` 在兩個 setup block（eod/realtime）帶（PLAN 7 起 breakout block 也帶，走 `buildBreakoutPreInst()` 插值），純回傳值擴充不動評分。screening 展開列按 `SignalResult.stage` 選元件，多帶的欄位它不讀 → 視覺不變。`breakoutDay` / `extended` → 三欄卡（`grid-cols-1 lg:grid-cols-3`，深色卡）：左 `<SignalSparkPanel code>`（client，按需 `getSignalSpark(code)` 撈近 60 交易日走勢）/ 中 `<InstitutionalFlowPanel inst warnings>`（投信 / 外資 diverging bar，**柱長用 `inst.trustRatio` / `inst.foreignRatio`（近 5 日淨買超 ÷ volumeMa20），不是股數不是分數**；箭頭 ▲/▼ = 今日單日方向；翻臉柱子 `opacity-50`）/ 右 `<BreakoutFactorBars factors rs>`（4 列，**長條用因子內部分數 0~100，不用原始值**；分級 `>=70` 綠 / `40~69` 琥珀 / `<40` 紅；不放 base 因子）。詳細規格與設計理由見 `docs/PROGRESS.md`「Screening 頁表格 UI 改版」段。
  - `priceSource === "estimated"`（realtime 缺成交價、`h` 代入）列尾「估」badge（`bg-warning/10 text-warning`）+ row 淡色 + 展開多一行提示。
  - `SignalResult.warnings` 命中 → 展開最上方 `<WarningBanner>` 逐條紅/琥珀框（查 `WARNING_LABELS`）+ 表格「籌碼」欄 chip 轉對應色。**`warnings` 與 `degraded` 語意分開**：`degraded` = 因資料不足所以不確定；`warnings` = 資料充足、系統明確發現的風險訊號。與 `SignalScanOutput.warnings`（管線層級警語）是不同層級。**PLAN 8 起 4 個 warning key**（中央清單在 `components/signal/labels.ts` 的 `WARNING_LABELS`，含 `trigger` 註解）：`"margin-chasing"`（`breakoutDay`/`extended`：突破當日投信+外資淨賣超 ∧ 融資餘額 5 日增速排自己 40 天前 20%）/ `"institution-crowded"`（`breakoutDay`/`extended`：`(trustRatio + foreignRatio) > crowdedThreshold(0.8)`，`computeInstitutionalFlow` 回布林，法人濃度過高、體質脆弱、尚未反手）/ `"trend-reversal"`（**setup，PLAN 8 起 setup 首次有非空 warnings**：投信近 5 日淨買超合計 < 0 ∧ 前 15 日 > 0，`computeTrendReversal`）/ `"concentration"`（setup：20 日外資+自營單一交易日買超 ÷ 窗內總正買超 > 0.5，`computeSingleDayConcentration`）。**全部只標記不動分數**（比照 `marginChasing`：純函式回布林、呼叫端 push）。
  - **`SignalResult.instBackground?: "positioned-early" | "fresh-entry"`（PLAN 8 §6，中性背景非風險）**：突破階段法人這波是早佈局（前 15 日投信也在買）還是剛進場（前段無動作、近 5 日才買）。`classifyInstBackground` 純函式，複用 PLAN 7 已撈的 `accInputs` 20 日投信序列（0 額外查詢）。**不進 `warnings`**（兩情境解讀甚至相反）；資料不足 → 欄位省略。前端查 `INST_BACKGROUND_LABELS` 在 `InstitutionalFlowPanel` 下方渲染一行小灰字。只 `breakoutDay`/`extended` 帶。
  - **`SignalResult` 的 UI 專用欄位**（UI 改版新增，皆 optional）：`volumeRatio?` / `inst?: { trustRatio, foreignRatio, todayTrustDir, todayForeignDir }` / `factors?: { breakoutMarginScore, breakoutMarginPct, proximityShortScore, proximityLongScore, proximityShortPct, proximityLongPct }`（**只在 `breakoutDay` / `extended` 有值**）；`preInst?: { buyDayFlags, buyDays, consecutiveBuyDays, dataDays, trustScore, otherInstScore, trustNetRatio, otherInstRatio, degraded }`（**PLAN 7 起 `setup` + `breakoutDay` + `extended` 皆有值**——`setup` 走 `preBreakoutExtras()`（全 setup 母體 `rankScore` 百分位）；`breakoutDay` / `extended` 走私有 `buildBreakoutPreInst()`（`percentileOf(值, setup 母體升冪陣列)` **插值不進母體**，避免污染 setup 分數），結構同 `WatchlistCardRow.preInst`）。另 setup 的 `scores` PLAN 7 起也帶 `relativeStrength` key（合成公式不吃、只給 `prByCode` 收）。純回傳值擴充，**不動評分**——`--date` 重跑逐檔 `totalScore` diff = 0 驗證過（PLAN 8 也逐檔驗過）。私有 `percentileOf(value, sortedAsc, naScore)` = 二分找 `count(<=value)/N*100`，與 `rankScore` 語意對齊。
  - 勾選加入：直接 `addToWatchlist({ codes: [...selected] })`（PLAN 6 §2 廢除 `source`；加入時 `addToWatchlist` 自算 `autoStage` 寫進 `userStage`，不再前端分組傳 source）。
- **三階段 + 按階段不同合併方式（設計理由）**：`run-signal-scan.ts` 對全市場算「同一份因子分」，但**最後合成總分的公式按階段不同**——不只是換權重數字。`setup`（還沒站上上軌）用**乘法**`籌碼分 × 技術就緒係數`（就緒係數下限 0.5）：這是「籌碼強 ∧ 技術收斂」的 AND 關係，加權和會讓「籌碼極高但股價亂噴沒收斂」的股票靠籌碼分硬拉出高總分，正是乘法 + 保底要擋的。`breakoutDay` / `extended`（已站上上軌）用**8 分項加權和**（沿用 breakout 結構 + 新 `institutionalFlow`）。三階段由「連續站上布林上軌天數」互斥判定（`consecutiveAboveBand`：≤ 0 → setup；1~2 → breakoutDay；> 2 → extended），一支股票只落一個階段。**排名為同階段內名次**（三階段各 1..N），跨階段分數語意不同、不可直接比較（前端已收斂成三個單一階段 tab，無跨階段檢視）。統一 gate（市值 + 當日量 + 均量）比舊 accumulation 候選池嚴（多了市值下限 + 當日量下限），setup 候選數因此明顯少於舊 accumulation 腳本。**`margin-chasing` 警示（融資融券修正因子）**：`breakoutDay`/`extended` 階段，若「突破當日三大法人（投信＋外資）淨賣超 ∧ 這檔融資餘額近期增速排在自己 40 天歷史前 20%」→ `SignalResult.warnings` push `"margin-chasing"`。**刻意只標記不動分數**（既有 `sellCapScore` 封頂上再疊動態調整 = 特例規則互相牽扯難除錯；標記取代動態調整，跟 `degraded` 哲學一致）。realtime 當日融資餘額拿不到 → 恆不觸發。
- **`/watchlist` 頁（卡片 gallery + 階段分頁，2026-09-01 改版；資料源 PLAN 3 再改）**：`listWatchlist` 回 `WatchlistCardRow[]`（`components/watchlist/WatchlistGallery.tsx` "use client" tab + `md:2/xl:3/2xl:4` 欄 grid、`WatchlistCard.tsx` 單張卡）。
  - **階段 tab**（三個，順序固定 **首次突破 → 延續爆發 → 醞釀中**，預設「首次突破」）：分類依 `userStage ?? autoStage`（PLAN 6：**手動分類優先**，既有資料 `userStage` null 時 fallback `autoStage`）。`autoStage` = `consecutiveAboveBand()` 即時判定（`<=0` setup / `1~2` breakoutDay / `>2` extended），只拿來比對 + 不一致 chip。tab label 顯示「{中文}（{total}{· N 異動}）」——「異動」= 該 tab 裡 `autoStage !== (userStage ?? autoStage)` 的檔數（`countByStage` 是 `Record<SignalStage, { total, mismatch }>`）。空 tab 顯「此分類目前無觀察股」。
  - **手動分類**（PLAN 6 §3）：`addToWatchlist` 加入時私有 `computeAutoStages(codes)`（逐 code 撈最新 quote + 布林上軌歷史 → `consecutiveAboveBand`，缺指標 fallback `"setup"`，**只用 DB 不打 MIS**）算出 `autoStage` 寫進 `userStage`。`buildCardRow` 的 `stage`（決定卡片因子渲染）= `item.userStage ?? autoStage`；`WatchlistCardRow` 帶 `userStage` + `autoStage` 兩欄。`userStage !== autoStage` → 卡片標頭加「⚠ 自動判定：{中文}」chip。卡片底部三顆按鈕（setup / breakoutDay / extended，當前 `userStage` 高亮）→ `updateWatchlistItem({ code, userStage })`（**`SignalStage` 字串值 == Prisma enum 成員名，直接傳不需 map**）→ `revalidatePath("/watchlist")`。盤中 `autoStage` 閃動（衝上上軌 → 異動 +1，回落復原）是預期行為。
  - **資料源（PLAN 3 — 三分支，接 `resolveDataContext()`）**：`buildCardRow(item, ctx)`，`ctx.mode` 決定：
    - `eod`（DB 最新一般股票交易日 == 台北今日）→ 讀 DB 當日（卡片左上 ⚡ `LightningBoltIcon`）。與改版前輸出相同。
    - `intraday`（非今日 + 盤中 + 有夠新 realtime 掃描 JSON）→ 讀 `ctx.latestScan.watchlistQuotesByCode.get(code)`（`close` / `changePercent` / `volume` 估全日量 / `priceSource` / `refDate`）；該 code 不在（MIS 沒抓到 / z·h 皆缺）→ 個別退回 DB 昨收。左上 🕐、日期字串「盤中 {refDate}」。
    - `stale`（盤外 / 無夠新 JSON）→ 讀 DB 昨收。左上 🕐、日期字串「收盤定案 {latestEodDate}」（本份對使用者的明確化：盤外看到的是收盤價）。
    - **進頁不再打 MIS**——盤中即時報價統一由 `intraday-scan.ts`（launchd 每 30 分）產出、寫進掃描 JSON，watchlist 頁只讀 JSON。`WatchlistCardRow` 加 `mode: DataMode` + `latestEodDate`；`dataFresh` 僅 `mode === "eod"` 為 true。
  - **`volumeRatio` 一律現算**（`effectiveVolume` ÷ T-1 `volumeMa20`——需要「當前 volume」，DB 當日量 or 盤中估全日量）；`autoStage` 一律 `consecutiveAboveBand()` 現算。
  - **醞釀中卡片不再顯示 K棒/力道/位階**（PLAN 3 需求調整）——`candleScore` / `volumeScore` / `baseScore` + 相關 `degraded` 從 `WatchlistCardRow` 移除。那三個「單檔現算」分數是「盤中要現算還是讀 JSON」岔路的來源；拿掉後三個模式一律不現算。`FactorList` setup 分支只留「量增」`Cell`（`ScoreCell` / `scoreClass` 移除）。**dashboard 的 `WatchlistPerfRow` 不受影響**（自己算自己的三分數）。
  - **籌碼**恆用 DB 最新一筆；`InstitutionalFlowPanel` 底部「以上為近日資料」文字沿用（**不加時鐘**）；「今日方向」只有籌碼日期 == `refDate` 才算。
  - **強度 PR 欄 / 醞釀籌碼**（breakout 階段 PR、setup 投信/外資百分位）：全市場百分位、watchlist 少檔算不出 → 統一走 `ctx.latestScan`（`preferRealtime: true` 讀最新掃描 JSON）的 `prByCode` / `preInstByCode`（PLAN 2 豁免 gate 後對觀察股都有值）。不在結果裡 → 「—」；`scanDate != refDate` → `PR{n}` 旁加 🕐（`relativeStrengthStale`）。
  - **PLAN 8：籌碼面觀察標籤**——`WatchlistCardRow` 加 `warnings: string[]` + `instBackground: InstBackground | null`，`buildCardRow` 從 `ctx.latestScan.resultByCode.get(code)` 帶（0 新查詢、0 新讀檔——`resultByCode` 已在 `LatestScan` 裡；策略同 PR / preInst：統一從掃描 JSON 帶讓卡片與 screening 頁一致）。卡片走勢圖下方 `<WarningBanner warnings={row.warnings} />`；breakout 卡片法人區塊下方 `instBackground` 一行小灰字。頂部 chip 兩個 `resolve*Chip` 現在收 `row.warnings`（原傳 `[]`）——`trend-reversal` / `concentration` / `institution-crowded` 命中會改 chip 文字。
  - **買入狀態 input 全移除**（`isPurchased` / `buyPrice` / … UI 拿掉）；**schema 欄位保留**（YAGNI），`updateWatchlistItem` action 保留 + PLAN 6 加 `userStage?: SignalStage` 參數（手動分類 UI 用）。
  - **醞釀中卡片的法人籌碼區塊**（`components/signal/PreBreakoutInstitutional.tsx`，**screening 展開列與 watchlist 卡片共用**，與突破階段完全不同）：標題「法人買賣」→「近20日投信買超日」+ 右側 ⓘ（買超天數 / 連續天數）→ **20 格點狀日曆**（2×10，舊→新，綠=投信買超日 / 灰=非買超 / `bg-muted/40`=補位；只問有沒有買不分賣超與缺值，對齊 `buyFrequency`）→ **投信進度條**（綠，寬度 = `scores.trustScore`）+ ⓘ（佔已發行股數% / 優於全市場%）→ **外資/自營進度條**（橘，寬度 = `scores.otherInstScore`）+ ⓘ（佔同期成交量%）。**進度條分數是全市場 `rankScore` 百分位**——screening 展開列由 `runSignalScan` 現算帶入 `SignalResult.preInst`（PLAN 7 起 breakout 階段也帶，用 `percentileOf` 對 setup 母體插值）；watchlist 幾檔算不出 → 讀最近 eod 掃描結果 JSON（`readLatestScan()` 的 `preInstByCode`）。watchlist 該檔不在掃描結果 → 只留日曆 + 投信列（無條、無「優於全市場」行）；20 日序列 < `ceil(20×0.5)` 天 → `degraded` → 只留標題 + 「資料不足」+ badge。日曆逐日陣列 = `trustNetBuyNewestFirst.map(v>0)` reverse（scan `preInst` 已含 / watchlist action 自撈 20 日 `investmentTrustNetBuy`）。ⓘ = `@radix-ui/react-tooltip` + 手刻 `components/ui/Tooltip.tsx`。頂部籌碼 badge 走 `components/signal/pre-breakout-chip.ts` 的 `resolvePreBreakoutChip`（9 條判斷表 + PLAN 8 加 `trend-reversal` / `concentration` 前置分支，`WatchlistCard` 依 stage 分流）。`run-signal-scan.ts` 只加 `SignalResult.preInst` / `instBackground` 回傳欄、setup `warnings` push，**不動評分**。
  - 共用元件在 `components/signal/`（見下）。漲跌色台股慣例（漲紅跌綠）。
- **`components/signal/`（screening + watchlist 共用的訊號展示元件）**：
  - `labels.ts`：**標籤中文名 + tone 的單一出處**，純常數。`STAGE_LABELS` / `STAGE_PILL_CLASS` / `STAGE_ORDER` / `FACTOR_LABELS`；**PLAN 8 加**：`Tone` / `TONE_CHIP`（從 `InstitutionalFlowPanel.tsx` 搬來，該檔改 re-export，既有 import 路徑不炸）、`WARNING_LABELS`（4 個 `warnings` key 的 `{ title, detail, trigger, tone }` 中央清單——`trigger` 是觸發條件白話給開發者看，門檻值本身在 `config.ts` 的 `SignalScanConfig`）、`INST_CHIP` / `PRE_CHIP`（`resolveInstChip` / `resolvePreBreakoutChip` 的 chip 文字常數化，**函式分支結構不變**、只改引用 + 每分支補觸發條件註解）、`INST_BACKGROUND_LABELS`（`instBackground` 的 `{ text, hint }`）。screening 的 `ScreeningPanel` / `BreakoutFactorBars` / `SignalDetail` 都 import 這裡。stage 標籤兩頁統一為「首次突破」/「延續爆發」。
  - `WarningBanner.tsx`（PLAN 8 §8.3，新）：`<WarningBanner warnings={string[]} />` → 對命中的每個 warning 查 `WARNING_LABELS` 渲染一行 `⚠ {title}：{detail}`，底色依 tone。空陣列不渲染。screening 展開列（`SignalDetail`）+ watchlist 卡片（`WatchlistCard`，走勢圖下 / 法人區塊上）共用，取代 `SignalDetail` 舊寫死的 `margin-chasing` 紅框。
  - `InstitutionalFlowPanel.tsx`（原 `components/screening/InstitutionalFlow.tsx`，`git mv` 過來）：法人 diverging bar + 結論 chip + 過期文字。`export resolveInstChip`（PLAN 8：加 `institution-crowded` 前置分支——`noData` 之後、一般流向之前，但今日淨賣超的 `destructive` chip 仍優先）、`TONE_CHIP` / `Tone` re-export（實體搬到 `labels.ts`）、`showChip?: boolean`（watchlist 卡片頂已有 chip → false）。
  - `FactorList.tsx`：卡片底排「label + 數字」（**非長條圖**；screening 的 `BreakoutFactorBars` 保留長條版，兩者只共用資料 + `FACTOR_LABELS`）。breakout = 量增/距年高%/強度 PR/突破%；setup = **只有量增**（PLAN 3 移除 K棒/力道/位階，`ScoreCell` / `scoreClass` 已刪）。
  - `FreshnessBadge.tsx`：`fresh ? <LightningBoltIcon/> : <ClockIcon/>`（`@radix-ui/react-icons`）。
  - `PreBreakoutInstitutional.tsx` / `pre-breakout-chip.ts`：醞釀階段法人籌碼區塊 + 頂部 badge（見上 `/watchlist` 段）。**PLAN 8**：`resolvePreBreakoutChip` 簽名加 `warnings: string[]`（預設 `[]`），加 `trend-reversal` / `concentration` 前置分支（`noData` 之後、9 條判斷表之前），chip 文字引用 `PRE_CHIP`。
  - `labels.ts` 的 `FACTOR_LABELS` 另含醞釀階段字（`institutionalTrade`「法人買賣」/ `trustBuyCalendar`「近20日投信買超日」/ `otherInstitution`「外資 / 自營商」）。
- **`components/ui/Tooltip.tsx`**：`@radix-ui/react-tooltip` 的手刻薄包裝（照 `Button.tsx` 用 token 的模式，**不引入 shadcn**）。`<Tooltip content={...}>{trigger}</Tooltip>`，Portal + 自動 side 翻轉處理卡片邊緣碰撞，`delayDuration={200}`，手機 tap 觸發。目前只有 `PreBreakoutInstitutional` 的 ⓘ 用。
- **`app/page.tsx` 等會查 DB 的頁面檔頭加 `export const dynamic = "force-dynamic";`**——否則 `next build` 會試圖靜態預渲染、在 build time 連本機 Postgres（CI / 沒開 DB 時直接失敗）。
- **`*.css` import**：tsconfig 有 `noUncheckedSideEffectImports: true`，會擋 `import "./globals.css"`。對策是 `app/globals.css.d.ts` 內容 `declare module "*.css";`。**不要為此關掉該旗標**（`scripts/` 也吃這個旗標）。
- **`next.config.ts` 的 `serverExternalPackages`**：列 `@prisma/client` / `@prisma/adapter-pg` / `pg`，讓 Prisma 相關套件走 Node 原生 require 不進 bundler（generated client 用 `import.meta.url` 定位 engine，被打包會找不到路徑）。
- **背景任務**：模式是「Server Action `spawn` 一個 detached 子進程 → 子進程每步覆寫 `data/xxx/progress.json` → 另一個 Server Action 輪詢讀檔 → client component `setInterval` poll」。**`spawn` 用 `process.execPath` + `node_modules/tsx/dist/cli.mjs` 絕對路徑 + 目標腳本當參數**，不要用 `pnpm tsx` / `npx tsx`（detached 子進程解析 launcher 慢、PATH 依賴）。`.npmrc` 設 `node-linker=hoisted`，所以 `node_modules/tsx/dist/cli.mjs` 是扁平路徑、解析得到。原子寫 `progress.json`（先寫 `.tmp` 再 `renameSync`）避免輪詢讀到寫一半。**已有正式使用者**：(1) ~~`startSignalScan()` + `_run-signal-scan.ts`~~ **PLAN 4 已刪**——選股頁 realtime 掃描不再前端 spawn（「立即掃描」改成 Server Action 內同步跑，卡 UI ~30 秒）。realtime 掃描的背景執行者只剩 `scripts/pipeline/intraday-scan.ts`（PLAN 3）——由 launchd（`com.piercelin.intradayscan.plist`）每 30 分觸發、無前端在等，寫 `data/signal-scan-results/{台北今日}-intraday.json`（**PLAN 5**：單一檔每 30 分原子覆蓋，取代累積的 `{timestamp}.json`；`writeResult` 先寫 `.tmp` 再 `renameSync`；`runRealtime` 內部仍寫 `progress.json` done 態，前端不讀了，只剩 CLI 紀錄用途）。`readLatestScan({ preferRealtime: true, todayIso })` 讀今日 `{date}-intraday.json`。 (2) `lib/actions/pipeline.ts`（首頁「立即更新資料」按鈕）——spawn 的是**未改造的 `daily-pipeline.ts` 本體**（該檔要保持給 launchd 用、不加 progress callback），所以沒有子進程自寫進度：`stdio` 導到 `data/daily-pipeline-runs/{stamp}.log`，**parent（Next 進程）監聽 `child.on("exit")` 覆寫 `progress.json` 終態**，進度只有粗粒度（`status` + 存活秒數 + log 檔尾 12 行）。dev hot-reload 會丟這個 exit listener → `getDailyPipelineStatus()` 用 `STALE_MS`(20 分) 判定 running 逾時回報 `error` 兜底。`PipelineRunner.tsx` 完成後 `window.location.reload()` 刷新覆蓋率（`app/page.tsx` 是 `force-dynamic`）。（PoC 檔案 `scripts/_poc/` + `lib/actions/poc.ts` + `components/PocRunner.tsx` 已於 ROADMAP 3.1 刪除。）
- **指令**：`pnpm dev`（開發）、`pnpm build`、`pnpm start`。
- **⚠️ 不要自己起 / 殺 dev server**：使用者平常自己開著 `pnpm dev`（port 3000）。要驗證 runtime 渲染時，**直接 `curl http://localhost:3000/...` 打使用者那隻**；打不通就跟使用者說「請開 dev server」或改用 `pnpm build` + `tsc --noEmit` 靜態驗證，**不要自己 `pnpm dev` 起一隻**。**絕對不要 `pkill -f "next dev"` / `pkill -f next-server`**——那會連使用者的 server 一起砍掉（字串比對不分你我）。真的非得自己起，用獨立 port（`PORT=3123 pnpm dev`）且收尾只 `kill` 自己記下的那個 PID。
- **tsconfig**：Next 首次 `dev` 會自動改 tsconfig（加 `moduleResolution: "bundler"`、`plugins: [{name:"next"}]`、`include` 等），已一併調 `module: "esnext"` 讓 scripts 與 app 共用同一份。`pnpm exec tsc --noEmit` 對 `scripts/` 維持乾淨。

## 目前進度

進度詳情記錄在 [docs/PROGRESS.md](docs/PROGRESS.md)，不寫在這裡。每次完成功能或做出重大調整，記得同步更新該檔案。

## 既有腳本

`scripts/` 依用途分子資料夾：`pipeline/`（每日自動跑）、`screening/`（要選股時手動跑）、`backfill/`（偶爾手動回補）、`lib/`（純函式庫 + 撈 DB helper，多處共用）、`archive/`（已停用、留著參考，不刪不維護）。移動腳本時記得一併更新這裡與 README 的路徑。（`scripts/backtest/` 已隨回測系統放棄移除，見下方「回測系統 — 已放棄」段。）

### `scripts/pipeline/` — 每日排程

- **`intraday-scan.ts`**：盤中掃描薄殼（**ROADMAP 4.5.x / PLAN 3**，launchd 觸發、非 daily-pipeline 的一步）。內部判「台北平日 09:00–13:30？」否 → `console.log` + `return`（靜默、exit 0；此判斷是「要不要**跑掃描**」，維持 13:30 上界——收盤後不需再抓；頁面「顯示哪份」的 09:00–17:00 判斷在 `data-context.ts`）；是 → `runSignalScan(new Date(), { prisma, source: "realtime" })` 寫 `data/signal-scan-results/{台北今日}-intraday.json`（**PLAN 5**：單一檔每 30 分原子覆蓋）。**不寫 DB**（盤中衍生值不落地）。**PLAN 4 起這是 realtime 掃描唯一的背景執行者**（`_run-signal-scan.ts` + `startSignalScan()` 已刪）——被 launchd 排程觸發、無前端在等、只需靜默跑完寫 JSON。搭配 `com.piercelin.intradayscan.plist`（台北平日 **10:00–13:30**（PLAN 5，原 09:00–13:30——開盤第一小時 MIS 不穩、量能估計不準）整點 + 半點觸發，週末靠腳本內部 skip，無 `.ok` 標記——盤中每 30 分覆蓋是預期行為，**已部署到 launchd**）。內部時段判斷仍是 09:00–13:30（腳本層防線比 plist 寬鬆無害）。`pnpm tsx scripts/pipeline/intraday-scan.ts`（非交易時段跑會印 skip 訊息並 exit 0）。**下游**：`/screening` 頁 `intraday`/`stale` 模式進頁讀今日 `{date}-intraday.json`（**無自動刷新，PLAN 4 移除**——要看新的手動重整；PLAN 5：該份缺失 > 10% 進頁同步重抓）、`/watchlist` 頁 `intraday` 模式的 `watchlistQuotes` + PR / 醞釀籌碼來源。
- **`daily-pipeline.ts`**：每日排程主控腳本，八步：補今日報價（**TWSE 關鍵路徑；TPEx 非關鍵路徑**——TPEx 失敗只 warn + `warningCount++`，隔天 pipeline 再抓 / 缺的那天用 `backfill-daily-quotes.ts` 補；TWSE↔TPEx 之間 `sleep(TPEX_COLD_GAP_MS=4000)`）→ **補今日加權指數 TAIEX（第 1.5 步）** → 抓今日籌碼 → 抓今日估值 → **抓今日融資融券（第 3.5 步）** → 算技術指標（`mode: "latest"`，只算當天，秒級）→ **算大盤濾網（市場狀態燈號）** → 算產業熱度。TWSE 報價/TAIEX/籌碼/估值任一失敗印錯誤並非 0 結束；TPEx 報價、第 3.5 步（融資融券，證交所通常傍晚才出）與大盤濾網為**非關鍵路徑**，失敗只印警告 + `warningCount++`，不中斷。**第 1.5 步 TAIEX 為關鍵路徑**：`fillTodayIndex(todayStr, prisma)`（`backfill-index-quotes.ts` 匯出，打 FinMind），FinMind 拋錯就 throw；但若只是「FinMind 尚無當日 TAIEX（資料源不同步）」則印警告不中斷（大盤濾網會自動降級 `step1-breadth-only`）。第 4 步 `calculateTechnicalIndicators` 的 `where` 納入 TAIEX，會把第 1.5 步補好的當日 close 一併算出 `TechnicalIndicator`。**非當日資料防呆**：報價步驟後若 TWSE+TPEx 皆無當日資料，提前結束整支 pipeline 並印警告數。**當日成功標記檔**：跑完無致命錯誤（正常結束 or 非交易日提前結束）時寫 `data/daily-pipeline-runs/{YYYY-MM-DD}.ok`（gitignored），`main()` 開頭若當日 `.ok` 已存在則印訊息並 `return`（讓 17:30 / 18:00 補跑在 17:00 已成功時秒退）；致命錯誤不寫 `.ok`。無 `isMain` guard，`import` 這支即會執行 `main()`（`lib/actions/pipeline.ts` 靠這點 spawn 它跑首頁「立即更新資料」按鈕）。整支實測 ~22 秒。`pnpm tsx scripts/pipeline/daily-pipeline.ts`。搭配 `com.piercelin.dailypipeline.plist`（每天 17:00 / 17:30 / 18:00 三次觸發，後兩次為補跑、靠 `{date}.ok` 標記避免重跑，**已部署到 launchd**）。
- **`fill-daily-quotes.ts`**：補齊全市場報價，自動新增 `Stock`、upsert `DailyQuote`。匯出 `fillOneDayTwse(date)`（`MI_INDEX`，可補任意歷史日）與 `fillTodayTpex(expectedIsoDate?)`（TPEx OpenAPI，只能拿「目前最新一天」，日期不符會跳過寫入並回傳 `isStaleDate: true`）。對外 fetch 走 `lib/http.ts` 的 `fetchJson`，pipeline 四支抓取腳本對政府端點統一傳 `{ retries: 5, baseDelayMs: 3000 }`（`http.ts` 本體預設 3 次 / 2s 不改）；本檔另對 `www.tpex.org.tw` 做冷連線預熱（`warmUpTpex()`，`fetchTpexQuotes()` 正式呼叫前打一個允許失敗的 `fetch("https://www.tpex.org.tw/")` 把 DNS/TLS 暖起來——launchd 冷進程碰第二個 host 第一次握手常被 RST）。`pnpm tsx scripts/pipeline/fill-daily-quotes.ts --date=YYYY-MM-DD`。
- **`fill-institutional-trading.ts`**：抓指定日期 TWSE（`T86`，可查任意歷史日）+ TPEx（`tpex_3insti_daily_trading`，只能拿最新一天）三大法人買賣超寫入 `InstitutionalTrading`。只 upsert 已存在的一般股票，不新增股票。對外 fetch 走 `lib/http.ts` 的 `fetchJson`。匯出 `fillOneDayInstitutional(date)`。`pnpm tsx scripts/pipeline/fill-institutional-trading.ts --date=YYYY-MM-DD`。**下游**：`run-signal-scan.ts` 的 setup 籌碼分項 + breakout 階段 `institutionalFlow` 分項。
- **`fill-gap-valuation.ts`**：抓指定日期 TWSE（`BWIBBU_d`）+ TPEx（`peQryDate`）估值寫入 `StockValuation`，兩邊皆可查任意歷史日期。對外 fetch 走 `lib/http.ts` 的 `fetchJson`。匯出 `fillOneDayValuation(date)`。`pnpm tsx scripts/pipeline/fill-gap-valuation.ts --date=YYYY-MM-DD`（不帶參數抓今天）。
- **`fill-margin-trading.ts`**：抓指定日期 TWSE（`MI_MARGN`，`tables[1]` 個股明細）+ TPEx（`margin/balance`，`tables[0]`）個股信用交易餘額寫入 `MarginTrading`，兩邊皆可查任意歷史日期（回傳日期不符即 throw，無 `isStaleDate` 邏輯）。單位換算 ×1000（張→股）。套過濾規則只留一般股票 + 特別股，只 upsert 已存在的 `Stock`。對外 fetch 走 `lib/http.ts` 的 `fetchJson`。`daily-pipeline.ts` 第 3.5 步（非關鍵路徑）。匯出 `fillOneDayMargin(date)`。`pnpm tsx scripts/pipeline/fill-margin-trading.ts --date=YYYY-MM-DD`（不帶參數抓今天）／`--backfill=N`（往回抓、跳非交易日，實得約 N 個交易日；日曆日上限 `max(n+6, n*2)`，大 N 不提早停）。**下游**：`run-signal-scan.ts` 的 `margin-chasing` 警示（見上方 `MarginTrading` 條）。
- **`calculate-technical-indicators.ts`**：依 `DailyQuote` 算 MA5/10/20/60、布林通道、量能均線、`volatility20d`/`maxDrawdown20d`/`atr20`/`rsi14`/`macdStatus`，寫入 `TechnicalIndicator`。匯出 `calculateTechnicalIndicators(codes?: string[], options?: { mode?: "full" | "latest" })`——傳代號陣列只重算那幾支，不傳跑全市場一般股票 + TAIEX。**`mode` 預設 `"full"`**（每支全部歷史 `DailyQuote` 逐日重算並 upsert，回補報價後手動重算 / debug 用，~2149 支約 10 分鐘）；**`"latest"`**（`daily-pipeline.ts` 第 4 步用）每支只撈最近 250 筆當輸入（足夠算 MA60 / MACD EMA 暖身）、只 upsert 最新一筆日期，秒級。**代價**：pipeline 漏跑那天的技術指標不會自動補，需手動 `calculate-technical-indicators.ts <code>` 全歷史重算。CLI 與不帶 `options` 時一律 `full`。`pnpm tsx scripts/pipeline/calculate-technical-indicators.ts`（全市場）或 `... 4104 2330`（指定股票）。
- **`calculate-market-regime.ts`**：大盤濾網（市場狀態燈號）pipeline 薄殼。`calculateOneDayRegime(date, prisma)` → 呼叫 `scripts/lib/market-regime.ts` 的 `calculateMarketRegime` → 原子寫 `data/market-regime/{date}.json`（**不進 DB**：整個市場每天一標籤、資訊量小、盤中多次取允許一天多筆覆寫、不做回測 → 建表不划算）。`daily-pipeline.ts` 第 5 步（非關鍵路徑），CLI `pnpm tsx scripts/pipeline/calculate-market-regime.ts`（無參數跑 DB 最新交易日）／`--date=YYYY-MM-DD`（補算）。**三維度合成三段式已全部上線**（Step 1 breadth + Step 2 indexPosition / ma60Slope），`stage` 由 `hasTaiexIndicatorForDate()` 自動判定（TAIEX 指標補到該日 → `step2-full`；沒有 → 降級 `step1-breadth-only`）。
- **`calculate-industry-heat.ts`**：依 `DailyQuote` 算各產業每日等權熱度寫入 `IndustryHeatSnapshot`（純資料庫計算，零 API）。匯出 `calculateOneDayHeat(date)`。`daily-pipeline.ts` 第 6 步。`pnpm tsx scripts/pipeline/calculate-industry-heat.ts`（最近交易日）或 `--backfill 20`。

### `scripts/screening/` — 選股（手動）

- **`run-signal-scan.ts`（ROADMAP 4.5.3 統一評分管線，唯一的選股腳本）**：跑全市場一般股票 → 單一 gate（`minMarketCap` 30 億 + 當日 `volume` ≥ 1M 股 + `volumeMa20` ≥ 500k 股）→ 依「連續站上布林上軌天數」打階段標籤（`setup` / `breakoutDay` / `extended`）→ 同一份因子分、階段套**不同合併方式**（`setup` = 乘法「籌碼分 × 就緒係數」，下限 0.5；`breakoutDay`·`extended` = 8 分項加權和）→ **各階段各自排名** → 落地 `data/signal-scan-results/{date}.json`（eod）/ `{date}-intraday.json`（realtime，**PLAN 5**：固定檔名每 30 分原子覆蓋，取代舊 `{timestamp}.json`；`writeResult` 先寫 `.tmp` 再 `renameSync`）+ `params`。**資料源自動切換**：查「今天（Asia/Taipei）有 `DailyQuote` 嗎」→ 有 `source: "eod"`（讀 DB）/ 沒有 `source: "realtime"`（打 `mis.twse.com.tw`，缺成交價用當日最高價 `h` 代入、`candleShape` 走 degraded、`priceSource: "estimated"`；`z`/`h` 皆缺跳過）。「觸發量比 ≥ 2」只對 breakout 階段當進榜門檻。匯出 `runSignalScan(date, { prisma?, config?, source?, now? })`（傳 `prisma` 不 `$disconnect`；`config` 為 `DeepPartial<SignalScanConfig>`）。CLI：`pnpm tsx scripts/screening/run-signal-scan.ts`（自動判斷）／`--date=YYYY-MM-DD`（強制 eod）／`--source=realtime|eod`。**獨立手動執行，不進 daily-pipeline**。**三處偏離舊 breakout 預設**：`baseCurve` 深度/時長 = 0.4/0.6（舊 0.6/0.4）、`breakoutMargin` 改單調遞增（> 3% 封頂 100 不倒扣）、7 分項各 ×0.90 勻出 `institutionalFlow: 0.10`。**PLAN 7 跨階段欄位統一**：setup 的 `scores` 補 `relativeStrength`（值 = gate 前全市場算好的 `rsByCode`，合成公式不吃、只給 `lib/latest-scan.ts` 的 `prByCode` 收）；`breakoutDay` / `extended` 補 `preInst`——私有 `buildBreakoutPreInst()` 對這 ~20 檔算 accumulation 籌碼分，用私有 `percentileOf(值, setup 母體升冪陣列, naScore)`（二分找 `count(<=v)/N*100`）**插值不 push 進 setup rankScore 母體**（避免百分位語意改變污染 setup 分數）。`fetchAccumulationRawInputs` code 清單擴為 `[...preCodes, ...breakoutCodes]`。**逐檔 `totalScore` diff = 0 驗證過，不動評分。** **PLAN 8 籌碼面觀察標籤**：setup `preStaged.forEach` 算 `computeTrendReversal` / `computeSingleDayConcentration` → push `"trend-reversal"` / `"concentration"`（取代硬編 `warnings: []`，**setup 首次有非空 warnings**）；`computeBreakoutFactors` 的 `flow.institutionCrowded` → push `"institution-crowded"`；breakout `results.push` 加 `instBackground: classifyInstBackground(...)`（複用 PLAN 7 `accInputs`，0 額外查詢）。逐檔 `totalScore` / `scores.*` / `rank` / `stage` / `stats` diff = 0 驗證過（`--date=2026-09-03`）。
  - **watchlist 成員豁免 gate（PLAN 2，eod + realtime 兩路徑）**：迴圈前撈 `watchlistItem.stockCode` 成 set，gate 四檢查（市值 / 當日量 / 均量 / 觸發量比）+ `sharesOutstanding null` 的 `continue` 全部 `if (!exempt && ...)`。gate 目的是「限制掃描結果數量」，對使用者手動選入的股票放行不違反設計意圖（RS 分數本來就在 gate 之前全市場算，豁免後 6226 這種當日被市值/量門檻篩掉的觀察股仍帶得到完整 `SignalResult` → 強度 PR / 醞釀籌碼不再空白）。`SignalResult.fromWatchlist?: boolean`（是觀察股就帶，含已過 gate 者）；`stats.watchlistExempt`（豁免計數）。**防呆**：exempt 檔若 `volumeMa20` 缺 → 階段降 `setup`。**不動 `signal-factors/*`**（豁免是在 staged 迴圈跳過檢查，不改 `config.gate.*` / `compute*`）。
  - **`SignalScanOutput.watchlistQuotes?: WatchlistQuote[]`（PLAN 2）**：對**所有** watchlist 成員（不只過 gate 的）產出即時報價 `{ code, name, close, changePercent, volume, priceSource, refDate }`。eod = 一次 `dailyQuote.findMany` 查掃描日；realtime = 直接查 `misQuotes` 記憶體 Map（零額外 API）。供 watchlist 頁 PLAN 3 讀（`lib/latest-scan.ts` 的 `watchlistQuotesByCode`）。
- **`_run-signal-scan.ts` 已於 PLAN 4（2026-09-01）`git rm`**——唯一呼叫者是被刪的 `startSignalScan()`。realtime 掃描的背景執行者只剩 `scripts/pipeline/intraday-scan.ts`（launchd）；選股頁「立即掃描」改成 Server Action 內同步跑（卡 UI ~30 秒），不再 spawn。
- **舊三支已於 4.5.4（2026-09-01）退役移除**：`calculate-breakout-strength.ts` / `calculate-accumulation-score.ts` / `check-intraday-breakout.ts` + 孤兒 runner `_run-intraday-scan.ts` 直接 `git rm`（不進 `archive/`——本體只是「串 shared 函式 + 寫 JSON」的 CLI 殼，評分邏輯已整併進 `scripts/lib/signal-factors/`，設計理由在 `docs/PROGRESS.md`，要看舊實作 `git show` 撈得回）。同批：`scripts/lib/breakout-shared.ts` / `accumulation-shared.ts` / `signal-factors.ts`（單檔）刪除，整併成 `scripts/lib/signal-factors/` 目錄（見下方「純函式庫」段）。輸出資料夾 `data/breakout-strength-results/` / `accumulation-score-results/` / `intraday-breakout-snapshots/` 一併刪（僅舊腳本會寫、gitignored、無保留價值）。

### 回測系統 — 已放棄（2026-08-31）

回測系統（ROADMAP 第 3 節，Layer 0～Layer 3 + 訓練/驗證切分 + 完整儀表板 UI）**已從 `main` 移除，且不再撿回**。2026-08-30 曾因效能問題擱置，2026-08-31 決定直接放棄這條路——參數校準改用「肉眼看單日排名 + 實盤觀察」。完整實作凍結在 `feat/backtest-ui-3.6-3.7` 分支（回測第 4 批做完的狀態），僅供參考。移除的檔案：`scripts/backtest/*`、`scripts/lib/backtest-{replay,stats,stats.test,config-hash}.ts`、`lib/actions/backtest.ts`、`app/backtest/*`、`components/{BacktestRunner,BacktestSummary,ForwardReturnsBuilder}.tsx` 及 `components/backtest/*`。

放棄原因：`runBacktestSummary` 進儀表板時把整個 run 的 `raw-factors/*.jsonl` 一次 `JSON.parse` 進記憶體，即使「一年 breakout」也讓 Next dev server heap OOM（8 GB 打滿）。要修得把 Layer 0 讀取整個改成逐日串流，投入產出比不划算。

**留在 `main` 的殘留物**（都是回測前置抽出的，選股腳本正式跑不用，但留著無害、不要刪）：
- `scripts/lib/{breakout,accumulation}-shared.ts` 的 config 三件組（`XxxConfig` / `DEFAULT_XXX_CONFIG` / `resolveXxxConfig`）與 `fetchXxxRawInputs` helper。
- `scripts/lib/types.ts` 的 `DeepPartial<T>`。
- 這些讓「用 config 覆蓋參數跑一次比對排名」這種輕量校準仍可行。

### `scripts/backfill/` — 歷史回補（偶爾手動）

- **`backfill-daily-quotes.ts`**：FinMind 逐支回補歷史報價至 `DailyQuote`（只跑 `securityType=stock` 且 `delistedAt: null`）。回補區間為 `BACKFILL_START_DATE`（預設 `2020-01-01`）到執行當天，實測 FinMind 單支查詢 6 年不會被截斷。`pnpm tsx scripts/backfill/backfill-daily-quotes.ts`（`BACKFILL_LIMIT` 限制測試支數；`BACKFILL_START_DATE=YYYY-MM-DD` 覆蓋起始日）。
- **`backfill-institutional-trading.ts`**：逐支一般股票（排除 `delistedAt` 非 null）用 FinMind 回補歷史三大法人買賣超至 `InstitutionalTrading`（TWSE 可回補任意歷史，TPEx 受端點限制效果有限）。回補區間同上。單支失敗不中斷。`pnpm tsx scripts/backfill/backfill-institutional-trading.ts`（`BACKFILL_LIMIT=10` 小量測試）。**已執行完成（6 年回補）**：2026-08-28 實測 `InstitutionalTrading` 涵蓋 **2020-01-02 → 2026-08-28、約 1476 個交易日、2,662,485 筆、每年 1808~1983 檔**（逐年筆數：2020=349,547／2021=376,427／2022=388,570／2023=392,978／2024=429,336／2025=432,544／2026(至 08-28)=293,083）。與 `DailyQuote`（3,066,466 筆）、`TechnicalIndicator`（3,060,066 筆）同為 2020-01-02 起，可支撐 accumulation 回測拉滿 6 年區間。（舊紀錄「2025-05-02 起 324 個交易日」已過時，是 2026-08-27 6 年回補前的狀態。）
- **`backfill-benchmark-quotes.ts`**：FinMind 回補回測用大盤基準標的的 `DailyQuote`（目前 `BENCHMARK_CODES = ["0050"]`，要加 006208／其他改陣列）。**只寫 `DailyQuote`，不碰 `TechnicalIndicator` / `InstitutionalTrading`**——基準只需要收盤價算報酬。0050 為未還原股價，除息日 `close` 含假跌幅；若基準報酬對除息敏感，日後改抓 `TaiwanStockPriceAdj`。`pnpm tsx scripts/backfill/backfill-benchmark-quotes.ts`。**已執行**：0050 已補 2020-01-02 ~ 今，約 1612 筆。
- **`backfill-index-quotes.ts`**：FinMind `TaiwanStockPrice?data_id=TAIEX` 回補加權指數日線至 `DailyQuote`（`ensureIndexStock` upsert `Stock{ code:"TAIEX", securityType:"index" }`）。照抄 `backfill-benchmark-quotes.ts` 結構（`REQUEST_DELAY_MS`、`BACKFILL_START_DATE` 覆蓋、upsert `DailyQuote`），**只寫 `DailyQuote`**（完整 OHLC）。給大盤濾網算 MA60/帶寬用，不進選股。**匯出 `fillTodayIndex(isoDate, injectedPrisma?)`**（比照 `fillOneDayTwse`：吃 isoDate、可傳 pipeline 的 prisma 單例不 `$disconnect`、回 `{ processed, isNonTradingDay }`；FinMind 非交易日不回該日 row → `isNonTradingDay: true`）供 `daily-pipeline.ts` 第 1.5 步呼叫；全區間回補 `main()` 加 `import.meta.url === file://${process.argv[1]}` guard，被 import 時不觸發。全區間回補：`pnpm tsx scripts/backfill/backfill-index-quotes.ts`（`BACKFILL_START_DATE=YYYY-MM-DD` 覆蓋，預設 `2020-01-01`）。**已執行**：TAIEX 已補 2020-01-02 ~ 今，1619 筆。
- **`update-shares-outstanding.ts`**：下載 MOPS 股本 CSV 更新 `Stock.sharesOutstanding`。**獨立手動執行，月頻，不進 daily pipeline**。`pnpm tsx scripts/backfill/update-shares-outstanding.ts`。
- **`mark-delisted.ts`**：維護 `Stock.delistedAt` 下市標記。**獨立手動執行，月頻，不進 daily pipeline**——復牌股行情入庫不依賴 flag（`fill-daily-quotes` 是 API 回什麼就 upsert 什麼），flag 消費者（覆蓋率分母 / FinMind backfill 名單 / realtime MIS 清單）遲鈍幾週無害。規則：最後 `DailyQuote` 距 DB 最新交易日 **> 60 日曆日**（或從無行情）→ 標記 `delistedAt` = 最後行情日（從無行情 = `2020-01-01`）；反向若已標記但 60 天內有新行情 → 清回 null（復牌自癒，減資/分割型停牌約兩週不會誤標）。首輪（2026-09-01）標記 170 檔、存續 1,978 檔。`pnpm tsx scripts/backfill/mark-delisted.ts`。

### `scripts/lib/` — 純函式庫

- **`signal-factors/`（ROADMAP 4.5.3 統一因子庫，4.5.4 目錄化）**：`run-signal-scan.ts` 與 `lib/actions/dashboard.ts` 的單一 import 點（都 import `signal-factors/index`）。4.5.4（2026-09-01）把原「`signal-factors.ts` 薄 re-export 層 + `breakout-shared.ts` / `accumulation-shared.ts` 本體」整併成這個目錄，舊三支同批退役移除（見 `scripts/screening/` 段）。**子檔分工**：
  - `index.ts` — barrel（`export * from` 各子檔）。
  - `util.ts` — `clip` / `rankScore`（原本兩個 shared 檔各一份重複，合併）。
  - `breakout.ts` — 原 `breakout-shared.ts`：`GATES` / `WEIGHTS` / `TRIGGER_VOLUME_RATIO` 等常數、`BreakoutConfig` / `DEFAULT_BREAKOUT_CONFIG` / `resolveBreakoutConfig`、7 項 breakout 評分函式（`computeVolumeStrength` / `computeBreakoutMargin` / `computeFirstBar` / `computeBase` / `computeProximityToHigh` / `computeMarketWideReturns` / `computeCandleShape`）、撈 DB helper（`fetchTodayQuotes` / `fetchIndicatorsForDate` / `fetchHistoryWindow` / `fetchBreakoutRawInputs`）。曲線轉折點已抽進 `config.score.curves`：`computeVolumeStrength`（2×→40/6×→100）、`computeBreakoutMargin`（3% 轉折 + 每 1% 扣 5 分 + 下限 60）、`computeBase`（0.6/0.4 權重 + duration 封頂 40 天）；`computeCandleShape` / `computeProximityScale` / `computeFirstBar` / `computeBase` 的 p25 門檻維持寫死。`GATES.minMarketCap` 為 30 億。
  - `accumulation.ts` — 原 `accumulation-shared.ts`：視窗/門檻/權重常數（`INSTITUTIONAL_WINDOW_DAYS=20`、`SQUEEZE_VOLUME_WINDOW_DAYS=5`、`MIN_AVG_VOLUME_SHARES`、`READINESS_FLOOR` 等）、`AccumulationConfig` / `DEFAULT_ACCUMULATION_CONFIG` / `resolveAccumulationConfig`、`computeTrustRawMetrics` / `computeOtherInstitutionRatio` / `computeQuietVolumeRatio` / `combineChipScore` / `combineTrustScore` / `computeReadinessCoefficient` / `combineFinalScore`、`fetchAccumulationRawInputs`。壓縮度分數沿用 `breakout.ts` 的 `computeBase`；`BASE_MIN_HISTORY_DAYS` 改從 `breakout.ts` import（單一數值來源，原本各自寫死 40）。
  - `institutional.ts` — `computeInstitutionalFlow()`（三大法人流向：近 5 日投信+外資淨買超各 ÷ `volumeMa20` → clip `[0,0.5]` 線性 0~100 → 投信 0.6/外資 0.4；突破當日淨賣超 → `min(score, 40)`；當日法人 null → 不封頂但 degraded。**另收 `marginSurgePercentile` 輸入、回 `marginChasing` 布林**；**PLAN 8 §4 再回 `institutionCrowded` 布林**——`(trustRatio + foreignRatio) > crowdedThreshold(0.8)`。兩者**都不動 score**，只給呼叫端掛 `SignalResult.warnings`）、`computeMarginSurgePercentile()`（純函式：融資餘額近 `lookbackDays`(5) 日累積變化率，對「這檔過去 `historyWindowDays`(40) 天各自的同種變化率」母體取百分位，**用嚴格小於**避免平盤誤觸發；母體有效樣本 < `minHistoryDays`(20) → null）、**`classifyInstBackground()`（PLAN 8 §6）**——比較近 5 日 vs 前 15 日投信淨買超方向回 `"positioned-early" | "fresh-entry" | null`（中性背景非風險，不進 warnings）。`InstitutionalFlowConfig` 上方有「突破階段刻意排除自營商」設計理由註解（PLAN 8 §7：5 日短窗下自營 delta hedging 噪音佔比過高）。
  - `accumulation.ts` — 除既有籌碼/技術評分外，**PLAN 8 檔尾兩個純函式**：`computeTrendReversal()`（投信近 5 日淨賣超合計 < 0 ∧ 前 15 日 > 0 → `"trend-reversal"`；偵測 20 日等權加總看不到的時間結構轉折）/ `computeSingleDayConcentration()`（外資+自營窗內單一交易日買超 ÷ 窗內總正買超 > 0.5 → `"concentration"`；分母用「總正買超」不用淨買超合計，避免賣日壓低分母假陽性）。皆吃新到舊序列、不查 DB、只回布林。
  - `staging.ts` — `consecutiveAboveBand()`（階段判定 helper，回 `{ ok, latestAboveBand, consecutiveDays }`；`breakout.ts` 的 `computeFirstBar` 本體不動）、`computeBreakoutMarginMonotone()`（§2.5：> 3% 封頂 100 不倒扣，只讀 `kneePct`；`breakout.ts` 的 `computeBreakoutMargin` 不動）。
  - `config.ts` — `SignalScanConfig` / `DEFAULT_SIGNAL_CONFIG` / `resolveSignalConfig`（三處偏離舊 breakout：`baseCurve` 0.4/0.6、`breakoutMargin` monotone、7 項各 ×0.90 勻出 `institutionalFlow: 0.10`；`breakout.institutionalFlow.marginChasing` 子區塊 = `margin-chasing` 觸發參數，首版拍腦袋待校準）、`baseCurveFor()`。**PLAN 8 新增欄位**（全未校準，見 `docs/PLAN.md` PLAN 8 §14）：`preBreakout.trendReversal { recentDays: 5, priorDays: 15, minDataDays: 12 }` / `preBreakout.concentration { thresholdRatio: 0.5, minWindowDays: 10, minTotalNetBuy: 0 }` / `breakout.institutionalFlow.crowdedThreshold: 0.8` / `breakout.institutionalFlow.background { recentDays: 5, priorDays: 15, activityEpsilon: 0.02, minDataDays: 12 }`。
  - 單測 `signal-factors/factors.test.ts`（`node:test` + `pnpm tsx --test scripts/lib/signal-factors/factors.test.ts`，66 案——含 PLAN 8 的 `computeTrendReversal` / `institutionCrowded` / `computeSingleDayConcentration` / `classifyInstBackground`）。
- **`mis-quotes.ts`（MIS 即時報價抓取，純 Node 函式庫）**：2026-08-31 從當時的 `check-intraday-breakout.ts` 抽出（該檔已於 4.5.4 退役移除），現由 `run-signal-scan.ts` 的 realtime 路徑使用。`fetchMisBatch` / `fetchAllMisQuotes(stocks, onBatch?)` / `parseMisDate` / `computeElapsedRatio` / `MisQuote` 型別 / `BATCH_SIZE`(120) / `BATCH_DELAY_MS`(1500)。缺成交價（`z` 為 `-`/缺）的 row **不跳過**、`price: null` 保留，由呼叫端決定代入（`run-signal-scan.ts` 用 `h`）。**PLAN 5**：`fetchMisBatch` 一批失敗（`!res.ok` / `catch`）會 `sleep(BATCH_RETRY_DELAY_MS=2000)` 後重試（`BATCH_RETRIES=2` 總嘗試 2 次；`failed: false` 第一次就 return 不進重試）——開盤前 / 睡眠喚醒的整批掛多半重試一次就過。`intraday-scan.ts`（launchd）與 `getScreeningResult({ force: "realtime" })`（手動立即掃描）都走 `fetchAllMisQuotes` → `fetchMisBatch`，一起受益。
- **`security-type.ts`**：`toSecurityType(code, name)` 證券分類單一出處（2026-09-01 從三支 pipeline 抓取腳本的重複複製整併）。判斷順序見上方「過濾規則」條——「4 碼數字 → stock」刻意排在名稱購/售判斷之前。單測 `security-type.test.ts`（`pnpm tsx --test scripts/lib/security-type.test.ts`，7 案）。
- **`http.ts`**：`daily-pipeline.ts` 四個抓取步驟共用的 HTTP 工具（無 Prisma/CLI）。匯出 `fetchJson<T>(url, options?)`：每次嘗試帶 `AbortSignal.timeout`（預設 30s），對網路層拋錯（`ECONNRESET`/`terminated`/timeout）與 5xx/429 自動退避重試（預設 3 次，2s→4s），對其他 4xx 不重試。存在理由：pipeline 無人值守執行，裸 fetch 遇一次 TLS 連線中斷就整條掛掉（2026-08-28 事故）。**回應內容判斷（非交易日 `stat`/`totalCount`/空陣列）仍留在各呼叫端**。**pipeline 四支抓取腳本（`fill-daily-quotes` / `fill-institutional-trading` / `fill-gap-valuation` / `fill-margin-trading`）對政府端點的 `fetchJson` 呼叫統一傳 `{ retries: 5, baseDelayMs: 3000 }`**（總 5 次、3s→6s→12s→24s；`http.ts` 本體預設值不改，只在呼叫端覆蓋——backfill 等手動腳本沒必要一律變激進）；`fill-daily-quotes.ts` 另對 `www.tpex.org.tw` 做冷連線預熱（launchd 冷進程事故 2026-09-01）。`backfill-*.ts` 等手動腳本目前未接。
- **`types.ts`**：純型別工具，目前只有 `DeepPartial<T>`（遞迴可選，`resolve*Config(override?)` 與回測引擎共用）。
- **config 三件組（`signal-factors/breakout.ts` / `accumulation.ts` 各一套，回測參數化前置，ROADMAP 3.1）**：各匯出 `XxxConfig` 型別（分 `gate` 門檻類 + `score` 加權/曲線/視窗類兩子物件）、`DEFAULT_XXX_CONFIG`（從既有 `export const` 常數組出來，舊常數全保留當單一數值來源）、`resolveXxxConfig(override?: DeepPartial<XxxConfig>)`（手寫 2 層展開合併）。**門檻類走 `gate`**（`minMarketCap` / `minVolumeShares` / `triggerVolumeRatio` / `minAvgVolumeShares`，決定誰進候選池）、**加權/曲線/視窗/degraded 門檻走 `score`**（不剔除股票，只改分數怎麼組）。純函式一律吃「該函式需要的最小 config 片段」，不讀 module-level 常數。（`run-signal-scan.ts` 正式跑吃的是 `config.ts` 的 `SignalScanConfig`，這兩套 `XxxConfig` 現只剩「用 config 覆蓋參數跑一次比對排名」的輕量校準會用到。）
- **`market-regime.ts`**：大盤濾網（市場狀態燈號）純函式庫（無 CLI，允許查 DB，比照 `signal-factors/breakout.ts`）。`calculateMarketRegime(date, { prisma, config? })` → `MarketRegimeResult`（`label: "bullish"|"neutral"|"bearish"` / `totalScore` / `dimensions.{indexPosition,ma60Slope,breadth}` 各為 `DimensionScore | null` / `stage`）。三維度各投 `-1|0|1`：**breadth** = 該日全市場 `securityType="stock"` 有 `DailyQuote` 的股票中 `close > 當日 TechnicalIndicator.ma60` 佔比，`>55%` +1 / `<45%` -1 / 分母 <500 → `degraded`；**indexPosition** = TAIEX 收盤 vs 自己 MA60，連 `indexPosBufferDays`(=3) 日都在上／下才 ±1，混合則 0（過濾 whipsaw）；**ma60Slope** = TAIEX 的 MA60 近 `ma60SlopeLookbackDays`(=5) 交易日斜率 `(今−N日前)/N日前`，`>±0.5%` → ±1。`stage` 由 `hasTaiexIndicatorForDate()` 自動判定（有 TAIEX 該日 `ma60` → `step2-full` 三票合成、`totalScore >= ±2` → bullish/bearish；沒有 → `step1-breadth-only` 只看 `breadth.score`，優雅降級）。門檻集中在 `DEFAULT_REGIME_CONFIG`（首版未校準）。查 DB helper 同檔：`fetchBreadthInputs` / `fetchTaiexCloseVsMa60` / `fetchTaiexMa60Series` / `hasTaiexIndicatorForDate`。**三維度已全部上線。**

（`backtest-replay.ts` / `backtest-stats.ts` / `backtest-stats.test.ts` / `backtest-config-hash.ts` 隨回測系統放棄移出 `main`，見上方「回測系統 — 已放棄」段。）

### `scripts/archive/` — 已停用，不維護

- **`calculate-screen-score.ts`**：全市場九因子加權排序（value/size/liquidity/momentum/reversal/activity/stability/theme_heat/topic_alignment）。已由 breakout + accumulation 兩支取代，停用。
- **`run-screener.ts` + `screener-conditions.json`**：選股 v1（布林 `bollinger_breakout`/`volume_surge`/`bandwidth_squeeze` 條件式篩選）。已由 `run-signal-scan.ts` 的階段化評分邏輯取代，停用。
- **`fetch-candidate-details.ts`**：讀 v1 的 `data/screener-results/{date}.json` 候選股清單逐支抓籌碼/月營收/季報/新聞。輸入來源（v1 篩選器）已停用，一併封存；若日後要對候選股做深度抓取，改讀 `run-signal-scan.ts` 輸出重寫。
- **`top20-gainers.js`**：最早的一次性玩具，抓當日資料印漲幅前 20 名。功能已被 `run-signal-scan.ts` 涵蓋。

各腳本的設計理由、實測數字、修正過程記錄在 [docs/PROGRESS.md](docs/PROGRESS.md)，這裡只列現況。

## docs/PROGRESS.md 維護

完成功能、修 bug、或做出設計調整後，順手更新 [docs/PROGRESS.md](docs/PROGRESS.md) 對應段落，不要留給使用者提醒才補。這份文件是「目前進度」的詳細記錄（逐次累積，含實測數字與驗證過程），跟 CLAUDE.md（穩定知識）、`docs/ROADMAP.md`（打勾式任務清單）、`docs/PLAN.md`（單一任務規格書，做完即被取代）角色不同，不要混用。

## README.md 維護

修改功能、新增腳本、調整環境需求或專案結構時，順手檢查 [README.md](README.md) 是否還反映目前狀態（尤其是「目前功能」與「使用方式」區塊），過時就一併更新，不要留給下次對話才發現。

## docs/ROADMAP.md 維護

完成 ROADMAP 上的某個 todo 項目後，順手把對應的 `- [ ]` 打勾成 `- [x]`，不要留給使用者自己對照勾選。若某個階段（如「1. 盤後選股 v2」）底下所有項目都打勾完成，在該次回覆裡明確提醒使用者這個階段已全部完成；若整份 ROADMAP 所有階段都完成，額外提醒使用者可以考慮規劃下一輪內容。這份文件是持續累積更新的（做完的項目打勾保留、不刪除），跟 `docs/PLAN.md`（單一任務的實作規格書，做完即被下一份取代）角色不同，不要混用或互相覆蓋內容。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
